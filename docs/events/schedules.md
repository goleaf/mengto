# Event Schedules

An event has authoritative UTC timestamps and an IANA timezone; occurrences
can override date, format, location, capacity, and status. The Livewire
workspace lists a bounded occurrence set.

Tracks, rooms, sessions, speakers, reservations, waitlists, reordering, and
conflict detection are not implemented. No existing calendar card is labelled
as a full event schedule builder.
