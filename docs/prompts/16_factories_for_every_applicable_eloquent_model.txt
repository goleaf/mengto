# 16. Factories for Every Applicable Eloquent Model

## Principal-agent role

You are the principal Codex implementation agent responsible for creating valid, deterministic, expressive, constraint-aware factories and meaningful states for every first-party Eloquent model.

## Mission

Inventory every first-party Eloquent model and build or repair its factory so the default definition creates a valid minimal record. Add meaningful states and explicit relationship helpers, create local file fixtures, support parallel tests, document justified exemptions, and prove every factory/state against real database constraints.

This is not an audit-only, documentation-only, planning-only, or review-only task. Discovery and planning are mandatory intermediate phases. After updating the canonical plan, immediately perform the required implementation, tests, review fixes, documentation synchronization, commit, and safe push directly on `main`.

## Repository-wide technology baseline

Preserve and enforce this target unless a stricter, current repository requirement applies:

- PHP 8.5.
- Laravel 13, using the latest stable mutually compatible 13.x release available at execution time.
- Livewire 4, using the latest stable mutually compatible 4.x release available at execution time.
- Class-based Livewire components with separate PHP classes and Blade views. Do not use Volt.
- Tailwind CSS 4, using the latest stable mutually compatible 4.x release available at execution time.
- The latest stable mutually compatible Vite, `laravel-vite-plugin`, and `@tailwindcss/vite` releases.
- Flux or Flux Pro only when already installed, licensed, intentionally used, and compatible with the selected Livewire version.
- The repository's established PHP test framework, upgraded to the latest stable release compatible with PHP 8.5 and Laravel 13.
- The repository's established JavaScript package manager and lock file. Never introduce a second lock file.

Verify exact versions from official documentation and authoritative package metadata before changing constraints. Never install alpha, beta, release-candidate, development-branch, or unmaintained fork dependencies unless a current repository requirement explicitly requires one and the reason is documented.

## Non-negotiable operating protocol

1. **Work only on the existing `main` branch.**
   - Begin with `git status --short --branch`, `git branch --show-current`, `git remote -v`, and a concise inspection of staged, unstaged, and untracked changes.
   - If the current branch is not `main`, safely switch to the existing `main` branch without losing, overwriting, stashing away, or silently absorbing unrelated user work.
   - Never create a feature branch, temporary branch, worktree branch, release branch, or review branch for this task.

2. **Protect the working tree.**
   - Preserve all unrelated existing changes.
   - Do not use `git reset --hard`, `git clean -fd`, `git checkout -- .`, force push, history rewriting, or any command that can discard work.
   - Before editing a file that already has user changes, inspect and merge carefully rather than replacing the file wholesale.
   - Commit only task-owned changes. Do not claim unrelated user changes as your work.

3. **Load governing instructions before work.**
   - Read the applicable `AGENTS.md` and `AGENTS.override.md` instruction chain from repository root to the current directory.
   - Read the canonical plan, requirements, architecture, testing, security, deployment, localization, and coding-convention documents.
   - Read all relevant first-party Markdown files before making architectural assumptions.
   - Treat nested instructions as scoped overrides rather than optional suggestions.

4. **Use specialized subagents for discovery.**
   - Spawn every required analysis subagent listed in this prompt.
   - Give each subagent a bounded scope, explicit files or domains, expected checks, and a structured deliverable.
   - Run independent scopes in parallel when safe.
   - Do not allow multiple subagents to edit the same files concurrently.
   - The principal agent remains the sole integrator and decision owner.

5. **Do not ask the user questions.**
   - Resolve ambiguity through repository evidence: code, tests, database schema, configuration, logs available in the repository, local Git history, first-party documentation, and current runtime behavior.
   - Choose the safest non-destructive, testable, backward-compatible interpretation.
   - Record material assumptions in the plan or an ADR and continue.
   - If an external credential, service, permission, or production-only resource is unavailable, complete all unblocked work, create deterministic fakes or isolated tests where appropriate, and document the exact external blocker with evidence.

6. **Quality is more important than speed.**
   - Work in small, reviewable, dependency-aware passes.
   - Inspect the diff after each meaningful pass.
   - Run targeted checks immediately after each change set.
   - Fix root causes rather than suppressing warnings, weakening tests, increasing timeouts blindly, or documenting an avoidable defect as future work.

7. **The plan is a mandatory gate, not the final deliverable.**
   - Locate the canonical execution plan. Prefer the existing canonical file; if none exists, create `PLANS.md` at repository root.
   - Do not create competing plan files.
   - Update the plan after discovery and before modifying production code.
   - Immediately begin implementation after saving the updated plan. Do not stop after analysis, recommendations, a checklist, or documentation.
   - Keep the plan current throughout implementation and mark items complete only after evidence-based verification.

8. **Implement, test, review, fix, and re-verify.**
   - Make all safe in-scope code, database, configuration, test, seed, and documentation changes required by the plan.
   - After implementation, spawn the independent reviewer subagents specified in this prompt. Reviewers must not be the same agents that implemented the change.
   - Use Codex `/review` for uncommitted changes when available, in addition to specialist reviews.
   - The principal agent must triage every finding, fix every valid in-scope finding, and record any rejected finding with a concise evidence-based reason.
   - Re-run all affected checks after fixes.

9. **Never fabricate evidence.**
   - Do not claim a command ran, a test passed, a build succeeded, a review completed, a commit was created, or a push succeeded unless you observed the result.
   - Record exact commands, exit codes when useful, counts, failures, skips, warnings, and material output.
   - Distinguish pre-existing failures from regressions introduced by this task.

10. **Finish documentation and Git work.**
    - Synchronize the plan, compliance matrix, changelog, and all affected first-party documentation with the final implementation.
    - Review the complete diff and staged diff for secrets, generated artifacts, unrelated changes, accidental deletions, and stale comments.
    - Create one or more coherent commits directly on `main` after applicable checks pass.
    - Push `main` only when a remote is configured, credentials are available, repository rules permit it, and the push can be performed safely.
    - Report the actual commit hashes and push result.

## Subagent coordination contract

The principal agent must create a work ledger before delegation. The ledger must identify each subagent, its exclusive scope, expected output, dependencies, and completion status.

Every analysis subagent must return a concise structured report containing:

1. Scope inspected.
2. Files, classes, routes, tables, components, or workflows examined.
3. Confirmed findings, each with severity and evidence.
4. Suspected findings that require principal-agent validation.
5. Missing coverage or unavailable evidence.
6. Recommended implementation order.
7. Suggested tests and verification commands.
8. Risks of changing the affected area.

Subagents must not dump raw logs without interpretation. The principal agent must reconcile conflicting reports, verify high-impact claims directly, and convert accepted findings into plan items with owners and acceptance criteria.

## Required analysis subagents and detailed roles

Spawn at least the following specialized subagents. Give each one the exact mission, scope, checks, deliverable, and restrictions below.

### Model and Factory Inventory Analyst

- **Mission:** Enumerate every first-party Eloquent model and current factory coverage.
- **Scope:** Models, factory classes, migrations, relationships, seeders, tests, package-owned models, and abstract/pivot models.
- **Must inspect:**
  - Missing factories, wrong namespaces/discovery, abstract or read-only models, pivot treatment, and factories pointing to obsolete attributes.
  - Models instantiated indirectly only.
  - Exemption candidates.
- **Required checks:**
  - Prove model ownership and instantiability.
  - Map factory use in tests/seeders.
  - Classify required versus justified exemption.
- **Required deliverable:**
  - A model-to-factory inventory.
  - Missing/broken coverage.
  - Exemption proposals with evidence.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Domain State and Enum Factory Analyst

- **Mission:** Identify meaningful model states from requirements, enums, workflows, policies, and tests.
- **Scope:** Status enums/constants, state transitions, validation, policies, seeders, fixtures, and existing factory states.
- **Must inspect:**
  - Missing states, meaningless generic states, invalid combinations, outdated enum values, and implicit transition prerequisites.
  - Active/inactive, pending/approved/rejected, public/private, verified/unverified, completed/failed/cancelled, archived/expired, blocked/suspended, soft-deleted, and role states where meaningful.
  - State naming consistency.
- **Required checks:**
  - Build a valid-state matrix per model.
  - Identify mutually exclusive or dependent states.
  - Require state definitions to satisfy constraints.
- **Required deliverable:**
  - A meaningful state catalog.
  - Invalid/obsolete states to remove.
  - Tests required per state.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Relationship and Object-Graph Factory Analyst

- **Mission:** Design explicit relationship helpers without hidden explosive object graphs.
- **Scope:** Belongs-to, has-many, many-to-many, polymorphic, through, pivot, tenant, owner, parent/child, media, and permission relationships.
- **Must inspect:**
  - Default factories recursively creating large graphs, circular factory calls, missing required parents, tenant mismatch, and ambiguous polymorphic targets.
  - Relationship helpers needed for common workflows.
  - Callbacks with hidden side effects.
- **Required checks:**
  - Define minimal default parents.
  - Use explicit `for...`/`with...` states for optional graphs.
  - Verify same-tenant and ownership consistency.
- **Required deliverable:**
  - A graph-creation strategy.
  - Relationship helper list.
  - Cycle/performance risk findings.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Constraint and Uniqueness Factory Analyst

- **Mission:** Ensure generated data respects schema, validation, domain invariants, and parallel execution.
- **Scope:** Unique columns, composite unique keys, foreign/check constraints, enums, money, dates, slugs, usernames, tenant-scoped keys, and factories.
- **Must inspect:**
  - Random collisions, invalid date ordering, floating money, unsupported enum values, nullable/default mismatches, and globally unique data where uniqueness is tenant-scoped.
  - Faker providers incompatible with locale or parallel workers.
  - Sequence assumptions.
- **Required checks:**
  - Test repeated and parallel creation.
  - Use deterministic uniqueness strategies.
  - Check database-engine differences.
- **Required deliverable:**
  - A constraint compatibility report.
  - Required generation strategies.
  - Parallel-test risks and fixes.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### File, Image, and External-Payload Fixture Analyst

- **Mission:** Create local deterministic fixtures for models requiring files, images, documents, JSON, or provider responses.
- **Scope:** Upload models, media relations, filesystem disks, image processing, import payloads, webhook fixtures, API client tests, and seeders.
- **Must inspect:**
  - Internet-dependent factories, real personal data, oversized fixtures, invalid MIME/content mismatch, and filesystem leakage.
  - Fixture ownership and cleanup.
  - Multiple formats/states needed.
- **Required checks:**
  - Use generated local fixtures and fake disks.
  - Verify content and dimensions.
  - Separate test fixtures from demo assets.
- **Required deliverable:**
  - A fixture registry.
  - Required generated files/payloads.
  - Cleanup and reuse strategy.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Factory Determinism and Performance Analyst

- **Mission:** Keep factories reproducible, fast, memory-bounded, and suitable for large test suites.
- **Scope:** Factory callbacks, event dispatch, observers, password hashing, file generation, relationships, and test setup.
- **Must inspect:**
  - Expensive hidden operations, external calls, nondeterministic time, random flaky values, and event side effects.
  - Factory states that create unnecessary data.
  - Parallel-test file/name collisions.
- **Required checks:**
  - Measure representative creation cost.
  - Use frozen time or explicit values when needed.
  - Provide explicit high-volume strategies.
- **Required deliverable:**
  - A performance/determinism report.
  - Required simplifications.
  - Volume fixture recommendations.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Factory Test Architect

- **Mission:** Create comprehensive automated proof that every factory and meaningful state produces valid records.
- **Scope:** Factories, states, models, schema, relationships, parallel test config, and coverage tooling.
- **Must inspect:**
  - Factories never directly tested, states with no callers, invalid constraints, and hidden network/filesystem effects.
  - Missing assertions for tenant and ownership consistency.
  - Exemptions without tests.
- **Required checks:**
  - Generate data-driven tests over inventory.
  - Test relationship helpers and repeated/parallel creation.
  - Verify fake disk and event behavior.
- **Required deliverable:**
  - A factory-test matrix.
  - Generic and model-specific test design.
  - Exact exemption checks.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

## Mandatory plan update before implementation

Update the canonical plan with a dedicated section for this task. At minimum include:

- The complete first-party model inventory and factory status.
- A justified factory exemption list with model, reason, creation method, requirement, and test.
- Minimal valid default attributes and required relationships for each model.
- Meaningful states and valid/invalid combinations for each status/role/workflow model.
- Explicit relationship helpers and object-graph ownership.
- Constraint, uniqueness, date, money, enum, tenant, and parallel-test strategies.
- Local files, images, documents, JSON, webhook, and provider-response fixtures.
- Automated factory/state/helper validity and performance tests.

Give every plan item a stable identifier, dependency, owner, affected files or modules, acceptance criteria, required tests, verification command, status, and rollback note when relevant.

Save the plan, then immediately begin implementation. Do not return a plan to the user and wait for approval.

## Domain-specific guardrails

- Do not create a fake factory for abstract, external read-only, database-view, or technically non-instantiable models; document a precise exemption instead.
- Do not make default factories create large hidden graphs or external side effects.
- Do not rely on the public internet, real user data, production credentials, or nondeterministic provider responses.
- Do not create generic states that violate the actual domain or duplicate enum values without behavior.
- Do not silence database constraints for factory convenience.

## Required implementation scope

Complete all applicable work below. Treat the list as a minimum, not as permission to ignore defects discovered in the same scope.

- Create or repair a factory for every applicable first-party Eloquent model and ensure framework discovery works.
- Make each default factory produce the smallest valid record that satisfies schema, foreign keys, enums, ownership, tenant, date-order, money, and domain constraints.
- Add meaningful named states for real workflow/status/privacy/verification/role/archive/failure/completion conditions only where applicable.
- Create explicit relationship helpers such as `forUser`, `forTenant`, `forCompany`, `forParent`, `withChildren`, `withMedia`, `withPermissions`, and `withStatus` where they match actual models.
- Prevent circular/recursive graph creation and hidden large datasets; make optional complex graphs explicit.
- Use deterministic uniqueness, sequences, frozen time, explicit values, and parallel-safe filesystem paths where needed.
- Create local generated fixtures for images, documents, uploads, JSON imports, webhook events, and external provider responses.
- Ensure factories do not make real network calls, unexpectedly send notifications, or write outside fake/test storage.
- Create data-driven tests that instantiate every factory, meaningful state, and relationship helper and assert database constraints, enum validity, tenant ownership, and file existence.
- Document factory ownership, states, fixtures, exemptions, and preferred usage patterns.

## Required verification

Run the repository's real commands rather than inventing command names. Perform all safe applicable checks below and capture exact results:

- Automated inventory assertion that every applicable first-party model has a discoverable factory or documented exemption.
- A valid-record test for every default factory.
- A valid-record and state-invariant test for every meaningful factory state.
- Relationship helper tests, including same-tenant and ownership consistency.
- Repeated and parallel creation tests for unique values and file paths.
- Foreign-key, unique, check-constraint, enum, money, and date-order validation.
- Fake filesystem tests for every file/media fixture and a no-real-network guarantee.
- Full PHP suite, fresh migration, seed run, static analysis, formatter, and performance sampling for high-use factories.

## Independent review protocol

After implementation and before committing:

1. Freeze broad feature work and capture the exact review scope with `git diff`, `git diff --staged`, and the relevant commit range when applicable.
2. Spawn every reviewer subagent listed below. Reviewer agents must be independent from implementation agents.
3. Run Codex `/review` against uncommitted changes when available.
4. Require reviewers to inspect behavior, not only style.
5. Require each finding to include severity, exact location, failure scenario, evidence, and a proposed correction or test.
6. Have the principal agent reproduce or validate each material finding.
7. Fix every valid in-scope finding.
8. Add a regression test for every behavior defect when practical.
9. Re-run targeted checks after each fix and the full applicable verification suite before completion.
10. Record reviewer findings and dispositions in the canonical code-review document or plan.

## Required independent reviewer subagents

### Factory Validity Reviewer

- **Review mission:** Independently instantiate every factory and detect invalid defaults, hidden side effects, and discovery problems.
- **Scope:** All factories, models, migrations, callbacks, fixtures, and tests.
- **Must inspect:**
  - Constraint violations, incorrect attributes, recursive graphs, network calls, real storage writes, and flaky uniqueness.
  - Factories that pass only because events/constraints are disabled.
  - Exemptions that are not justified.
- **Required checks:**
  - Run inventory-driven creation.
  - Inspect callbacks and observers.
  - Use multiple database workers/iterations where practical.
- **Required deliverable:**
  - Validity findings.
  - Required default/callback fixes.
  - A factory coverage verdict.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### Domain State Reviewer

- **Review mission:** Verify that factory states represent valid domain conditions and do not create impossible combinations.
- **Scope:** Enums, state transitions, policies, validation, states, seeders, and tests.
- **Must inspect:**
  - Missing important states, obsolete states, contradictory attributes, and role/privacy/status drift.
  - State names that misrepresent behavior.
  - Transitions not represented.
- **Required checks:**
  - Compare states to canonical requirements.
  - Instantiate combinations and test invariants.
  - Review policy behavior with states.
- **Required deliverable:**
  - State-model findings.
  - Required state additions/removals.
  - A domain fidelity assessment.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### Constraint and Parallelism Reviewer

- **Review mission:** Stress factory data generation under constraints, repeated runs, and parallel test execution.
- **Scope:** Unique generation, composite keys, files, tenant data, sequences, parallel setup, and tests.
- **Must inspect:**
  - Collisions, race conditions, shared files, database-engine differences, and time-dependent flakes.
  - Overly slow hashing or graph creation.
  - Test isolation failures.
- **Required checks:**
  - Run repeated/parallel creation.
  - Inspect deterministic overrides.
  - Review cleanup behavior.
- **Required deliverable:**
  - Parallelism findings.
  - Required uniqueness/isolation fixes.
  - Performance notes.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

## Required documentation synchronization

Update the canonical plan plus all affected first-party documentation. At minimum review and update, when present or applicable:

- The canonical plan, compliance matrix, and changelog.
- Factory, testing, seeding, data-model, file-fixture, and contribution documentation.
- A factory coverage matrix listing model, factory, states, helpers, fixtures, tests, and exemptions.
- AGENTS.md rules requiring factories for new models and valid default definitions.
- ADRs for unusual model-exemption or deterministic-ID strategies.

Remove stale claims and commands. Keep useful historical changelog information. Ensure documentation describes the implemented and verified state, not the intended future state.

## Definition of done

This prompt is complete only when all applicable conditions are true:

- Every applicable first-party Eloquent model has a valid discoverable factory or a precise justified exemption.
- Default definitions satisfy real schema and domain constraints without large hidden graphs or side effects.
- Every meaningful model state has a valid named factory state and tests.
- Relationship helpers create coherent owner/tenant graphs explicitly.
- Fixtures are local, deterministic, fake-storage-safe, and network-independent.
- Repeated and parallel factory creation is reliable.
- Independent validity, domain-state, and parallelism reviewers are resolved, and changes are documented, committed, and safely pushed on `main`.

## Required final report

Return one factual report with these sections:

1. Branch and initial working-tree state.
2. Applicable instructions and canonical plan file.
3. Analysis subagents created, their scopes, and the important findings accepted or rejected.
4. Plan changes made before implementation.
5. Implementation summary by module and file group.
6. Database, security, performance, accessibility, localization, factory, seeder, and test impact where applicable.
7. Exact verification commands and observed results.
8. Independent reviewers created, findings, fixes, and remaining rejected findings with reasons.
9. Documentation synchronized.
10. Final Git status, commit hashes, and factual push result.
11. Remaining limitations, containing only genuine external or environmental blockers with exact affected requirement or plan identifiers.

Do not describe planned work as completed. Do not hide unfinished in-scope work in a limitations section.

## Start now

Read the applicable instruction chain and first-party documentation, inspect the repository, create the required analysis subagents, update the canonical plan, and then immediately implement the complete in-scope plan. Do not stop after analysis or planning.
