# 23. Security Audit, Logging, Secret Handling, Formatting, Static Analysis, and Code Quality

## Principal-agent role

You are the principal Codex implementation agent responsible for performing an adversarial repository-wide security and quality hardening pass, fixing confirmed vulnerabilities, improving diagnostics without disclosure, and enforcing durable automated quality gates.

## Mission

Threat-model all critical boundaries, scan dependencies and secrets, repair authentication/authorization/injection/file/integration/session/data risks, normalize safe logging and exception reporting, remove dead and debug code, run Pint, establish the highest sensible PHPStan/Larastan level, and add regression and architecture tests for every accepted finding.

This is not an audit-only, documentation-only, planning-only, or review-only task. Discovery and planning are mandatory intermediate phases. After updating the canonical plan, immediately perform the required implementation, tests, review fixes, documentation synchronization, commit, and safe push directly on `main`.

## Repository-wide technology baseline

Preserve and enforce this target unless a stricter, current repository requirement applies:

- PHP 8.5.
- Laravel 13, using the latest stable mutually compatible 13.x release available at execution time.
- Livewire 4, using the latest stable mutually compatible 4.x release available at execution time.
- Class-based Livewire components with separate PHP classes and Blade views. Do not use Volt.
- Tailwind CSS 4, using the latest stable mutually compatible 4.x release available at execution time.
- The latest stable mutually compatible Vite, `laravel-vite-plugin`, and `@tailwindcss/vite` releases.
- Flux or Flux Pro only when already installed, licensed, intentionally used, and compatible with the selected Livewire version.
- The repository's established PHP test framework, upgraded to the latest stable release compatible with PHP 8.5 and Laravel 13.
- The repository's established JavaScript package manager and lock file. Never introduce a second lock file.

Verify exact versions from official documentation and authoritative package metadata before changing constraints. Never install alpha, beta, release-candidate, development-branch, or unmaintained fork dependencies unless a current repository requirement explicitly requires one and the reason is documented.

## Non-negotiable operating protocol

1. **Work only on the existing `main` branch.**
   - Begin with `git status --short --branch`, `git branch --show-current`, `git remote -v`, and a concise inspection of staged, unstaged, and untracked changes.
   - If the current branch is not `main`, safely switch to the existing `main` branch without losing, overwriting, stashing away, or silently absorbing unrelated user work.
   - Never create a feature branch, temporary branch, worktree branch, release branch, or review branch for this task.

2. **Protect the working tree.**
   - Preserve all unrelated existing changes.
   - Do not use `git reset --hard`, `git clean -fd`, `git checkout -- .`, force push, history rewriting, or any command that can discard work.
   - Before editing a file that already has user changes, inspect and merge carefully rather than replacing the file wholesale.
   - Commit only task-owned changes. Do not claim unrelated user changes as your work.

3. **Load governing instructions before work.**
   - Read the applicable `AGENTS.md` and `AGENTS.override.md` instruction chain from repository root to the current directory.
   - Read the canonical plan, requirements, architecture, testing, security, deployment, localization, and coding-convention documents.
   - Read all relevant first-party Markdown files before making architectural assumptions.
   - Treat nested instructions as scoped overrides rather than optional suggestions.

4. **Use specialized subagents for discovery.**
   - Spawn every required analysis subagent listed in this prompt.
   - Give each subagent a bounded scope, explicit files or domains, expected checks, and a structured deliverable.
   - Run independent scopes in parallel when safe.
   - Do not allow multiple subagents to edit the same files concurrently.
   - The principal agent remains the sole integrator and decision owner.

5. **Do not ask the user questions.**
   - Resolve ambiguity through repository evidence: code, tests, database schema, configuration, logs available in the repository, local Git history, first-party documentation, and current runtime behavior.
   - Choose the safest non-destructive, testable, backward-compatible interpretation.
   - Record material assumptions in the plan or an ADR and continue.
   - If an external credential, service, permission, or production-only resource is unavailable, complete all unblocked work, create deterministic fakes or isolated tests where appropriate, and document the exact external blocker with evidence.

6. **Quality is more important than speed.**
   - Work in small, reviewable, dependency-aware passes.
   - Inspect the diff after each meaningful pass.
   - Run targeted checks immediately after each change set.
   - Fix root causes rather than suppressing warnings, weakening tests, increasing timeouts blindly, or documenting an avoidable defect as future work.

7. **The plan is a mandatory gate, not the final deliverable.**
   - Locate the canonical execution plan. Prefer the existing canonical file; if none exists, create `PLANS.md` at repository root.
   - Do not create competing plan files.
   - Update the plan after discovery and before modifying production code.
   - Immediately begin implementation after saving the updated plan. Do not stop after analysis, recommendations, a checklist, or documentation.
   - Keep the plan current throughout implementation and mark items complete only after evidence-based verification.

8. **Implement, test, review, fix, and re-verify.**
   - Make all safe in-scope code, database, configuration, test, seed, and documentation changes required by the plan.
   - After implementation, spawn the independent reviewer subagents specified in this prompt. Reviewers must not be the same agents that implemented the change.
   - Use Codex `/review` for uncommitted changes when available, in addition to specialist reviews.
   - The principal agent must triage every finding, fix every valid in-scope finding, and record any rejected finding with a concise evidence-based reason.
   - Re-run all affected checks after fixes.

9. **Never fabricate evidence.**
   - Do not claim a command ran, a test passed, a build succeeded, a review completed, a commit was created, or a push succeeded unless you observed the result.
   - Record exact commands, exit codes when useful, counts, failures, skips, warnings, and material output.
   - Distinguish pre-existing failures from regressions introduced by this task.

10. **Finish documentation and Git work.**
    - Synchronize the plan, compliance matrix, changelog, and all affected first-party documentation with the final implementation.
    - Review the complete diff and staged diff for secrets, generated artifacts, unrelated changes, accidental deletions, and stale comments.
    - Create one or more coherent commits directly on `main` after applicable checks pass.
    - Push `main` only when a remote is configured, credentials are available, repository rules permit it, and the push can be performed safely.
    - Report the actual commit hashes and push result.

## Subagent coordination contract

The principal agent must create a work ledger before delegation. The ledger must identify each subagent, its exclusive scope, expected output, dependencies, and completion status.

Every analysis subagent must return a concise structured report containing:

1. Scope inspected.
2. Files, classes, routes, tables, components, or workflows examined.
3. Confirmed findings, each with severity and evidence.
4. Suspected findings that require principal-agent validation.
5. Missing coverage or unavailable evidence.
6. Recommended implementation order.
7. Suggested tests and verification commands.
8. Risks of changing the affected area.

Subagents must not dump raw logs without interpretation. The principal agent must reconcile conflicting reports, verify high-impact claims directly, and convert accepted findings into plan items with owners and acceptance criteria.

## Required analysis subagents and detailed roles

Spawn at least the following specialized subagents. Give each one the exact mission, scope, checks, deliverable, and restrictions below.

### Authentication and Session Threat Analyst

- **Mission:** Attack login, reset, verification, invitation, magic-link, MFA, session, cookie, and account-state controls.
- **Scope:** Authentication code/config, sessions, tokens, rate limits, notifications, logs, and tests.
- **Must inspect:**
  - Fixation, replay, enumeration, weak expiry/revocation, insecure cookies, bypassed account states, and demo access.
  - Secrets in URLs/logs.
  - Missing concurrency controls.
- **Required checks:**
  - Construct realistic abuse paths.
  - Review direct HTTP/Livewire calls.
  - Validate negative tests and production config.
- **Required deliverable:**
  - A severity-ranked auth threat report.
  - Required fixes/tests.
  - Verified controls.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Authorization, IDOR, and Tenant Threat Analyst

- **Mission:** Find unauthorized read/write paths across routes, Livewire, APIs, files, caches, commands, and deferred work.
- **Scope:** Policies, gates, bindings, queries, tenant scopes, roles, files, exports/imports, caches, and tests.
- **Must inspect:**
  - Forged IDs, UI-only checks, late scoping, cross-tenant counts/existence, admin bypass, and mass assignment.
  - Background context loss.
  - Private file leakage.
- **Required checks:**
  - Use owner/non-owner and multiple tenants.
  - Trace queries before serialization.
  - Review direct actions/events.
- **Required deliverable:**
  - An access-control vulnerability list.
  - Reproduction scenarios.
  - Required policy/query/tests.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Injection, XSS, SSRF, and Redirect Threat Analyst

- **Mission:** Audit all interpreters and external-address boundaries for injection and browser/server request abuse.
- **Scope:** SQL/raw queries, commands/processes, templates/raw HTML, Markdown/rich text, URLs, HTTP fetches, redirects, file paths, and integrations.
- **Must inspect:**
  - SQL/command/template/header injection, stored/reflected XSS, unsafe SVG/HTML, path traversal, SSRF, DNS rebinding, metadata access, and open redirects.
  - Insufficient sanitization or URL normalization.
  - Error paths leaking payloads.
- **Required checks:**
  - Trace untrusted input to sink.
  - Use malicious fixtures.
  - Verify schemes/hosts/IP ranges/redirects/timeouts/size limits.
- **Required deliverable:**
  - A sink/source threat map.
  - Severity and exploitability.
  - Required remediations and tests.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### File, Webhook, Payment, and Integration Threat Analyst

- **Mission:** Attack uploads, private delivery, signatures, replay, idempotency, provider trust, and financial transitions.
- **Scope:** Files/media, webhook verification, payments, external clients, retries, event storage, logs, and tests.
- **Must inspect:**
  - Spoofed uploads, public private files, signature bypass, duplicate/out-of-order events, client-trusted amount/currency/status, unsafe retries, and secret leakage.
  - Partial transaction/file state.
  - Provider outage behavior.
- **Required checks:**
  - Model malicious and repeated requests.
  - Inspect atomic boundaries and locks.
  - Review redaction and failure classification.
- **Required deliverable:**
  - A boundary-specific threat report.
  - Required fixes/tests.
  - Financial/file/integration readiness verdict.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Secrets, Configuration, and Logging Analyst

- **Mission:** Find committed or runtime-exposed secrets and improve structured diagnostics without sensitive disclosure.
- **Scope:** Git-tracked files, env examples, config, logs, exception context, HTTP clients, deployment scripts, CI, seeders, tests, and documentation.
- **Must inspect:**
  - Passwords, tokens, private keys, auth headers, connection strings, magic links, payment secrets, production personal data, and debug output.
  - Logs dumping requests/responses.
  - Missing request/user/tenant/provider context.
- **Required checks:**
  - Scan current tree and relevant local history when safe.
  - Classify false positives.
  - Trace redaction at exception and retry paths.
  - Review production debug/log channels.
- **Required deliverable:**
  - A secret/logging findings report.
  - Required rotation note for any exposed secret without printing the secret.
  - Structured context and redaction changes/tests.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Dependency and Supply-Chain Security Analyst

- **Mission:** Assess Composer/npm dependencies, repositories, scripts, advisories, and abandoned packages.
- **Scope:** Composer/package metadata and locks, audits, custom repositories, install scripts, Git dependencies, and CI.
- **Must inspect:**
  - Known advisories, unmaintained forks, mutable branches, excessive plugin/script privileges, and unexpected packages.
  - Advisories not actually reachable versus exploitable use.
  - Lock-file integrity and version pinning.
- **Required checks:**
  - Run authoritative audits.
  - Map advisories to used functionality.
  - Recommend upgrade/remove/mitigate with evidence.
- **Required deliverable:**
  - A supply-chain risk register.
  - Required dependency actions.
  - Documented temporary exceptions with owner/expiry.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Static Analysis and Type-Safety Architect

- **Mission:** Raise static analysis to the highest sensible level and fix real defects without broad suppression.
- **Scope:** PHPStan/Larastan config, baselines, PHP code, generics/array shapes, framework extensions, and CI scripts.
- **Must inspect:**
  - Broad ignores, giant baselines, mixed return/parameter types, unreachable branches, invalid model properties, missing generics, and framework false positives.
  - Analysis level reduced to pass.
  - Differences between local and CI.
- **Required checks:**
  - Introduce increases incrementally.
  - Validate every suppression narrowly.
  - Prioritize security/data defects.
  - Keep generated/vendor code excluded appropriately.
- **Required deliverable:**
  - A static-analysis migration plan.
  - Findings by category/severity.
  - Justified narrow suppressions and final level.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Formatting, Dead Code, and Architecture Quality Analyst

- **Mission:** Enforce consistent formatting and remove proven dead/debug/obsolete code without breaking discovery or contracts.
- **Scope:** Pint/config, source tree, routes, views, providers, translations, packages, imports, comments, debug calls, and architecture tests.
- **Must inspect:**
  - Unused imports/classes/methods/routes/views/config/packages, commented implementations, dumps, TODO/FIXME, duplicate helpers, and inconsistent style.
  - Code appearing unused but discovered by Laravel/reflection/Blade/config/external contracts.
  - Formatting changes obscuring behavioral diff.
- **Required checks:**
  - Prove deadness through references and framework conventions.
  - Separate mechanical formatting commits/passes if useful.
  - Design durable architecture checks.
- **Required deliverable:**
  - A cleanup candidate list with evidence.
  - Formatting/architecture rule changes.
  - Regression risks and tests.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Security Regression Test Architect

- **Mission:** Create adversarial tests and quality gates for every confirmed vulnerability and security invariant.
- **Scope:** Threat findings, test framework, fixtures, fakes, policies, integration clients, file handling, logs, and architecture rules.
- **Must inspect:**
  - Findings without tests, tests that only assert status, missing side-effect/data leakage assertions, and unsafe real services.
  - Race/replay paths.
  - Production configuration branches.
- **Required checks:**
  - Turn every accepted finding into a failing-before/passing-after test where practical.
  - Use malicious fixtures and concurrent scenarios.
  - Automate secret/log checks carefully.
- **Required deliverable:**
  - A finding-to-test matrix.
  - Required regression/architecture tests.
  - Exact verification commands.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

## Mandatory plan update before implementation

Update the canonical plan with a dedicated section for this task. At minimum include:

- A threat model covering authentication, authorization, tenant isolation, sessions, tokens, injection, XSS, SSRF, redirects, files, webhooks, payments, integrations, logging, privacy, and admin operations.
- Every confirmed vulnerability with severity, exploit scenario, affected assets/requirements, code location, remediation, and regression test.
- Dependency advisories, abandoned packages, supply-chain concerns, and temporary exception policy.
- Secrets/config/log findings, required redaction, and rotation notification without exposing secret values.
- Static-analysis baseline, target level, defect categories, and narrow suppression policy.
- Formatting, dead/debug/obsolete code cleanup with proof of non-use.
- Architecture/security quality gates and CI command integration.
- The ordered remediation sequence, prioritizing exploitable and data-integrity issues.

Give every plan item a stable identifier, dependency, owner, affected files or modules, acceptance criteria, required tests, verification command, status, and rollback note when relevant.

Save the plan, then immediately begin implementation. Do not return a plan to the user and wait for approval.

## Domain-specific guardrails

- Do not print or repeat discovered secret values in plans, reports, logs, commits, or tests.
- Do not weaken authentication, authorization, CSRF, origin, validation, rate limiting, encryption, or logging controls merely to make tests pass.
- Do not add broad static-analysis ignores or a massive baseline that hides current findings.
- Do not delete apparently unused code until framework discovery, reflection, routes, Blade, config, serialization, and external contracts are checked.
- Do not run destructive or exploitative tests against systems or data outside the owned local/test repository environment.
- Do not label low-confidence scanner output as a confirmed vulnerability without validation.

## Required implementation scope

Complete all applicable work below. Treat the list as a minimum, not as permission to ignore defects discovered in the same scope.

- Build a repository-specific threat model with assets, actors, trust boundaries, entry points, abuse cases, and severity criteria.
- Run dependency audits, secret scans where available, repository pattern scans, static analysis, formatter checks, architecture tests, and existing security tests.
- Fix confirmed authentication/session/token, authorization/IDOR/tenant, mass-assignment, injection/XSS/SSRF/redirect, file, webhook/payment, integration/retry, exception, logging, and configuration vulnerabilities.
- For server-side URL fetching, enforce allowed schemes, normalized hosts, DNS/IP validation, blocking of localhost/loopback/link-local/private/metadata ranges unless explicitly required, safe redirects, timeouts, and response-size limits.
- Ensure uploads use actual content validation and safe names, private files require authorization, webhooks verify signatures before trust, payments use server-authoritative values, and retryable mutations are idempotent.
- Remove or rotate references to exposed secrets without publishing values; update config/env examples and deployment guidance.
- Implement structured logging with useful request/user/tenant/operation/model/provider IDs while redacting credentials, tokens, headers, payment data, private content, and sensitive personal data.
- Normalize exception reporting so expected domain failures are safe and unexpected failures retain diagnostic context without production disclosure.
- Run Laravel Pint or the established formatter and raise PHPStan/Larastan to the highest sensible passing level by fixing code rather than suppressing findings.
- Use Rector only in narrowly configured, reviewed passes with tests; do not allow unreviewed semantic transformations.
- Remove proven dead code, debug calls, commented legacy implementations, obsolete config/packages, and duplicate helpers while preserving discovered contracts.
- Add regression tests for every accepted security finding plus architecture tests for durable prohibitions and log/secret redaction tests.
- Re-run audits, analysis, tests, builds, and adversarial checks after remediation.

## Required verification

Run the repository's real commands rather than inventing command names. Perform all safe applicable checks below and capture exact results:

- Composer and frontend dependency audits with exact unresolved advisories and applicability.
- Secret scan of tracked first-party content and safe review of relevant local history when practical, without printing secrets.
- Static analysis at the documented final level and a review of every remaining suppression.
- Laravel Pint/formatter with no unintended behavioral changes hidden in formatting.
- Authentication, authorization, tenant, token replay, CSRF/origin, rate-limit, session, XSS, SSRF, redirect, path traversal, upload, webhook, payment, integration, and logging adversarial tests.
- Production debug/cookie/log/error configuration review.
- Architecture tests for no Volt, no @php, no env outside config, passive Blade, no debug calls, no real network in tests, and other repository rules.
- Full PHP suite, migration/seed checks, frontend audit/build, browser tests, and final diff/secret/generated-artifact review.
- A finding-to-test/compliance audit proving every accepted vulnerability is fixed and verified.

## Independent review protocol

After implementation and before committing:

1. Freeze broad feature work and capture the exact review scope with `git diff`, `git diff --staged`, and the relevant commit range when applicable.
2. Spawn every reviewer subagent listed below. Reviewer agents must be independent from implementation agents.
3. Run Codex `/review` against uncommitted changes when available.
4. Require reviewers to inspect behavior, not only style.
5. Require each finding to include severity, exact location, failure scenario, evidence, and a proposed correction or test.
6. Have the principal agent reproduce or validate each material finding.
7. Fix every valid in-scope finding.
8. Add a regression test for every behavior defect when practical.
9. Re-run targeted checks after each fix and the full applicable verification suite before completion.
10. Record reviewer findings and dispositions in the canonical code-review document or plan.

## Required independent reviewer subagents

### Adversarial Security Reviewer

- **Review mission:** Independently threat-model and attack the final diff and adjacent unchanged code for exploitable vulnerabilities.
- **Scope:** All changed security boundaries plus authentication, authorization, data, files, integrations, payments, and config.
- **Must inspect:**
  - Bypasses, incomplete mitigations, chained attacks, race/replay paths, unsafe defaults, and production-only gaps.
  - Scanner false confidence.
  - Tests that fail to prove absence of side effects/leakage.
- **Required checks:**
  - Construct realistic attacker stories.
  - Use direct endpoints/actions and malicious fixtures.
  - Prioritize P0/P1/P2 style severity.
- **Required deliverable:**
  - Actionable findings with evidence.
  - Exploit scenario and blast radius.
  - Required remediation/test.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### Static Analysis and Code Quality Reviewer

- **Review mission:** Verify analysis level, suppressions, types, formatting, dead-code removals, and architecture checks.
- **Scope:** PHPStan/Larastan config/output, Pint diff, removed code, architecture tests, and CI scripts.
- **Must inspect:**
  - Broad ignores, lowered levels, false types, deleted discovered code, formatting-only noise, and rules not executed in CI.
  - Remaining debug/dead code.
  - Analysis discrepancies between local and CI.
- **Required checks:**
  - Re-run quality commands.
  - Inspect suppressions individually.
  - Sample deleted symbols and framework discovery.
- **Required deliverable:**
  - Quality findings.
  - Required config/code/test fixes.
  - A final analysis integrity verdict.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### Logging and Secret Reviewer

- **Review mission:** Attempt to find sensitive values in source, config, errors, logs, tests, fixtures, docs, and provider failure paths.
- **Scope:** Tracked files, environment examples, logging/exception code, HTTP clients, auth tokens, payments, deployment, and tests.
- **Must inspect:**
  - Secrets, full tokens, auth headers, PII, provider payloads, stack traces, SQL/paths, and insufficient diagnostic context.
  - Redaction applied only to success paths.
  - Test fixtures resembling real credentials.
- **Required checks:**
  - Trigger representative failures.
  - Inspect structured context and redaction.
  - Review repository scans without echoing values.
- **Required deliverable:**
  - Disclosure findings.
  - Required redaction/rotation notes.
  - A safe logging verdict.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### Security Regression Reviewer

- **Review mission:** Verify every accepted security finding has a reliable regression test and no mitigation was bypassed by configuration.
- **Scope:** Finding ledger, code fixes, tests, environments, compliance matrix, and command logs.
- **Must inspect:**
  - Missing test, weak assertion, wrong environment, skipped adversarial test, and behavior protected only by UI.
  - Tests altered to avoid exploit path.
  - Unverified deployment setting.
- **Required checks:**
  - Map finding to test and command.
  - Run high-severity tests independently.
  - Inspect failure-before rationale.
- **Required deliverable:**
  - Coverage gaps.
  - Required tests/config fixes.
  - Release-blocking findings.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

## Required documentation synchronization

Update the canonical plan plus all affected first-party documentation. At minimum review and update, when present or applicable:

- The canonical plan, requirements, compliance matrix, code-review ledger, and changelog.
- `SECURITY.md`, security architecture, threat model, logging/redaction, secrets, testing, deployment, and incident/recovery documentation.
- Static-analysis and formatting standards with final commands/levels.
- Dependency advisory exception register without secret values.
- AGENTS.md code-review rules and security definition of done.

Remove stale claims and commands. Keep useful historical changelog information. Ensure documentation describes the implemented and verified state, not the intended future state.

## Definition of done

This prompt is complete only when all applicable conditions are true:

- Every confirmed in-scope security finding is fixed or blocked only by a precise external dependency with mitigation and evidence.
- No known secret value is committed or exposed in logs, responses, fixtures, or documentation.
- Authentication, authorization, tenant, session, token, injection, XSS, SSRF, file, webhook, payment, and integration controls have adversarial tests.
- Production error/log/debug behavior is safe and diagnostically useful.
- Dependency audits are resolved or have narrow documented temporary exceptions.
- Pint passes and static analysis passes at the highest sensible documented level without broad suppressions.
- Proven dead/debug/obsolete code is removed without breaking framework discovery or contracts.
- Independent adversarial, quality, logging/secret, and security-regression reviewers are resolved, and changes are documented, committed, and safely pushed on `main`.

## Required final report

Return one factual report with these sections:

1. Branch and initial working-tree state.
2. Applicable instructions and canonical plan file.
3. Analysis subagents created, their scopes, and the important findings accepted or rejected.
4. Plan changes made before implementation.
5. Implementation summary by module and file group.
6. Database, security, performance, accessibility, localization, factory, seeder, and test impact where applicable.
7. Exact verification commands and observed results.
8. Independent reviewers created, findings, fixes, and remaining rejected findings with reasons.
9. Documentation synchronized.
10. Final Git status, commit hashes, and factual push result.
11. Remaining limitations, containing only genuine external or environmental blockers with exact affected requirement or plan identifiers.

Do not describe planned work as completed. Do not hide unfinished in-scope work in a limitations section.

## Start now

Read the applicable instruction chain and first-party documentation, inspect the repository, create the required analysis subagents, update the canonical plan, and then immediately implement the complete in-scope plan. Do not stop after analysis or planning.
