# 12. Blade Purity, Flux, Alpine, and JavaScript Lifecycle Modernization

## Principal-agent role

You are the principal Codex implementation agent responsible for turning Blade into a passive, secure presentation layer and making Flux, Alpine, and custom JavaScript integrations consistent, accessible, lifecycle-safe, and compatible with Livewire 4.

## Mission

Remove PHP blocks, direct database/model/service calls, hidden business logic, unsafe raw HTML, and oversized inline scripts from first-party Blade. Consolidate reusable presentation components, validate Flux usage against installed capabilities, eliminate duplicate Alpine setup, and make every third-party JavaScript integration safe across Livewire navigation.

This is not an audit-only, documentation-only, planning-only, or review-only task. Discovery and planning are mandatory intermediate phases. After updating the canonical plan, immediately perform the required implementation, tests, review fixes, documentation synchronization, commit, and safe push directly on `main`.

## Repository-wide technology baseline

Preserve and enforce this target unless a stricter, current repository requirement applies:

- PHP 8.5.
- Laravel 13, using the latest stable mutually compatible 13.x release available at execution time.
- Livewire 4, using the latest stable mutually compatible 4.x release available at execution time.
- Class-based Livewire components with separate PHP classes and Blade views. Do not use Volt.
- Tailwind CSS 4, using the latest stable mutually compatible 4.x release available at execution time.
- The latest stable mutually compatible Vite, `laravel-vite-plugin`, and `@tailwindcss/vite` releases.
- Flux or Flux Pro only when already installed, licensed, intentionally used, and compatible with the selected Livewire version.
- The repository's established PHP test framework, upgraded to the latest stable release compatible with PHP 8.5 and Laravel 13.
- The repository's established JavaScript package manager and lock file. Never introduce a second lock file.

Verify exact versions from official documentation and authoritative package metadata before changing constraints. Never install alpha, beta, release-candidate, development-branch, or unmaintained fork dependencies unless a current repository requirement explicitly requires one and the reason is documented.

## Non-negotiable operating protocol

1. **Work only on the existing `main` branch.**
   - Begin with `git status --short --branch`, `git branch --show-current`, `git remote -v`, and a concise inspection of staged, unstaged, and untracked changes.
   - If the current branch is not `main`, safely switch to the existing `main` branch without losing, overwriting, stashing away, or silently absorbing unrelated user work.
   - Never create a feature branch, temporary branch, worktree branch, release branch, or review branch for this task.

2. **Protect the working tree.**
   - Preserve all unrelated existing changes.
   - Do not use `git reset --hard`, `git clean -fd`, `git checkout -- .`, force push, history rewriting, or any command that can discard work.
   - Before editing a file that already has user changes, inspect and merge carefully rather than replacing the file wholesale.
   - Commit only task-owned changes. Do not claim unrelated user changes as your work.

3. **Load governing instructions before work.**
   - Read the applicable `AGENTS.md` and `AGENTS.override.md` instruction chain from repository root to the current directory.
   - Read the canonical plan, requirements, architecture, testing, security, deployment, localization, and coding-convention documents.
   - Read all relevant first-party Markdown files before making architectural assumptions.
   - Treat nested instructions as scoped overrides rather than optional suggestions.

4. **Use specialized subagents for discovery.**
   - Spawn every required analysis subagent listed in this prompt.
   - Give each subagent a bounded scope, explicit files or domains, expected checks, and a structured deliverable.
   - Run independent scopes in parallel when safe.
   - Do not allow multiple subagents to edit the same files concurrently.
   - The principal agent remains the sole integrator and decision owner.

5. **Do not ask the user questions.**
   - Resolve ambiguity through repository evidence: code, tests, database schema, configuration, logs available in the repository, local Git history, first-party documentation, and current runtime behavior.
   - Choose the safest non-destructive, testable, backward-compatible interpretation.
   - Record material assumptions in the plan or an ADR and continue.
   - If an external credential, service, permission, or production-only resource is unavailable, complete all unblocked work, create deterministic fakes or isolated tests where appropriate, and document the exact external blocker with evidence.

6. **Quality is more important than speed.**
   - Work in small, reviewable, dependency-aware passes.
   - Inspect the diff after each meaningful pass.
   - Run targeted checks immediately after each change set.
   - Fix root causes rather than suppressing warnings, weakening tests, increasing timeouts blindly, or documenting an avoidable defect as future work.

7. **The plan is a mandatory gate, not the final deliverable.**
   - Locate the canonical execution plan. Prefer the existing canonical file; if none exists, create `PLANS.md` at repository root.
   - Do not create competing plan files.
   - Update the plan after discovery and before modifying production code.
   - Immediately begin implementation after saving the updated plan. Do not stop after analysis, recommendations, a checklist, or documentation.
   - Keep the plan current throughout implementation and mark items complete only after evidence-based verification.

8. **Implement, test, review, fix, and re-verify.**
   - Make all safe in-scope code, database, configuration, test, seed, and documentation changes required by the plan.
   - After implementation, spawn the independent reviewer subagents specified in this prompt. Reviewers must not be the same agents that implemented the change.
   - Use Codex `/review` for uncommitted changes when available, in addition to specialist reviews.
   - The principal agent must triage every finding, fix every valid in-scope finding, and record any rejected finding with a concise evidence-based reason.
   - Re-run all affected checks after fixes.

9. **Never fabricate evidence.**
   - Do not claim a command ran, a test passed, a build succeeded, a review completed, a commit was created, or a push succeeded unless you observed the result.
   - Record exact commands, exit codes when useful, counts, failures, skips, warnings, and material output.
   - Distinguish pre-existing failures from regressions introduced by this task.

10. **Finish documentation and Git work.**
    - Synchronize the plan, compliance matrix, changelog, and all affected first-party documentation with the final implementation.
    - Review the complete diff and staged diff for secrets, generated artifacts, unrelated changes, accidental deletions, and stale comments.
    - Create one or more coherent commits directly on `main` after applicable checks pass.
    - Push `main` only when a remote is configured, credentials are available, repository rules permit it, and the push can be performed safely.
    - Report the actual commit hashes and push result.

## Subagent coordination contract

The principal agent must create a work ledger before delegation. The ledger must identify each subagent, its exclusive scope, expected output, dependencies, and completion status.

Every analysis subagent must return a concise structured report containing:

1. Scope inspected.
2. Files, classes, routes, tables, components, or workflows examined.
3. Confirmed findings, each with severity and evidence.
4. Suspected findings that require principal-agent validation.
5. Missing coverage or unavailable evidence.
6. Recommended implementation order.
7. Suggested tests and verification commands.
8. Risks of changing the affected area.

Subagents must not dump raw logs without interpretation. The principal agent must reconcile conflicting reports, verify high-impact claims directly, and convert accepted findings into plan items with owners and acceptance criteria.

## Required analysis subagents and detailed roles

Spawn at least the following specialized subagents. Give each one the exact mission, scope, checks, deliverable, and restrictions below.

### Blade Purity and Data-Flow Analyst

- **Mission:** Find all non-presentation behavior in Blade and map it to the correct owner.
- **Scope:** All first-party Blade views/components, layouts, mail views, error views, pagination, and rendered fragments.
- **Must inspect:**
  - `@php`/`@endphp`, raw PHP, model/DB/service/facade/container calls, complex collections, business calculations, permission decisions, SEO/JSON-LD generation, and hidden relation loading.
  - Hardcoded user-facing text and raw HTML.
  - Views receiving unclear or excessive data.
- **Required checks:**
  - Trace each offending expression to callers.
  - Classify simple presentation logic versus business logic.
  - Detect query-triggering property access.
- **Required deliverable:**
  - A Blade violation inventory.
  - Correct destination for each moved behavior.
  - Architecture-test candidates.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Blade Component and Presentation Contract Analyst

- **Mission:** Consolidate repeated markup into accessible Blade or Flux components with explicit data contracts.
- **Scope:** Repeated cards, forms, tables, buttons, status badges, modals, empty states, errors, navigation, and layouts.
- **Must inspect:**
  - Duplicated class sets/markup, overly generic components, implicit slot contracts, and component proliferation.
  - Static components incorrectly using Livewire.
  - Missing labels, states, and translation keys.
- **Required checks:**
  - Identify meaningful reusable patterns.
  - Define props, slots, defaults, and validation expectations.
  - Preserve semantic HTML and responsive behavior.
- **Required deliverable:**
  - A component consolidation map.
  - Proposed explicit contracts.
  - Usage and test updates.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Flux Compatibility and Accessibility Analyst

- **Mission:** Verify every Flux/Flux Pro component and attribute against the installed compatible version and licensing.
- **Scope:** Flux packages, Blade usage, forms, modals, menus, tables, notifications, themes, and documentation.
- **Must inspect:**
  - Invented attributes/slots/events, deprecated API, Pro-only use without entitlement, accessibility regressions, and custom duplicates.
  - Validation integration, focus handling, keyboard behavior, localization, and dark mode.
  - Components that should remain custom.
- **Required checks:**
  - Cross-check exact installed capabilities.
  - Exercise keyboard and form-error behavior.
  - Review upgrade compatibility.
- **Required deliverable:**
  - A Flux usage matrix.
  - Invalid/deprecated usage list.
  - Replace/retain decisions and tests.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Alpine Integration Analyst

- **Mission:** Ensure a single Livewire-compatible Alpine runtime and clear ownership of ephemeral client state.
- **Scope:** JavaScript entry points, package metadata, Livewire bootstrap, Alpine plugins/components, Blade `x-` directives, and tests.
- **Must inspect:**
  - Duplicate Alpine installation, version conflicts, global state leakage, server decisions implemented only in Alpine, and unsanitized executable data.
  - Plugins initialized repeatedly after navigation.
  - State that should belong to Livewire or plain HTML.
- **Required checks:**
  - Map Alpine imports and startup.
  - Classify each Alpine component by responsibility.
  - Check CSP/sanitization considerations.
- **Required deliverable:**
  - An Alpine ownership map.
  - Duplicate/conflict findings.
  - Required migration and lifecycle tests.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### JavaScript Navigation Lifecycle Analyst

- **Mission:** Audit all custom and third-party JavaScript for idempotent initialization and teardown across Livewire navigation.
- **Scope:** Charts, editors, maps, media players, modals, tooltips, observers, timers, event listeners, upload widgets, and global scripts.
- **Must inspect:**
  - Duplicated handlers, memory leaks, stale DOM references, reinitialization failures, and full-page-load assumptions.
  - Authorization-sensitive state persisted incorrectly.
  - Scripts excluded from Vite or loaded multiple times.
- **Required checks:**
  - Run repeated navigation paths.
  - Identify init/destroy APIs.
  - Track listeners/timers/observers.
  - Check account switch/logout and back/forward navigation.
- **Required deliverable:**
  - A lifecycle registry.
  - Leak and stale-state findings.
  - Integration wrapper and browser-test requirements.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Raw HTML and XSS Boundary Analyst

- **Mission:** Locate every raw HTML rendering path and verify sanitization, ownership, and content policy.
- **Scope:** Blade raw echo, Markdown/rich text rendering, emails, previews, admin content, JSON-LD, SVG, and third-party widgets.
- **Must inspect:**
  - Untrusted raw output, incomplete sanitization, stored XSS, attribute injection, unsafe URLs, and sanitizer bypass.
  - Double encoding or broken trusted markup.
  - Content Security Policy implications when present.
- **Required checks:**
  - Trace content origin and transformations.
  - Test malicious payloads.
  - Define explicit trusted/sanitized types or helpers where useful.
- **Required deliverable:**
  - An XSS boundary map.
  - Required sanitization fixes.
  - Adversarial tests.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Frontend Architecture Test Analyst

- **Mission:** Design automated checks for passive Blade, no Volt, single Alpine, safe raw output, and forbidden dependencies.
- **Scope:** Test suite, Blade tree, package metadata, static analysis capabilities, and repository rules.
- **Must inspect:**
  - Reliable detection methods and false-positive exclusions.
  - Direct model/DB/service calls and PHP blocks.
  - Duplicate frontend runtimes.
- **Required checks:**
  - Prefer AST/reflection-aware checks where practical.
  - Keep rules maintainable.
  - Include failing fixture examples.
- **Required deliverable:**
  - An architecture-check specification.
  - Implementation approach.
  - Documented intentional exceptions.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

## Mandatory plan update before implementation

Update the canonical plan with a dedicated section for this task. At minimum include:

- Every Blade purity violation and the correct owning class or component.
- Repeated presentation patterns and explicit Blade/Flux component contracts.
- Flux version/licensing/API compatibility and accessibility corrections.
- Alpine runtime ownership, plugin use, and state-boundary corrections.
- Every custom/third-party JavaScript integration, initialization/teardown lifecycle, and navigation risk.
- Every raw HTML/XSS boundary and required sanitization control.
- Architecture, Livewire, feature, and browser tests for all changed presentation behavior.
- An implementation order that avoids simultaneous edits to shared layouts and components.

Give every plan item a stable identifier, dependency, owner, affected files or modules, acceptance criteria, required tests, verification command, status, and rollback note when relevant.

Save the plan, then immediately begin implementation. Do not return a plan to the user and wait for approval.

## Domain-specific guardrails

- Do not move Blade business logic into a random global helper or facade.
- Do not create Livewire components solely for static presentation reuse.
- Do not install Flux if the project does not already intentionally use it without a documented benefit and licensing decision.
- Do not install a second Alpine runtime; Livewire-compatible ownership must remain singular.
- Do not render user-controlled HTML raw without an explicit, tested sanitization boundary.
- Do not invent Flux APIs or assume Pro availability.

## Required implementation scope

Complete all applicable work below. Treat the list as a minimum, not as permission to ignore defects discovered in the same scope.

- Remove every first-party `@php`, `@endphp`, raw PHP block, direct model/DB/service/action/facade/container call, complex collection pipeline, business calculation, permission decision, SEO/JSON-LD generation, and hidden relation load from Blade.
- Move business logic to actions/domain services, presentation preparation to controllers/Livewire computed properties/typed view models, and reusable formatting to explicit dedicated formatters.
- Keep Blade limited to simple presentation conditions, simple loops over prepared data, escaped output, translations, components, Flux components, and Livewire directives.
- Consolidate meaningful repeated markup into accessible Blade or Flux components with explicit props, slots, defaults, and states.
- Upgrade and correct Flux usage only against the exact installed compatible version and license; preserve keyboard, focus, validation, localization, and dark-mode behavior.
- Remove duplicate Alpine packages/startup, retain only required plugins, and keep Alpine limited to ephemeral local DOM state while server decisions remain in Livewire/Laravel.
- Create idempotent initialize/destroy wrappers for every third-party widget and make them safe for repeated `wire:navigate`, browser back/forward, logout, and account switch.
- Audit and secure every raw HTML, SVG, Markdown, rich-text, URL, JSON-LD, and script-data boundary.
- Extract oversized inline JavaScript into versioned modules and ensure Vite owns loading exactly once.
- Add architecture tests for no PHP blocks, no Volt, no direct Blade dependencies, one Alpine runtime, and controlled raw output.
- Add Livewire/feature/browser tests for changed forms, components, modals, navigation, focus, widgets, and XSS cases.

## Required verification

Run the repository's real commands rather than inventing command names. Perform all safe applicable checks below and capture exact results:

- Repository-wide scans and architecture tests for `@php`, `@endphp`, raw PHP, Volt, direct Blade DB/model/service/facade/container access, and duplicate Alpine.
- Static analysis/formatter for moved PHP logic.
- Flux component rendering, validation, keyboard, focus, localization, and dark-mode tests where applicable.
- Stored/reflected XSS tests for every raw-content boundary.
- Repeated `wire:navigate`, back/forward, logout, and account-switch browser cycles with console and listener/timer inspection.
- Production frontend build and asset duplication inspection.
- Full PHP suite, Livewire tests, architecture tests, and critical browser tests.
- Final diff review of shared layouts and reusable component APIs.

## Independent review protocol

After implementation and before committing:

1. Freeze broad feature work and capture the exact review scope with `git diff`, `git diff --staged`, and the relevant commit range when applicable.
2. Spawn every reviewer subagent listed below. Reviewer agents must be independent from implementation agents.
3. Run Codex `/review` against uncommitted changes when available.
4. Require reviewers to inspect behavior, not only style.
5. Require each finding to include severity, exact location, failure scenario, evidence, and a proposed correction or test.
6. Have the principal agent reproduce or validate each material finding.
7. Fix every valid in-scope finding.
8. Add a regression test for every behavior defect when practical.
9. Re-run targeted checks after each fix and the full applicable verification suite before completion.
10. Record reviewer findings and dispositions in the canonical code-review document or plan.

## Required independent reviewer subagents

### Blade Architecture Reviewer

- **Review mission:** Verify that Blade is passive, data flow is explicit, and moved logic belongs in appropriate layers.
- **Scope:** All changed views, view models, controllers, Livewire components, actions, formatters, and architecture tests.
- **Must inspect:**
  - Residual business logic, hidden queries, helper dumping ground, overgeneric components, and hardcoded text.
  - Moved logic creating new coupling or test gaps.
  - Raw output without ownership.
- **Required checks:**
  - Trace representative view data.
  - Search for forbidden patterns.
  - Inspect component contracts.
- **Required deliverable:**
  - Blade findings.
  - Required ownership corrections.
  - Architecture-test gaps.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### Flux and Accessibility Reviewer

- **Review mission:** Inspect Flux/custom component behavior for version correctness, licensing, semantics, focus, keyboard, and form integration.
- **Scope:** Changed components, Flux usage, forms, modals, menus, dark mode, translations, and tests.
- **Must inspect:**
  - Unsupported API, inaccessible custom replacements, focus loss, missing labels/errors, and Pro-only use without authorization.
  - Visual behavior that differs across themes or locales.
  - Component abstraction regressions.
- **Required checks:**
  - Exercise keyboard workflows.
  - Review installed package capabilities.
  - Inspect translated and error states.
- **Required deliverable:**
  - Compatibility/accessibility findings.
  - Required component corrections.
  - Verified usage list.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### JavaScript Lifecycle and XSS Reviewer

- **Review mission:** Independently attack navigation lifecycle, duplicate initialization, memory leaks, and executable-data boundaries.
- **Scope:** JavaScript modules, Alpine, persisted/ignored DOM, raw HTML, URLs, widget wrappers, and browser tests.
- **Must inspect:**
  - Duplicated listeners/timers, stale authorization state, unsafe interpolation, sanitizer gaps, and multiple script loads.
  - Cleanup only working on one navigation path.
  - CSP regressions where applicable.
- **Required checks:**
  - Repeat navigation and widget creation/destruction.
  - Use malicious payloads.
  - Inspect console and DOM after logout/account change.
- **Required deliverable:**
  - Lifecycle/XSS findings.
  - Reproduction steps.
  - Required fixes and tests.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

## Required documentation synchronization

Update the canonical plan plus all affected first-party documentation. At minimum review and update, when present or applicable:

- The canonical plan, compliance matrix, and changelog.
- AGENTS.md Blade, Flux, Alpine, JavaScript, and raw-HTML rules.
- Frontend, Livewire, accessibility, design-system, security, testing, and localization documentation.
- A component contract inventory and JavaScript lifecycle registry.
- Documented intentional raw-HTML exceptions and sanitizer ownership.

Remove stale claims and commands. Keep useful historical changelog information. Ensure documentation describes the implemented and verified state, not the intended future state.

## Definition of done

This prompt is complete only when all applicable conditions are true:

- No first-party Blade contains `@php`, direct data-layer/service calls, hidden business logic, unsafe raw output, or unjustified large inline scripts.
- Reusable presentation components have explicit, accessible contracts without unnecessary Livewire conversion.
- Flux usage is version-correct, licensed where required, and accessibility-preserving.
- There is one Livewire-compatible Alpine runtime and clear client/server state ownership.
- Every third-party widget initializes and tears down safely across repeated navigation and account changes.
- Architecture, XSS, Livewire, frontend-build, and browser tests pass.
- Independent Blade, Flux/accessibility, and lifecycle/XSS reviewers are resolved, and changes are documented, committed, and safely pushed on `main`.

## Required final report

Return one factual report with these sections:

1. Branch and initial working-tree state.
2. Applicable instructions and canonical plan file.
3. Analysis subagents created, their scopes, and the important findings accepted or rejected.
4. Plan changes made before implementation.
5. Implementation summary by module and file group.
6. Database, security, performance, accessibility, localization, factory, seeder, and test impact where applicable.
7. Exact verification commands and observed results.
8. Independent reviewers created, findings, fixes, and remaining rejected findings with reasons.
9. Documentation synchronized.
10. Final Git status, commit hashes, and factual push result.
11. Remaining limitations, containing only genuine external or environmental blockers with exact affected requirement or plan identifiers.

Do not describe planned work as completed. Do not hide unfinished in-scope work in a limitations section.

## Start now

Read the applicable instruction chain and first-party documentation, inspect the repository, create the required analysis subagents, update the canonical plan, and then immediately implement the complete in-scope plan. Do not stop after analysis or planning.
