# 04. Repository-Wide PHP 8.5 Source Modernization

## Principal-agent role

You are the principal Codex implementation agent responsible for modernizing all first-party PHP source for PHP 8.5 correctness, explicit typing, deprecation-free execution, and maintainable language usage without gratuitous syntax churn.

## Mission

Inspect and modernize every first-party PHP file so the application, tests, migrations, seeders, commands, and integration clients run cleanly on PHP 8.5. Fix real compatibility defects, strengthen types where safe, remove obsolete compatibility code, and apply modern language features only when they improve correctness or clarity.

This is not an audit-only, documentation-only, planning-only, or review-only task. Discovery and planning are mandatory intermediate phases. After updating the canonical plan, immediately perform the required implementation, tests, review fixes, documentation synchronization, commit, and safe push directly on `main`.

## Repository-wide technology baseline

Preserve and enforce this target unless a stricter, current repository requirement applies:

- PHP 8.5.
- Laravel 13, using the latest stable mutually compatible 13.x release available at execution time.
- Livewire 4, using the latest stable mutually compatible 4.x release available at execution time.
- Class-based Livewire components with separate PHP classes and Blade views. Do not use Volt.
- Tailwind CSS 4, using the latest stable mutually compatible 4.x release available at execution time.
- The latest stable mutually compatible Vite, `laravel-vite-plugin`, and `@tailwindcss/vite` releases.
- Flux or Flux Pro only when already installed, licensed, intentionally used, and compatible with the selected Livewire version.
- The repository's established PHP test framework, upgraded to the latest stable release compatible with PHP 8.5 and Laravel 13.
- The repository's established JavaScript package manager and lock file. Never introduce a second lock file.

Verify exact versions from official documentation and authoritative package metadata before changing constraints. Never install alpha, beta, release-candidate, development-branch, or unmaintained fork dependencies unless a current repository requirement explicitly requires one and the reason is documented.

## Non-negotiable operating protocol

1. **Work only on the existing `main` branch.**
   - Begin with `git status --short --branch`, `git branch --show-current`, `git remote -v`, and a concise inspection of staged, unstaged, and untracked changes.
   - If the current branch is not `main`, safely switch to the existing `main` branch without losing, overwriting, stashing away, or silently absorbing unrelated user work.
   - Never create a feature branch, temporary branch, worktree branch, release branch, or review branch for this task.

2. **Protect the working tree.**
   - Preserve all unrelated existing changes.
   - Do not use `git reset --hard`, `git clean -fd`, `git checkout -- .`, force push, history rewriting, or any command that can discard work.
   - Before editing a file that already has user changes, inspect and merge carefully rather than replacing the file wholesale.
   - Commit only task-owned changes. Do not claim unrelated user changes as your work.

3. **Load governing instructions before work.**
   - Read the applicable `AGENTS.md` and `AGENTS.override.md` instruction chain from repository root to the current directory.
   - Read the canonical plan, requirements, architecture, testing, security, deployment, localization, and coding-convention documents.
   - Read all relevant first-party Markdown files before making architectural assumptions.
   - Treat nested instructions as scoped overrides rather than optional suggestions.

4. **Use specialized subagents for discovery.**
   - Spawn every required analysis subagent listed in this prompt.
   - Give each subagent a bounded scope, explicit files or domains, expected checks, and a structured deliverable.
   - Run independent scopes in parallel when safe.
   - Do not allow multiple subagents to edit the same files concurrently.
   - The principal agent remains the sole integrator and decision owner.

5. **Do not ask the user questions.**
   - Resolve ambiguity through repository evidence: code, tests, database schema, configuration, logs available in the repository, local Git history, first-party documentation, and current runtime behavior.
   - Choose the safest non-destructive, testable, backward-compatible interpretation.
   - Record material assumptions in the plan or an ADR and continue.
   - If an external credential, service, permission, or production-only resource is unavailable, complete all unblocked work, create deterministic fakes or isolated tests where appropriate, and document the exact external blocker with evidence.

6. **Quality is more important than speed.**
   - Work in small, reviewable, dependency-aware passes.
   - Inspect the diff after each meaningful pass.
   - Run targeted checks immediately after each change set.
   - Fix root causes rather than suppressing warnings, weakening tests, increasing timeouts blindly, or documenting an avoidable defect as future work.

7. **The plan is a mandatory gate, not the final deliverable.**
   - Locate the canonical execution plan. Prefer the existing canonical file; if none exists, create `PLANS.md` at repository root.
   - Do not create competing plan files.
   - Update the plan after discovery and before modifying production code.
   - Immediately begin implementation after saving the updated plan. Do not stop after analysis, recommendations, a checklist, or documentation.
   - Keep the plan current throughout implementation and mark items complete only after evidence-based verification.

8. **Implement, test, review, fix, and re-verify.**
   - Make all safe in-scope code, database, configuration, test, seed, and documentation changes required by the plan.
   - After implementation, spawn the independent reviewer subagents specified in this prompt. Reviewers must not be the same agents that implemented the change.
   - Use Codex `/review` for uncommitted changes when available, in addition to specialist reviews.
   - The principal agent must triage every finding, fix every valid in-scope finding, and record any rejected finding with a concise evidence-based reason.
   - Re-run all affected checks after fixes.

9. **Never fabricate evidence.**
   - Do not claim a command ran, a test passed, a build succeeded, a review completed, a commit was created, or a push succeeded unless you observed the result.
   - Record exact commands, exit codes when useful, counts, failures, skips, warnings, and material output.
   - Distinguish pre-existing failures from regressions introduced by this task.

10. **Finish documentation and Git work.**
    - Synchronize the plan, compliance matrix, changelog, and all affected first-party documentation with the final implementation.
    - Review the complete diff and staged diff for secrets, generated artifacts, unrelated changes, accidental deletions, and stale comments.
    - Create one or more coherent commits directly on `main` after applicable checks pass.
    - Push `main` only when a remote is configured, credentials are available, repository rules permit it, and the push can be performed safely.
    - Report the actual commit hashes and push result.

## Subagent coordination contract

The principal agent must create a work ledger before delegation. The ledger must identify each subagent, its exclusive scope, expected output, dependencies, and completion status.

Every analysis subagent must return a concise structured report containing:

1. Scope inspected.
2. Files, classes, routes, tables, components, or workflows examined.
3. Confirmed findings, each with severity and evidence.
4. Suspected findings that require principal-agent validation.
5. Missing coverage or unavailable evidence.
6. Recommended implementation order.
7. Suggested tests and verification commands.
8. Risks of changing the affected area.

Subagents must not dump raw logs without interpretation. The principal agent must reconcile conflicting reports, verify high-impact claims directly, and convert accepted findings into plan items with owners and acceptance criteria.

## Required analysis subagents and detailed roles

Spawn at least the following specialized subagents. Give each one the exact mission, scope, checks, deliverable, and restrictions below.

### PHP Deprecation and Warning Scanner

- **Mission:** Find PHP 8.5 deprecations, warnings, removed behavior, and suppressed runtime defects.
- **Scope:** All first-party PHP, test bootstrap, migrations, seeders, scripts, and custom packages.
- **Must inspect:**
  - Dynamic properties, implicit conversions, null offsets, invalid argument handling, outdated resource close calls, old serialization, deprecated casts, string increment behavior, and error suppression.
  - Warnings hidden by test configuration or production error settings.
  - Version-specific branches that are now dead.
- **Required checks:**
  - Run full error reporting in isolation.
  - Map each runtime warning to exact code and reproduction.
  - Distinguish framework/vendor output from first-party defects.
- **Required deliverable:**
  - A deprecation catalog by severity.
  - Exact reproduction commands.
  - Recommended fixes and regression tests.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Type Safety Analyst

- **Mission:** Improve native and documented type contracts without introducing false precision or breaking framework integration.
- **Scope:** Methods, properties, closures, collections, array shapes, constructors, interfaces, overrides, model methods, and service boundaries.
- **Must inspect:**
  - Missing or incorrect parameter, return, and property types.
  - Nullable values that are not legitimate domain states.
  - Untyped associative arrays crossing multiple layers.
  - Incorrect generic collection annotations.
- **Required checks:**
  - Infer types from callers, tests, schema, and framework contracts.
  - Identify covariance/contravariance and inheritance risks.
  - Avoid mechanical strictness that changes coercion-dependent public contracts without migration.
- **Required deliverable:**
  - A prioritized type-hardening map.
  - Files safe for `declare(strict_types=1)`.
  - Contracts requiring DTOs, value objects, or documented array shapes.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Serialization, Reflection, and Magic-Method Analyst

- **Mission:** Audit language-sensitive behavior that often fails during PHP upgrades.
- **Scope:** Serializable objects, queued payloads, cache serialization, sessions, magic methods, reflection, attributes, proxies, mocks, and closures.
- **Must inspect:**
  - Legacy `__sleep`/`__wakeup`, invalid `__serialize`/`__unserialize`, `__debugInfo`, dynamic invocation, reflection assumptions, and closure serialization.
  - Objects persisted across deployments or serialized in cache/session.
  - Mock signatures incompatible with real methods.
- **Required checks:**
  - Trace serialized classes and compatibility expectations.
  - Check return types and method visibility for magic methods.
  - Identify data that requires cache/session invalidation during deployment.
- **Required deliverable:**
  - A serialization risk report.
  - Required migration or invalidation notes.
  - Tests for round-trip and backward compatibility where needed.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Modern PHP Design Analyst

- **Mission:** Find high-value opportunities for enums, readonly data, value objects, match expressions, attributes, and other modern constructs.
- **Scope:** State constants, immutable data, domain identifiers, money, URIs, mapping code, override methods, and critical return values.
- **Must inspect:**
  - Stringly typed state, mutable transport objects, repetitive switch logic, unsafe URL parsing, and ignored critical results.
  - Potential use of PHP 8.5-specific features only where supported and beneficial.
  - Places where modern syntax would obscure rather than improve code.
- **Required checks:**
  - Evaluate each feature by correctness, readability, runtime support, and migration cost.
  - Require tests before changing public serialization or database representation.
  - Avoid framework-hostile final/readonly choices.
- **Required deliverable:**
  - A PHP 8.5 applicability matrix.
  - Approved modernization candidates.
  - Rejected candidates with reasons.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Framework Contract Compatibility Analyst

- **Mission:** Ensure PHP typing and modernization remain compatible with Laravel, Livewire, PHPUnit/Pest, Eloquent, and package contracts.
- **Scope:** Framework overrides, contracts, service providers, model methods, Livewire lifecycle hooks, commands, policies, notifications, jobs, and tests.
- **Must inspect:**
  - Method signatures that must match framework parents or interfaces.
  - Attribute or final-class choices that break proxying, mocking, container resolution, or model behavior.
  - Strict-types changes at external boundaries.
- **Required checks:**
  - Compare parent and interface signatures.
  - Inspect package extension points.
  - Add `#[Override]` only where supported and correct.
- **Required deliverable:**
  - A contract-compatibility checklist.
  - Required signature fixes.
  - Tests for framework callbacks and extension points.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### PHP Test Compatibility Analyst

- **Mission:** Modernize tests and test helpers for PHP 8.5 without weakening assertions.
- **Scope:** PHPUnit/Pest tests, data providers, mocks, attributes, annotations, process isolation, time helpers, and coverage config.
- **Must inspect:**
  - Deprecated annotations, incompatible data providers, mock method signatures, warning expectations, and fragile type coercion.
  - Tests that pass only because warnings are hidden.
  - Assertions that need stricter semantics.
- **Required checks:**
  - Run targeted tests with full errors.
  - Compare test counts and skips.
  - Verify failures indicate defects rather than toolchain mismatch.
- **Required deliverable:**
  - A test-migration list.
  - Updated helper recommendations.
  - Missing regression tests for language changes.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

## Mandatory plan update before implementation

Update the canonical plan with a dedicated section for this task. At minimum include:

- Every confirmed PHP 8.5 deprecation, warning, removed behavior, invalid signature, and suppressed error.
- Files and boundaries selected for strict types and stronger native typing.
- Serialization, session, cache, queue, and deployment compatibility risks.
- Approved and rejected PHP 8.5 language-feature applications with reasons.
- The ordered source-migration sequence with targeted tests after each group.
- Static-analysis changes, baseline policy, and prohibited broad suppressions.
- Required cache/session invalidation or deployment notes.
- Regression tests for each behavior-affecting change.

Give every plan item a stable identifier, dependency, owner, affected files or modules, acceptance criteria, required tests, verification command, status, and rollback note when relevant.

Save the plan, then immediately begin implementation. Do not return a plan to the user and wait for approval.

## Domain-specific guardrails

- Do not mass-add `declare(strict_types=1)` without examining public input boundaries and existing coercion assumptions.
- Do not create DTOs, value objects, interfaces, readonly classes, or final classes merely to increase the count of modern constructs.
- Do not change persisted enum values, serialized formats, money representation, or public API shapes without a migration and compatibility test.
- Do not hide PHP warnings through error-reporting changes or suppression operators.

## Required implementation scope

Complete all applicable work below. Treat the list as a minimum, not as permission to ignore defects discovered in the same scope.

- Run the application and tests with full error reporting in an isolated environment and catalog every first-party warning or deprecation.
- Fix dynamic properties, invalid signatures, nullable assumptions, null offsets, implicit conversion defects, outdated casts, legacy magic serialization, obsolete resource-closing calls, string increment assumptions, outdated PDO constants, callback signature mismatches, and hidden errors.
- Add `declare(strict_types=1)` to new and materially modified first-party files where safe, and repair affected callers rather than weakening types.
- Improve parameter, return, property, closure, enum, iterable, array-shape, and generic collection typing based on repository evidence.
- Use enums for closed state sets, readonly data for truly immutable transport/value objects, final classes where extension is not a contract, match for exhaustive mappings, and attributes only where they improve framework clarity.
- Evaluate PHP 8.5 URI handling, clone-with, pipe usage, `#[NoDiscard]`, `#[Override]`, and new array helpers individually; implement only approved cases with tests.
- Remove dead version shims, unused imports, debug statements, broad error suppression, commented legacy implementations, and unreachable branches after proving they are unused.
- Update mocks, data providers, test attributes, and helpers without weakening behavioral coverage.
- Add regression tests for every language-level defect and a deployment note for any serialized/cache compatibility impact.

## Required verification

Run the repository's real commands rather than inventing command names. Perform all safe applicable checks below and capture exact results:

- PHP syntax check across all first-party PHP files.
- Application boot and representative console/HTTP smoke paths with full error reporting.
- Formatter and static analysis at the selected justified level.
- Targeted tests for each modernization group, then the complete PHP suite.
- Fresh testing migration and seed run to exercise migrations and seeders under PHP 8.5.
- Serialization round-trip, cache/session, queue payload, or deployment compatibility tests where affected.
- A repository scan for dynamic properties, suppression operators, deprecated patterns, debug calls, and stale compatibility branches.
- Final comparison of warning/deprecation counts against the baseline.

## Independent review protocol

After implementation and before committing:

1. Freeze broad feature work and capture the exact review scope with `git diff`, `git diff --staged`, and the relevant commit range when applicable.
2. Spawn every reviewer subagent listed below. Reviewer agents must be independent from implementation agents.
3. Run Codex `/review` against uncommitted changes when available.
4. Require reviewers to inspect behavior, not only style.
5. Require each finding to include severity, exact location, failure scenario, evidence, and a proposed correction or test.
6. Have the principal agent reproduce or validate each material finding.
7. Fix every valid in-scope finding.
8. Add a regression test for every behavior defect when practical.
9. Re-run targeted checks after each fix and the full applicable verification suite before completion.
10. Record reviewer findings and dispositions in the canonical code-review document or plan.

## Required independent reviewer subagents

### PHP Type and Runtime Reviewer

- **Review mission:** Find incorrect type assumptions, hidden coercion changes, deprecations, and runtime paths missed by tests.
- **Scope:** All changed PHP, public boundaries, model casts, framework overrides, and command/test output.
- **Must inspect:**
  - False non-null assumptions, over-specific types, inheritance mismatches, and warnings only triggered by edge data.
  - Strict-types regressions at HTTP, CLI, database, and external integration boundaries.
  - Modern features used without a correctness benefit.
- **Required checks:**
  - Inspect callers and callees.
  - Exercise representative edge values.
  - Compare runtime output with static-analysis claims.
- **Required deliverable:**
  - Exact runtime/type findings.
  - Required type or boundary corrections.
  - A verdict on PHP 8.5 cleanliness.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### Backward-Compatibility Reviewer

- **Review mission:** Assess persisted data, serialized objects, public APIs, and package extension points for unintended breakage.
- **Scope:** Enums, value objects, serialization, sessions, caches, queues, APIs, models, migrations, and framework overrides.
- **Must inspect:**
  - Changed serialized forms, enum values, database casts, method contracts, and deployment invalidation needs.
  - Behavior changes hidden as refactoring.
  - Missing migration or rollout steps.
- **Required checks:**
  - Trace old-to-new data paths.
  - Review deployment order.
  - Check compatibility tests.
- **Required deliverable:**
  - Breaking-change findings.
  - Migration and rollback requirements.
  - Approved compatibility exceptions.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### PHP Test and Deprecation Reviewer

- **Review mission:** Verify that tests expose rather than suppress PHP 8.5 issues and that every repaired defect is covered.
- **Scope:** Test config, new tests, warning handling, static analysis, coverage, and skipped tests.
- **Must inspect:**
  - Disabled error reporting, broad ignores, removed assertions, flaky tests, and untested warning paths.
  - Mismatch between reported and actual warning counts.
  - Insufficient negative or boundary cases.
- **Required checks:**
  - Re-run targeted suites with strict error settings.
  - Inspect changes to test expectations.
  - Review static-analysis suppressions.
- **Required deliverable:**
  - Verification gaps.
  - Required tests or config fixes.
  - Final evidence summary.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

## Required documentation synchronization

Update the canonical plan plus all affected first-party documentation. At minimum review and update, when present or applicable:

- The canonical plan and compliance matrix.
- `CHANGELOG.md`, architecture, testing, deployment, and coding-convention documentation.
- A PHP 8.5 applicability matrix with feature, decision, location, reason, and test evidence.
- Deployment notes for cache/session/serialized-state invalidation where relevant.
- Any ADR required for public type or serialization changes.

Remove stale claims and commands. Keep useful historical changelog information. Ensure documentation describes the implemented and verified state, not the intended future state.

## Definition of done

This prompt is complete only when all applicable conditions are true:

- All first-party PHP executes on PHP 8.5 without known unsuppressed deprecations or warnings in covered paths.
- Changed code has accurate type contracts and no broad analysis suppressions.
- Modern language features are used only where justified and documented.
- Persisted/serialized compatibility changes have migrations or rollout instructions and tests.
- PHP tests, migrations, seeders, commands, and application bootstrap run under PHP 8.5.
- Independent runtime, compatibility, and test reviewers complete their checks and all valid findings are fixed.
- The final work is documented, committed, and safely pushed on `main`.

## Required final report

Return one factual report with these sections:

1. Branch and initial working-tree state.
2. Applicable instructions and canonical plan file.
3. Analysis subagents created, their scopes, and the important findings accepted or rejected.
4. Plan changes made before implementation.
5. Implementation summary by module and file group.
6. Database, security, performance, accessibility, localization, factory, seeder, and test impact where applicable.
7. Exact verification commands and observed results.
8. Independent reviewers created, findings, fixes, and remaining rejected findings with reasons.
9. Documentation synchronized.
10. Final Git status, commit hashes, and factual push result.
11. Remaining limitations, containing only genuine external or environmental blockers with exact affected requirement or plan identifiers.

Do not describe planned work as completed. Do not hide unfinished in-scope work in a limitations section.

## Start now

Read the applicable instruction chain and first-party documentation, inspect the repository, create the required analysis subagents, update the canonical plan, and then immediately implement the complete in-scope plan. Do not stop after analysis or planning.
