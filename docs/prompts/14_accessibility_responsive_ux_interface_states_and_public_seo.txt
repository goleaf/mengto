# 14. Accessibility, Responsive UX, Interface States, and Public SEO

## Principal-agent role

You are the principal Codex implementation agent responsible for making every important user journey keyboard, screen-reader, touch, zoom, responsive, and failure-state accessible while applying SEO only to genuinely public indexable content.

## Mission

Audit and repair semantic HTML, headings, landmarks, labels, focus, keyboard flows, dialogs, status announcements, touch targets, contrast, motion, responsive layouts, loading/empty/error/offline states, and public-page metadata. Create browser and PHP coverage for critical journeys and prevent SEO logic from leaking into Blade.

This is not an audit-only, documentation-only, planning-only, or review-only task. Discovery and planning are mandatory intermediate phases. After updating the canonical plan, immediately perform the required implementation, tests, review fixes, documentation synchronization, commit, and safe push directly on `main`.

## Repository-wide technology baseline

Preserve and enforce this target unless a stricter, current repository requirement applies:

- PHP 8.5.
- Laravel 13, using the latest stable mutually compatible 13.x release available at execution time.
- Livewire 4, using the latest stable mutually compatible 4.x release available at execution time.
- Class-based Livewire components with separate PHP classes and Blade views. Do not use Volt.
- Tailwind CSS 4, using the latest stable mutually compatible 4.x release available at execution time.
- The latest stable mutually compatible Vite, `laravel-vite-plugin`, and `@tailwindcss/vite` releases.
- Flux or Flux Pro only when already installed, licensed, intentionally used, and compatible with the selected Livewire version.
- The repository's established PHP test framework, upgraded to the latest stable release compatible with PHP 8.5 and Laravel 13.
- The repository's established JavaScript package manager and lock file. Never introduce a second lock file.

Verify exact versions from official documentation and authoritative package metadata before changing constraints. Never install alpha, beta, release-candidate, development-branch, or unmaintained fork dependencies unless a current repository requirement explicitly requires one and the reason is documented.

## Non-negotiable operating protocol

1. **Work only on the existing `main` branch.**
   - Begin with `git status --short --branch`, `git branch --show-current`, `git remote -v`, and a concise inspection of staged, unstaged, and untracked changes.
   - If the current branch is not `main`, safely switch to the existing `main` branch without losing, overwriting, stashing away, or silently absorbing unrelated user work.
   - Never create a feature branch, temporary branch, worktree branch, release branch, or review branch for this task.

2. **Protect the working tree.**
   - Preserve all unrelated existing changes.
   - Do not use `git reset --hard`, `git clean -fd`, `git checkout -- .`, force push, history rewriting, or any command that can discard work.
   - Before editing a file that already has user changes, inspect and merge carefully rather than replacing the file wholesale.
   - Commit only task-owned changes. Do not claim unrelated user changes as your work.

3. **Load governing instructions before work.**
   - Read the applicable `AGENTS.md` and `AGENTS.override.md` instruction chain from repository root to the current directory.
   - Read the canonical plan, requirements, architecture, testing, security, deployment, localization, and coding-convention documents.
   - Read all relevant first-party Markdown files before making architectural assumptions.
   - Treat nested instructions as scoped overrides rather than optional suggestions.

4. **Use specialized subagents for discovery.**
   - Spawn every required analysis subagent listed in this prompt.
   - Give each subagent a bounded scope, explicit files or domains, expected checks, and a structured deliverable.
   - Run independent scopes in parallel when safe.
   - Do not allow multiple subagents to edit the same files concurrently.
   - The principal agent remains the sole integrator and decision owner.

5. **Do not ask the user questions.**
   - Resolve ambiguity through repository evidence: code, tests, database schema, configuration, logs available in the repository, local Git history, first-party documentation, and current runtime behavior.
   - Choose the safest non-destructive, testable, backward-compatible interpretation.
   - Record material assumptions in the plan or an ADR and continue.
   - If an external credential, service, permission, or production-only resource is unavailable, complete all unblocked work, create deterministic fakes or isolated tests where appropriate, and document the exact external blocker with evidence.

6. **Quality is more important than speed.**
   - Work in small, reviewable, dependency-aware passes.
   - Inspect the diff after each meaningful pass.
   - Run targeted checks immediately after each change set.
   - Fix root causes rather than suppressing warnings, weakening tests, increasing timeouts blindly, or documenting an avoidable defect as future work.

7. **The plan is a mandatory gate, not the final deliverable.**
   - Locate the canonical execution plan. Prefer the existing canonical file; if none exists, create `PLANS.md` at repository root.
   - Do not create competing plan files.
   - Update the plan after discovery and before modifying production code.
   - Immediately begin implementation after saving the updated plan. Do not stop after analysis, recommendations, a checklist, or documentation.
   - Keep the plan current throughout implementation and mark items complete only after evidence-based verification.

8. **Implement, test, review, fix, and re-verify.**
   - Make all safe in-scope code, database, configuration, test, seed, and documentation changes required by the plan.
   - After implementation, spawn the independent reviewer subagents specified in this prompt. Reviewers must not be the same agents that implemented the change.
   - Use Codex `/review` for uncommitted changes when available, in addition to specialist reviews.
   - The principal agent must triage every finding, fix every valid in-scope finding, and record any rejected finding with a concise evidence-based reason.
   - Re-run all affected checks after fixes.

9. **Never fabricate evidence.**
   - Do not claim a command ran, a test passed, a build succeeded, a review completed, a commit was created, or a push succeeded unless you observed the result.
   - Record exact commands, exit codes when useful, counts, failures, skips, warnings, and material output.
   - Distinguish pre-existing failures from regressions introduced by this task.

10. **Finish documentation and Git work.**
    - Synchronize the plan, compliance matrix, changelog, and all affected first-party documentation with the final implementation.
    - Review the complete diff and staged diff for secrets, generated artifacts, unrelated changes, accidental deletions, and stale comments.
    - Create one or more coherent commits directly on `main` after applicable checks pass.
    - Push `main` only when a remote is configured, credentials are available, repository rules permit it, and the push can be performed safely.
    - Report the actual commit hashes and push result.

## Subagent coordination contract

The principal agent must create a work ledger before delegation. The ledger must identify each subagent, its exclusive scope, expected output, dependencies, and completion status.

Every analysis subagent must return a concise structured report containing:

1. Scope inspected.
2. Files, classes, routes, tables, components, or workflows examined.
3. Confirmed findings, each with severity and evidence.
4. Suspected findings that require principal-agent validation.
5. Missing coverage or unavailable evidence.
6. Recommended implementation order.
7. Suggested tests and verification commands.
8. Risks of changing the affected area.

Subagents must not dump raw logs without interpretation. The principal agent must reconcile conflicting reports, verify high-impact claims directly, and convert accepted findings into plan items with owners and acceptance criteria.

## Required analysis subagents and detailed roles

Spawn at least the following specialized subagents. Give each one the exact mission, scope, checks, deliverable, and restrictions below.

### Semantic HTML and Structure Analyst

- **Mission:** Audit document structure, landmarks, headings, controls, tables, lists, and native semantics.
- **Scope:** Layouts, pages, Blade/Flux components, forms, tables, navigation, dialogs, and public pages.
- **Must inspect:**
  - Missing/duplicate page headings, incorrect heading order, div-based controls, landmark misuse, inaccessible tables, and redundant ARIA.
  - Component abstractions that generate invalid nesting.
  - Semantic differences across loading/empty states.
- **Required checks:**
  - Inspect rendered markup for critical pages.
  - Prefer native elements over ARIA emulation.
  - Check repeated component use and IDs.
- **Required deliverable:**
  - A semantic defect inventory.
  - Required markup/component changes.
  - Page-level structure test cases.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Keyboard, Focus, and Dialog Analyst

- **Mission:** Ensure every critical workflow is operable by keyboard with predictable focus movement.
- **Scope:** Navigation, forms, menus, dropdowns, dialogs, drawers, popovers, tabs, sortable lists, uploads, and Livewire updates.
- **Must inspect:**
  - Invisible focus, keyboard traps, lost focus after validation/navigation, incorrect modal trap/restore, inaccessible drag-and-drop, and hover-only actions.
  - Focus order disconnected from visual order.
  - Duplicate shortcuts or event handlers.
- **Required checks:**
  - Walk complete keyboard journeys.
  - Check open/close/submit/error focus targets.
  - Require alternative controls for sorting/reordering.
- **Required deliverable:**
  - A keyboard/focus flow map.
  - Defects with reproduction steps.
  - Required browser tests.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Screen Reader and Live-Region Analyst

- **Mission:** Verify names, descriptions, errors, statuses, dynamic updates, and reading order.
- **Scope:** Inputs, icon buttons, alerts, loading indicators, validation summaries, Livewire updates, tables, charts, images, and modals.
- **Must inspect:**
  - Missing accessible names, duplicate labels, detached errors, silent async completion, noisy live regions, and decorative content announced.
  - Status encoded only visually.
  - Dynamic content replacing focused nodes.
- **Required checks:**
  - Map accessible name/description computation.
  - Review live-region politeness and atomicity.
  - Check alternative text and chart summaries.
- **Required deliverable:**
  - A screen-reader findings report.
  - Required labels/descriptions/announcements.
  - Test and manual-review checklist.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Responsive and Touch UX Analyst

- **Mission:** Evaluate critical interfaces across viewport sizes, orientation, zoom, and touch input.
- **Scope:** Navigation, sidebars, tables, forms, cards, filters, dialogs, toolbars, media, charts, pagination, and long translations.
- **Must inspect:**
  - Horizontal overflow, fixed dimensions, tiny targets, unreachable controls, hover dependencies, mobile keyboard overlap, and orientation issues.
  - Desktop/mobile content inconsistency.
  - Dialogs exceeding viewport.
- **Required checks:**
  - Test representative widths and 200% zoom where practical.
  - Use realistic long data and translations.
  - Check touch and coarse-pointer behavior.
- **Required deliverable:**
  - A responsive UX matrix.
  - Defects by page/component/width.
  - Required layout and browser-test changes.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Forms and Error-Recovery Accessibility Analyst

- **Mission:** Audit labels, instructions, validation, required states, grouping, autocomplete, and recovery after server errors.
- **Scope:** All forms, Livewire forms, file inputs, date/time inputs, multi-step flows, and confirmation screens.
- **Must inspect:**
  - Placeholder-only labels, inaccessible required indicators, errors not associated to fields, focus not moved after failure, lost input, and ambiguous disabled/loading states.
  - Incorrect autocomplete or input modes.
  - Confirmation/error messages not localized.
- **Required checks:**
  - Trace valid and invalid submissions.
  - Check fieldset/legend and group semantics.
  - Verify duplicate submission prevention without trapping users.
- **Required deliverable:**
  - A form accessibility matrix.
  - Required component/form changes.
  - Positive and failure browser tests.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Interface State and Resilience Analyst

- **Mission:** Ensure every data-driven interface has clear initial, loading, empty, filtered-empty, success, recoverable-error, fatal-error, offline, unauthorized, pending, and completed behavior.
- **Scope:** Livewire components, pages, tables, imports, uploads, integrations, notifications, and long operations.
- **Must inspect:**
  - Missing states, misleading skeletons, global blocking loaders, stale success, retries without explanation, and unsaved/dirty data loss.
  - No offline prevention for unsafe actions.
  - Inconsistent terminology and severity.
- **Required checks:**
  - Build a state matrix per critical component.
  - Check precise `wire:target`, retry/cancel, focus, and announcements.
  - Verify no success is shown before server confirmation.
- **Required deliverable:**
  - An interface-state coverage matrix.
  - Missing states and required copy.
  - Tests and instrumentation.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Public SEO and Metadata Analyst

- **Mission:** Determine which routes are genuinely public/indexable and implement safe localized metadata only there.
- **Scope:** Public routes, layouts, titles/descriptions, canonical URLs, robots, sitemaps, Open Graph, structured data, pagination, locale routes, and tests.
- **Must inspect:**
  - Private pages accidentally indexable, duplicate titles/canonicals, unsafe JSON-LD, hardcoded metadata, and filtered-page indexing ambiguity.
  - SEO logic executed in Blade or causing queries.
  - Missing locale/canonical relationships.
- **Required checks:**
  - Classify route indexability.
  - Prepare typed metadata outside Blade.
  - Validate structured data and escaping.
  - Avoid unnecessary SEO infrastructure for private apps.
- **Required deliverable:**
  - A public-route SEO matrix.
  - Required metadata changes.
  - SEO tests and not-applicable decisions.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Accessibility and Browser Test Architect

- **Mission:** Design automated and manual verification for critical user journeys.
- **Scope:** Existing browser framework, component/feature tests, accessibility tooling, fixtures, locales, and responsive setups.
- **Must inspect:**
  - Missing keyboard/focus assertions, console checks, responsive states, and accessibility scans.
  - Overreliance on automated scores without manual workflow tests.
  - Flaky animation/timing assumptions.
- **Required checks:**
  - Reuse the existing browser framework.
  - Define stable selectors and deterministic data.
  - Separate automated checks from documented manual checks.
- **Required deliverable:**
  - A browser/accessibility test plan.
  - Critical journey list.
  - Exact viewport, locale, input-mode, and failure-state scenarios.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

## Mandatory plan update before implementation

Update the canonical plan with a dedicated section for this task. At minimum include:

- Critical user journeys and pages ranked by accessibility and business impact.
- Semantic, keyboard, focus, screen-reader, form, responsive, touch, contrast, motion, and interface-state defects.
- A state-coverage matrix for each critical data-driven component.
- Public/indexable route classification and SEO metadata requirements.
- Component-level fixes versus page-level fixes and shared design-system dependencies.
- Browser, Livewire, feature, automated accessibility, and manual verification cases.
- Long translation, zoom, mobile keyboard, forced-color, and reduced-motion scenarios.
- Acceptance criteria tied to requirement IDs and exact pages/components.

Give every plan item a stable identifier, dependency, owner, affected files or modules, acceptance criteria, required tests, verification command, status, and rollback note when relevant.

Save the plan, then immediately begin implementation. Do not return a plan to the user and wait for approval.

## Domain-specific guardrails

- Do not add ARIA when a native semantic element already provides correct behavior.
- Do not rely on color alone, hover alone, placeholder text, or visual order alone.
- Do not add public SEO infrastructure to private/authenticated applications without an explicit public use case.
- Do not generate SEO business data or JSON-LD inside Blade.
- Do not claim full accessibility based only on an automated scanner; verify critical keyboard and focus journeys.
- Do not remove animations globally when reduced-motion variants can preserve intent safely.

## Required implementation scope

Complete all applicable work below. Treat the list as a minimum, not as permission to ignore defects discovered in the same scope.

- Correct document landmarks, heading hierarchy, native controls, table semantics, lists, IDs, and component markup.
- Provide accessible names for icon controls, programmatic labels and descriptions for inputs, associated field errors, grouping semantics, and useful instructions.
- Implement visible focus, logical tab order, correct dialog focus trap/restore, keyboard menu/tab behavior, and alternative controls for drag-and-drop/sorting.
- Add appropriate live-region/status announcements for loading, validation, completion, errors, and dynamic Livewire updates without excessive noise.
- Ensure touch targets, zoom, long text, translated text, mobile keyboards, portrait/landscape, and coarse pointers work across critical screens.
- Implement precise loading, empty, filtered-empty, success, recoverable/fatal error, offline, unauthorized, dirty, pending, and completed states.
- Prevent duplicate submissions while retaining clear progress and recovery; never show success before server confirmation.
- Respect reduced motion, forced colors, focus visibility, sufficient contrast, and non-color status cues.
- Classify public routes and implement localized typed title/description/canonical/robots/Open Graph/structured-data behavior only where applicable.
- Add automated accessibility scans when supported, plus deterministic browser tests for keyboard, focus, forms, dialogs, responsive layouts, long locales, navigation, and console errors.
- Document manual verification steps and genuine tooling limitations.

## Required verification

Run the repository's real commands rather than inventing command names. Perform all safe applicable checks below and capture exact results:

- Rendered semantic structure and heading/landmark validation for critical pages.
- Keyboard-only completion of critical authentication, create/edit, navigation, modal, upload, and sorting flows.
- Focus movement after open/close, validation failure, successful submission, Livewire update, and navigation.
- Accessible-name, description, field-error, live-region, table, image, and chart-summary checks.
- Responsive tests across representative widths/orientations, touch/coarse pointer, long translations, and zoom.
- Reduced-motion, forced-colors, contrast, dark-mode, and focus-visible checks where applicable.
- Interface-state tests for loading, empty, filtered empty, error, offline, pending, and completion.
- SEO tests for public route metadata, canonical/robots, localization, JSON-LD safety, and private-page noindex behavior.
- Full PHP/Livewire/browser suites, frontend build, console inspection, and accessibility-tool output when available.

## Independent review protocol

After implementation and before committing:

1. Freeze broad feature work and capture the exact review scope with `git diff`, `git diff --staged`, and the relevant commit range when applicable.
2. Spawn every reviewer subagent listed below. Reviewer agents must be independent from implementation agents.
3. Run Codex `/review` against uncommitted changes when available.
4. Require reviewers to inspect behavior, not only style.
5. Require each finding to include severity, exact location, failure scenario, evidence, and a proposed correction or test.
6. Have the principal agent reproduce or validate each material finding.
7. Fix every valid in-scope finding.
8. Add a regression test for every behavior defect when practical.
9. Re-run targeted checks after each fix and the full applicable verification suite before completion.
10. Record reviewer findings and dispositions in the canonical code-review document or plan.

## Required independent reviewer subagents

### Accessibility Reviewer

- **Review mission:** Independently inspect critical journeys for semantic, keyboard, screen-reader, form, focus, contrast, and motion defects.
- **Scope:** Changed components/pages, browser output, tests, and accessibility documentation.
- **Must inspect:**
  - Automated-tool false confidence, missing accessible names/errors, focus traps/loss, noisy/silent updates, and native-semantic misuse.
  - Regressions hidden in mobile or error states.
  - ARIA that conflicts with native behavior.
- **Required checks:**
  - Complete journeys keyboard-only.
  - Inspect accessibility tree where tools permit.
  - Review success and failure states.
- **Required deliverable:**
  - Severity-ranked accessibility findings.
  - Manual reproduction steps.
  - Required fixes and tests.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### Responsive UX Reviewer

- **Review mission:** Test real content, long translations, zoom, orientation, touch, and mobile interaction independently.
- **Scope:** Critical screens, shared components, navigation, forms, tables, dialogs, and media.
- **Must inspect:**
  - Overflow, clipping, tiny targets, hidden controls, fixed heights, keyboard overlap, and content inconsistency.
  - Hover dependencies and coarse-pointer failures.
  - Breakpoint gaps.
- **Required checks:**
  - Use multiple widths and long data.
  - Test 200% zoom where practical.
  - Exercise touch-equivalent interactions.
- **Required deliverable:**
  - Responsive findings by viewport.
  - Required component/layout changes.
  - Browser-test gaps.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### SEO and Semantic Reviewer

- **Review mission:** Verify public-route classification, metadata accuracy, canonicalization, robots behavior, and safe structured data.
- **Scope:** Public/private routes, metadata builders, layouts, JSON-LD, locale handling, sitemap/robots, and tests.
- **Must inspect:**
  - Private content indexability, duplicate canonical/title, stale metadata, query work in Blade, unsafe serialization, and locale mistakes.
  - SEO added where not applicable.
  - Pagination/filter indexing errors.
- **Required checks:**
  - Inspect rendered head output.
  - Validate representative structured data.
  - Compare route policy with indexing policy.
- **Required deliverable:**
  - SEO/semantic findings.
  - Required metadata fixes.
  - A public indexing readiness verdict.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

## Required documentation synchronization

Update the canonical plan plus all affected first-party documentation. At minimum review and update, when present or applicable:

- The canonical plan, requirements, compliance matrix, and changelog.
- Accessibility, frontend, design-system, Livewire, Tailwind, localization, testing, and SEO documentation where applicable.
- Critical journey, interface-state, responsive viewport, and manual verification matrices.
- Public-route SEO/indexability matrix.
- AGENTS.md code-review rules for accessibility and passive metadata preparation.

Remove stale claims and commands. Keep useful historical changelog information. Ensure documentation describes the implemented and verified state, not the intended future state.

## Definition of done

This prompt is complete only when all applicable conditions are true:

- Critical journeys are operable by keyboard, screen reader, touch, and zoom with correct focus and semantic structure.
- All critical forms have labels, associated errors, preserved input, clear progress, and recoverable failure behavior.
- Every important data-driven interface has deliberate loading, empty, error, offline, pending, and success states.
- Responsive behavior is verified with realistic data and translations across required widths and input modes.
- Motion, contrast, forced colors, touch targets, and non-color cues meet documented requirements.
- SEO exists only for applicable public routes and metadata is localized, safe, canonical, and prepared outside Blade.
- Independent accessibility, responsive, and SEO reviewers are resolved, and changes are documented, committed, and safely pushed on `main`.

## Required final report

Return one factual report with these sections:

1. Branch and initial working-tree state.
2. Applicable instructions and canonical plan file.
3. Analysis subagents created, their scopes, and the important findings accepted or rejected.
4. Plan changes made before implementation.
5. Implementation summary by module and file group.
6. Database, security, performance, accessibility, localization, factory, seeder, and test impact where applicable.
7. Exact verification commands and observed results.
8. Independent reviewers created, findings, fixes, and remaining rejected findings with reasons.
9. Documentation synchronized.
10. Final Git status, commit hashes, and factual push result.
11. Remaining limitations, containing only genuine external or environmental blockers with exact affected requirement or plan identifiers.

Do not describe planned work as completed. Do not hide unfinished in-scope work in a limitations section.

## Start now

Read the applicable instruction chain and first-party documentation, inspect the repository, create the required analysis subagents, update the canonical plan, and then immediately implement the complete in-scope plan. Do not stop after analysis or planning.
