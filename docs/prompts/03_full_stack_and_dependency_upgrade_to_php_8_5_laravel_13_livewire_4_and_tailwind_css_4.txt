# 03. Full Stack and Dependency Upgrade to PHP 8.5, Laravel 13, Livewire 4, and Tailwind CSS 4

## Principal-agent role

You are the principal Codex implementation agent responsible for upgrading the entire application stack and dependency graph to the latest stable mutually compatible releases while preserving behavior and producing a reproducible working build.

## Mission

Upgrade PHP, Laravel, Livewire, Tailwind, Vite, Flux when applicable, testing tools, and all justified direct dependencies. Do not stop at version constraints: resolve package blockers, update lock files through the correct package managers, repair source incompatibilities, and prove that the application boots, tests, migrates, seeds, and builds.

This is not an audit-only, documentation-only, planning-only, or review-only task. Discovery and planning are mandatory intermediate phases. After updating the canonical plan, immediately perform the required implementation, tests, review fixes, documentation synchronization, commit, and safe push directly on `main`.

## Repository-wide technology baseline

Preserve and enforce this target unless a stricter, current repository requirement applies:

- PHP 8.5.
- Laravel 13, using the latest stable mutually compatible 13.x release available at execution time.
- Livewire 4, using the latest stable mutually compatible 4.x release available at execution time.
- Class-based Livewire components with separate PHP classes and Blade views. Do not use Volt.
- Tailwind CSS 4, using the latest stable mutually compatible 4.x release available at execution time.
- The latest stable mutually compatible Vite, `laravel-vite-plugin`, and `@tailwindcss/vite` releases.
- Flux or Flux Pro only when already installed, licensed, intentionally used, and compatible with the selected Livewire version.
- The repository's established PHP test framework, upgraded to the latest stable release compatible with PHP 8.5 and Laravel 13.
- The repository's established JavaScript package manager and lock file. Never introduce a second lock file.

Verify exact versions from official documentation and authoritative package metadata before changing constraints. Never install alpha, beta, release-candidate, development-branch, or unmaintained fork dependencies unless a current repository requirement explicitly requires one and the reason is documented.

## Non-negotiable operating protocol

1. **Work only on the existing `main` branch.**
   - Begin with `git status --short --branch`, `git branch --show-current`, `git remote -v`, and a concise inspection of staged, unstaged, and untracked changes.
   - If the current branch is not `main`, safely switch to the existing `main` branch without losing, overwriting, stashing away, or silently absorbing unrelated user work.
   - Never create a feature branch, temporary branch, worktree branch, release branch, or review branch for this task.

2. **Protect the working tree.**
   - Preserve all unrelated existing changes.
   - Do not use `git reset --hard`, `git clean -fd`, `git checkout -- .`, force push, history rewriting, or any command that can discard work.
   - Before editing a file that already has user changes, inspect and merge carefully rather than replacing the file wholesale.
   - Commit only task-owned changes. Do not claim unrelated user changes as your work.

3. **Load governing instructions before work.**
   - Read the applicable `AGENTS.md` and `AGENTS.override.md` instruction chain from repository root to the current directory.
   - Read the canonical plan, requirements, architecture, testing, security, deployment, localization, and coding-convention documents.
   - Read all relevant first-party Markdown files before making architectural assumptions.
   - Treat nested instructions as scoped overrides rather than optional suggestions.

4. **Use specialized subagents for discovery.**
   - Spawn every required analysis subagent listed in this prompt.
   - Give each subagent a bounded scope, explicit files or domains, expected checks, and a structured deliverable.
   - Run independent scopes in parallel when safe.
   - Do not allow multiple subagents to edit the same files concurrently.
   - The principal agent remains the sole integrator and decision owner.

5. **Do not ask the user questions.**
   - Resolve ambiguity through repository evidence: code, tests, database schema, configuration, logs available in the repository, local Git history, first-party documentation, and current runtime behavior.
   - Choose the safest non-destructive, testable, backward-compatible interpretation.
   - Record material assumptions in the plan or an ADR and continue.
   - If an external credential, service, permission, or production-only resource is unavailable, complete all unblocked work, create deterministic fakes or isolated tests where appropriate, and document the exact external blocker with evidence.

6. **Quality is more important than speed.**
   - Work in small, reviewable, dependency-aware passes.
   - Inspect the diff after each meaningful pass.
   - Run targeted checks immediately after each change set.
   - Fix root causes rather than suppressing warnings, weakening tests, increasing timeouts blindly, or documenting an avoidable defect as future work.

7. **The plan is a mandatory gate, not the final deliverable.**
   - Locate the canonical execution plan. Prefer the existing canonical file; if none exists, create `PLANS.md` at repository root.
   - Do not create competing plan files.
   - Update the plan after discovery and before modifying production code.
   - Immediately begin implementation after saving the updated plan. Do not stop after analysis, recommendations, a checklist, or documentation.
   - Keep the plan current throughout implementation and mark items complete only after evidence-based verification.

8. **Implement, test, review, fix, and re-verify.**
   - Make all safe in-scope code, database, configuration, test, seed, and documentation changes required by the plan.
   - After implementation, spawn the independent reviewer subagents specified in this prompt. Reviewers must not be the same agents that implemented the change.
   - Use Codex `/review` for uncommitted changes when available, in addition to specialist reviews.
   - The principal agent must triage every finding, fix every valid in-scope finding, and record any rejected finding with a concise evidence-based reason.
   - Re-run all affected checks after fixes.

9. **Never fabricate evidence.**
   - Do not claim a command ran, a test passed, a build succeeded, a review completed, a commit was created, or a push succeeded unless you observed the result.
   - Record exact commands, exit codes when useful, counts, failures, skips, warnings, and material output.
   - Distinguish pre-existing failures from regressions introduced by this task.

10. **Finish documentation and Git work.**
    - Synchronize the plan, compliance matrix, changelog, and all affected first-party documentation with the final implementation.
    - Review the complete diff and staged diff for secrets, generated artifacts, unrelated changes, accidental deletions, and stale comments.
    - Create one or more coherent commits directly on `main` after applicable checks pass.
    - Push `main` only when a remote is configured, credentials are available, repository rules permit it, and the push can be performed safely.
    - Report the actual commit hashes and push result.

## Subagent coordination contract

The principal agent must create a work ledger before delegation. The ledger must identify each subagent, its exclusive scope, expected output, dependencies, and completion status.

Every analysis subagent must return a concise structured report containing:

1. Scope inspected.
2. Files, classes, routes, tables, components, or workflows examined.
3. Confirmed findings, each with severity and evidence.
4. Suspected findings that require principal-agent validation.
5. Missing coverage or unavailable evidence.
6. Recommended implementation order.
7. Suggested tests and verification commands.
8. Risks of changing the affected area.

Subagents must not dump raw logs without interpretation. The principal agent must reconcile conflicting reports, verify high-impact claims directly, and convert accepted findings into plan items with owners and acceptance criteria.

## Required analysis subagents and detailed roles

Spawn at least the following specialized subagents. Give each one the exact mission, scope, checks, deliverable, and restrictions below.

### Composer Dependency Resolver

- **Mission:** Build the complete PHP dependency graph and design a safe Composer upgrade sequence.
- **Scope:** composer.json, composer.lock, Composer scripts, plugins, repositories, platform configuration, PHP extensions, and package discovery.
- **Must inspect:**
  - Direct and transitive constraints.
  - Packages blocking PHP 8.5 or Laravel 13.
  - Abandoned packages, security advisories, forks, replacement packages, and duplicated framework functionality.
  - Composer plugin permissions and unsafe scripts.
- **Required checks:**
  - Use Composer diagnostics and why/why-not evidence.
  - Separate required production dependencies from development-only tools.
  - Check package licenses and maintenance state for new or replacement packages.
  - Identify minimal targeted update groups.
- **Required deliverable:**
  - A dependency graph summary.
  - An ordered Composer upgrade plan.
  - Exact blockers and candidate remedies.
  - Required source-code migration notes for each major package.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### PHP Platform Compatibility Analyst

- **Mission:** Verify runtime, extension, and toolchain compatibility with PHP 8.5.
- **Scope:** PHP version constraints, extensions, Docker/server images, CI matrices, scripts, static analysis, test runner, and framework dependencies.
- **Must inspect:**
  - Missing extensions, obsolete INI assumptions, platform overrides, and incompatible binaries.
  - PHP 8.5 deprecations exposed by dependencies.
  - CI or deployment configurations still pinned to older PHP.
  - Composer platform emulation that hides real incompatibility.
- **Required checks:**
  - Compare declared and actual runtime.
  - Identify environment-specific blockers.
  - Check test and analysis tools for PHP 8.5 support.
  - Propose safe extension and deployment documentation updates.
- **Required deliverable:**
  - A platform compatibility matrix.
  - Required environment changes.
  - A list of source and toolchain blockers.
  - Verification commands for each environment.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Laravel 13 Package Upgrade Analyst

- **Mission:** Analyze all Laravel framework and first-party package upgrade requirements.
- **Scope:** laravel/framework, authentication packages, Sanctum, Scout, Cashier, Horizon, Reverb, Telescope, Octane, Boost, filesystem, mail, queue, and package discovery.
- **Must inspect:**
  - Breaking changes between current version and Laravel 13.
  - First-party package compatibility.
  - Outdated bootstrap, config, service provider, and middleware conventions.
  - Packages that are installed but operationally unsupported by repository constraints.
- **Required checks:**
  - Cross-check official upgrade requirements.
  - Identify configuration files that must be merged rather than overwritten.
  - Determine whether large optional packages should be retained, upgraded, or removed.
  - List required regression tests.
- **Required deliverable:**
  - A Laravel-specific upgrade checklist.
  - Package compatibility decisions.
  - Configuration migration risks.
  - Required code and test changes.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Livewire and Flux Dependency Analyst

- **Mission:** Determine the correct Livewire 4 and Flux upgrade path and source compatibility risks.
- **Scope:** livewire/livewire, Flux packages, Livewire config, Volt packages, component discovery, Alpine integration, uploads, pagination, navigation, and tests.
- **Must inspect:**
  - Installed Livewire generation and exact API usage.
  - Volt components or dependencies that must be removed or migrated.
  - Flux/Flux Pro version and licensing assumptions.
  - Third-party Livewire packages that block the target version.
- **Required checks:**
  - Check class-based component compatibility.
  - Identify removed or changed directives, attributes, lifecycle hooks, and testing APIs.
  - Verify Alpine is not separately duplicated.
  - List components requiring targeted migration.
- **Required deliverable:**
  - A package and API compatibility matrix.
  - Volt removal/migration inventory.
  - Flux upgrade decision.
  - Component-level regression test requirements.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Tailwind, Vite, and Frontend Dependency Analyst

- **Mission:** Design the frontend dependency and build-tool upgrade without introducing a second package manager or losing CSS behavior.
- **Scope:** package.json, lock file, Vite config, Tailwind config, PostCSS config, CSS entry points, plugins, browserslist, Node constraints, and build scripts.
- **Must inspect:**
  - Packages blocking Tailwind 4 or current Vite.
  - Obsolete PostCSS plugins, incompatible Tailwind plugins, and unsafe dynamic class generation.
  - Node runtime compatibility and CI build assumptions.
  - Duplicate or unused frontend dependencies.
- **Required checks:**
  - Identify the correct CSS-first migration sequence.
  - Verify source-detection requirements and vendor paths.
  - Check build scripts, test scripts, and asset entry points.
  - Estimate visual-regression risk.
- **Required deliverable:**
  - An ordered frontend dependency upgrade plan.
  - Packages to retain, replace, or remove.
  - Configuration migration checklist.
  - Build and visual verification requirements.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Testing Toolchain Upgrade Analyst

- **Mission:** Upgrade-path analysis for PHPUnit or Pest, browser tools, static analysis, formatters, and coverage drivers.
- **Scope:** phpunit.xml, Pest config, test bootstrap, Laravel testing helpers, browser test config, PHPStan/Larastan, Pint, coverage, CI, and scripts.
- **Must inspect:**
  - Version compatibility with PHP 8.5 and Laravel 13.
  - Deprecated test annotations or APIs.
  - Incompatible mock libraries and browser drivers.
  - Coverage configuration and parallel-test assumptions.
- **Required checks:**
  - Preserve the intentional primary test framework.
  - Avoid introducing duplicate testing stacks.
  - Identify tests that will fail due to framework behavior changes.
  - Propose targeted tool upgrades and config migrations.
- **Required deliverable:**
  - A toolchain compatibility table.
  - Required config and source changes.
  - A test migration sequence.
  - Commands for baseline and final verification.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Dependency Security and Supply-Chain Analyst

- **Mission:** Identify dependency vulnerabilities, untrusted repositories, risky scripts, and supply-chain concerns.
- **Scope:** Composer audit output, package advisories, npm audit output, custom Composer repositories, Git dependencies, install scripts, and lock files.
- **Must inspect:**
  - Known advisories, abandoned packages, unverifiable forks, mutable branches, and excessive install-script privileges.
  - Secrets or tokens embedded in repository URLs or config.
  - Packages with unclear maintenance or licensing.
  - Lock-file anomalies.
- **Required checks:**
  - Prioritize exploitable issues over cosmetic version lag.
  - Verify whether an advisory applies to used functionality.
  - Recommend framework-native replacements where appropriate.
  - Define evidence required to accept a temporary advisory exception.
- **Required deliverable:**
  - A severity-ranked supply-chain report.
  - Required upgrades or removals.
  - Documented exceptions with expiry and owner.
  - Post-upgrade audit checklist.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Runtime and Build Reproducibility Analyst

- **Mission:** Verify that local, CI, testing, and production build paths can converge on one reproducible dependency set.
- **Scope:** CI workflows, Docker files, server scripts, deployment docs, cache/build commands, package scripts, and environment examples.
- **Must inspect:**
  - Mismatch between local and CI versions.
  - Missing lock-file enforcement.
  - Non-deterministic install/update commands.
  - Deployment commands that update dependencies at runtime unexpectedly.
- **Required checks:**
  - Define reproducible install commands.
  - Check production versus development dependency separation.
  - Verify build artifact ownership and cache invalidation.
  - Identify unsupported runtime services.
- **Required deliverable:**
  - A reproducibility gap report.
  - Canonical install/build/deploy command set.
  - Environment matrix updates.
  - Required smoke tests.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

## Mandatory plan update before implementation

Update the canonical plan with a dedicated section for this task. At minimum include:

- The exact current and target version matrix for PHP, Laravel, Livewire, Tailwind, Vite, Flux, Node, test tools, and direct dependencies.
- Composer and frontend dependency blockers, grouped by required upgrade, replacement, removal, or temporary documented exception.
- The ordered dependency-update sequence with checkpoints, rollback strategy, and expected source-code migrations.
- Environment, CI, container, and deployment changes required for PHP 8.5 and the selected Node/Vite toolchain.
- Lock-file strategy and the one authorized JavaScript package manager.
- Security advisories and supply-chain risks with required resolution evidence.
- Targeted tests, boot checks, migration/seed checks, and frontend build checks after each dependency group.
- A compatibility fallback plan for any externally blocked package without weakening the target baseline.

Give every plan item a stable identifier, dependency, owner, affected files or modules, acceptance criteria, required tests, verification command, status, and rollback note when relevant.

Save the plan, then immediately begin implementation. Do not return a plan to the user and wait for approval.

## Domain-specific guardrails

- Do not run an uncontrolled “update everything” and then repair an unreadable diff. Use targeted dependency groups and review each lock-file change.
- Do not manually edit resolved package records in lock files.
- Do not retain an obsolete package merely because removing it requires source changes; implement and test the replacement when in scope.
- Do not add Octane, Horizon, Reverb, Telescope, a repository package, a DTO package, a permission package, or a frontend framework without a current requirement and an operational plan.
- Do not use Composer platform overrides to pretend PHP 8.5 compatibility when the real runtime is incompatible.

## Required implementation scope

Complete all applicable work below. Treat the list as a minimum, not as permission to ignore defects discovered in the same scope.

- Capture the dependency baseline with Composer validation, audit, outdated/why/why-not diagnostics, package-manager audit, runtime versions, and current application boot/build results.
- Verify exact latest stable compatible target releases from authoritative metadata and official upgrade documentation at execution time.
- Update PHP platform constraints, Laravel 13, Livewire 4, Tailwind 4, Vite, Laravel Vite plugin, Tailwind Vite plugin, Flux when applicable, the established test framework, and justified first-party packages.
- Resolve or replace packages that block the target stack. Remove package configuration, providers, imports, assets, tests, and documentation only after proving the package is unused or fully replaced.
- Update Composer and frontend lock files through the repository package managers and inspect the full resolution diff for unexpected major changes.
- Update CI matrices, Docker/server definitions, local setup, environment examples, package scripts, Composer scripts, and deployment instructions to use the target runtimes.
- Repair all source, configuration, test, migration, seeder, Livewire, Blade, JavaScript, and CSS incompatibilities exposed by the upgrades.
- Keep one primary PHP test framework and one JavaScript package manager.
- Run targeted verification after each dependency group and a complete final verification after all groups are integrated.
- Document every retained advisory or externally blocked package with exact evidence, owner, impact, mitigation, and planned resolution condition.

## Required verification

Run the repository's real commands rather than inventing command names. Perform all safe applicable checks below and capture exact results:

- `php -v`, required extension checks, Node and package-manager versions.
- `composer validate --strict`, `composer audit`, `composer outdated --direct`, relevant `composer why` and `composer why-not` commands.
- A clean dependency install from lock files in an isolated environment when practical.
- `php artisan about`, application boot, route listing, config cache, route cache when supported, and view cache.
- Formatter, static analysis, targeted tests after each dependency group, full PHP test suite, and coverage when configured.
- Fresh testing migration, fresh testing migration plus seeders, and seeder idempotency checks.
- Frontend dependency audit, lint, type checks when present, tests, and production build using the existing package manager.
- Browser smoke tests and console inspection for critical pages when available.
- Final Composer and frontend lock-file diff inspection for prereleases, abandoned packages, duplicate runtimes, and unintended removals.

## Independent review protocol

After implementation and before committing:

1. Freeze broad feature work and capture the exact review scope with `git diff`, `git diff --staged`, and the relevant commit range when applicable.
2. Spawn every reviewer subagent listed below. Reviewer agents must be independent from implementation agents.
3. Run Codex `/review` against uncommitted changes when available.
4. Require reviewers to inspect behavior, not only style.
5. Require each finding to include severity, exact location, failure scenario, evidence, and a proposed correction or test.
6. Have the principal agent reproduce or validate each material finding.
7. Fix every valid in-scope finding.
8. Add a regression test for every behavior defect when practical.
9. Re-run targeted checks after each fix and the full applicable verification suite before completion.
10. Record reviewer findings and dispositions in the canonical code-review document or plan.

## Required independent reviewer subagents

### Dependency Resolution Reviewer

- **Review mission:** Independently verify target versions, constraints, lock files, package removals, and package-manager consistency.
- **Scope:** Composer and frontend metadata, lock files, install scripts, package removals, and compatibility documentation.
- **Must inspect:**
  - Unexpected transitive majors, prereleases, abandoned packages, unused new dependencies, and duplicate functionality.
  - Whether source changes fully replaced removed packages.
  - Whether exact runtime requirements are reproducible.
- **Required checks:**
  - Re-run key dependency diagnostics.
  - Inspect lock-file provenance and package scripts.
  - Sample package usage references before accepting removals.
- **Required deliverable:**
  - Severity-ranked resolution findings.
  - A version and reproducibility verdict.
  - Required corrections before commit.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### Runtime Compatibility Reviewer

- **Review mission:** Challenge application compatibility with PHP 8.5, Laravel 13, Livewire 4, and the selected frontend toolchain.
- **Scope:** Bootstrap, config, runtime logs, tests, migrations, seeders, Livewire components, and build output.
- **Must inspect:**
  - Hidden deprecations, runtime-only failures, stale config, changed defaults, and environment mismatch.
  - Behavior that passes unit tests but fails application boot or browser flow.
  - Incorrect extension or Node assumptions.
- **Required checks:**
  - Run representative boot and smoke paths.
  - Inspect changed framework integration files.
  - Verify target versions in actual resolved output.
- **Required deliverable:**
  - Compatibility findings with reproduction commands.
  - A list of verified target-stack capabilities.
  - Required regression tests.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### Build and Test Upgrade Reviewer

- **Review mission:** Determine whether toolchain and dependency upgrades are fully verified rather than superficially green.
- **Scope:** Test runner config, static analysis, formatter, frontend scripts, CI, coverage, browser tests, and final command logs.
- **Must inspect:**
  - Skipped or silently disabled checks, reduced analysis levels, broad ignores, flaky tests, and build warnings.
  - Whether tests were altered merely to accept regressions.
  - Whether CI and local commands match.
- **Required checks:**
  - Compare baseline and final test counts.
  - Inspect removed or skipped tests.
  - Verify production build artifacts and error-free console paths.
- **Required deliverable:**
  - A quality-gate report.
  - Missing or weakened verification.
  - Exact remediation required.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

## Required documentation synchronization

Update the canonical plan plus all affected first-party documentation. At minimum review and update, when present or applicable:

- The canonical plan and compliance matrix.
- `README.md`, `CHANGELOG.md`, `AGENTS.md`, and environment setup documentation.
- Dependency, architecture, testing, frontend, Livewire, Tailwind, deployment, and operations documentation.
- A target-version and compatibility matrix.
- Documented dependency exceptions and security-advisory dispositions.

Remove stale claims and commands. Keep useful historical changelog information. Ensure documentation describes the implemented and verified state, not the intended future state.

## Definition of done

This prompt is complete only when all applicable conditions are true:

- The actual resolved runtime uses PHP 8.5, Laravel 13, Livewire 4, Tailwind 4, and compatible stable tooling.
- No unjustified prerelease, abandoned, duplicate, or target-blocking direct dependency remains.
- Composer and frontend lock files are valid, reproducible, and generated by the correct package managers.
- The application boots, routes load, tests execute, the database migrates and seeds in isolation, and the production frontend build succeeds.
- CI/deployment/setup documentation matches the new stack.
- Independent reviewers confirm dependency resolution, runtime compatibility, and verification integrity.
- All valid findings are fixed and the final work is committed and safely pushed on `main`.

## Required final report

Return one factual report with these sections:

1. Branch and initial working-tree state.
2. Applicable instructions and canonical plan file.
3. Analysis subagents created, their scopes, and the important findings accepted or rejected.
4. Plan changes made before implementation.
5. Implementation summary by module and file group.
6. Database, security, performance, accessibility, localization, factory, seeder, and test impact where applicable.
7. Exact verification commands and observed results.
8. Independent reviewers created, findings, fixes, and remaining rejected findings with reasons.
9. Documentation synchronized.
10. Final Git status, commit hashes, and factual push result.
11. Remaining limitations, containing only genuine external or environmental blockers with exact affected requirement or plan identifiers.

Do not describe planned work as completed. Do not hide unfinished in-scope work in a limitations section.

## Start now

Read the applicable instruction chain and first-party documentation, inspect the repository, create the required analysis subagents, update the canonical plan, and then immediately implement the complete in-scope plan. Do not stop after analysis or planning.
