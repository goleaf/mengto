# 15. Complete Localization and Removal of Hardcoded User-Facing Text

## Principal-agent role

You are the principal Codex implementation agent responsible for making every user-facing message, state, notification, validation response, accessibility label, and public metadata flow through the repository’s single canonical localization system.

## Mission

Inventory all supported locales and every user-facing string source. Remove hardcoded text from PHP, Blade, Livewire, JavaScript, validation, notifications, mail, API errors, accessibility labels, and SEO. Preserve all existing locales, normalize keys and placeholders, use locale-aware formatting, and create automated coverage for missing keys and critical rendering.

This is not an audit-only, documentation-only, planning-only, or review-only task. Discovery and planning are mandatory intermediate phases. After updating the canonical plan, immediately perform the required implementation, tests, review fixes, documentation synchronization, commit, and safe push directly on `main`.

## Repository-wide technology baseline

Preserve and enforce this target unless a stricter, current repository requirement applies:

- PHP 8.5.
- Laravel 13, using the latest stable mutually compatible 13.x release available at execution time.
- Livewire 4, using the latest stable mutually compatible 4.x release available at execution time.
- Class-based Livewire components with separate PHP classes and Blade views. Do not use Volt.
- Tailwind CSS 4, using the latest stable mutually compatible 4.x release available at execution time.
- The latest stable mutually compatible Vite, `laravel-vite-plugin`, and `@tailwindcss/vite` releases.
- Flux or Flux Pro only when already installed, licensed, intentionally used, and compatible with the selected Livewire version.
- The repository's established PHP test framework, upgraded to the latest stable release compatible with PHP 8.5 and Laravel 13.
- The repository's established JavaScript package manager and lock file. Never introduce a second lock file.

Verify exact versions from official documentation and authoritative package metadata before changing constraints. Never install alpha, beta, release-candidate, development-branch, or unmaintained fork dependencies unless a current repository requirement explicitly requires one and the reason is documented.

## Non-negotiable operating protocol

1. **Work only on the existing `main` branch.**
   - Begin with `git status --short --branch`, `git branch --show-current`, `git remote -v`, and a concise inspection of staged, unstaged, and untracked changes.
   - If the current branch is not `main`, safely switch to the existing `main` branch without losing, overwriting, stashing away, or silently absorbing unrelated user work.
   - Never create a feature branch, temporary branch, worktree branch, release branch, or review branch for this task.

2. **Protect the working tree.**
   - Preserve all unrelated existing changes.
   - Do not use `git reset --hard`, `git clean -fd`, `git checkout -- .`, force push, history rewriting, or any command that can discard work.
   - Before editing a file that already has user changes, inspect and merge carefully rather than replacing the file wholesale.
   - Commit only task-owned changes. Do not claim unrelated user changes as your work.

3. **Load governing instructions before work.**
   - Read the applicable `AGENTS.md` and `AGENTS.override.md` instruction chain from repository root to the current directory.
   - Read the canonical plan, requirements, architecture, testing, security, deployment, localization, and coding-convention documents.
   - Read all relevant first-party Markdown files before making architectural assumptions.
   - Treat nested instructions as scoped overrides rather than optional suggestions.

4. **Use specialized subagents for discovery.**
   - Spawn every required analysis subagent listed in this prompt.
   - Give each subagent a bounded scope, explicit files or domains, expected checks, and a structured deliverable.
   - Run independent scopes in parallel when safe.
   - Do not allow multiple subagents to edit the same files concurrently.
   - The principal agent remains the sole integrator and decision owner.

5. **Do not ask the user questions.**
   - Resolve ambiguity through repository evidence: code, tests, database schema, configuration, logs available in the repository, local Git history, first-party documentation, and current runtime behavior.
   - Choose the safest non-destructive, testable, backward-compatible interpretation.
   - Record material assumptions in the plan or an ADR and continue.
   - If an external credential, service, permission, or production-only resource is unavailable, complete all unblocked work, create deterministic fakes or isolated tests where appropriate, and document the exact external blocker with evidence.

6. **Quality is more important than speed.**
   - Work in small, reviewable, dependency-aware passes.
   - Inspect the diff after each meaningful pass.
   - Run targeted checks immediately after each change set.
   - Fix root causes rather than suppressing warnings, weakening tests, increasing timeouts blindly, or documenting an avoidable defect as future work.

7. **The plan is a mandatory gate, not the final deliverable.**
   - Locate the canonical execution plan. Prefer the existing canonical file; if none exists, create `PLANS.md` at repository root.
   - Do not create competing plan files.
   - Update the plan after discovery and before modifying production code.
   - Immediately begin implementation after saving the updated plan. Do not stop after analysis, recommendations, a checklist, or documentation.
   - Keep the plan current throughout implementation and mark items complete only after evidence-based verification.

8. **Implement, test, review, fix, and re-verify.**
   - Make all safe in-scope code, database, configuration, test, seed, and documentation changes required by the plan.
   - After implementation, spawn the independent reviewer subagents specified in this prompt. Reviewers must not be the same agents that implemented the change.
   - Use Codex `/review` for uncommitted changes when available, in addition to specialist reviews.
   - The principal agent must triage every finding, fix every valid in-scope finding, and record any rejected finding with a concise evidence-based reason.
   - Re-run all affected checks after fixes.

9. **Never fabricate evidence.**
   - Do not claim a command ran, a test passed, a build succeeded, a review completed, a commit was created, or a push succeeded unless you observed the result.
   - Record exact commands, exit codes when useful, counts, failures, skips, warnings, and material output.
   - Distinguish pre-existing failures from regressions introduced by this task.

10. **Finish documentation and Git work.**
    - Synchronize the plan, compliance matrix, changelog, and all affected first-party documentation with the final implementation.
    - Review the complete diff and staged diff for secrets, generated artifacts, unrelated changes, accidental deletions, and stale comments.
    - Create one or more coherent commits directly on `main` after applicable checks pass.
    - Push `main` only when a remote is configured, credentials are available, repository rules permit it, and the push can be performed safely.
    - Report the actual commit hashes and push result.

## Subagent coordination contract

The principal agent must create a work ledger before delegation. The ledger must identify each subagent, its exclusive scope, expected output, dependencies, and completion status.

Every analysis subagent must return a concise structured report containing:

1. Scope inspected.
2. Files, classes, routes, tables, components, or workflows examined.
3. Confirmed findings, each with severity and evidence.
4. Suspected findings that require principal-agent validation.
5. Missing coverage or unavailable evidence.
6. Recommended implementation order.
7. Suggested tests and verification commands.
8. Risks of changing the affected area.

Subagents must not dump raw logs without interpretation. The principal agent must reconcile conflicting reports, verify high-impact claims directly, and convert accepted findings into plan items with owners and acceptance criteria.

## Required analysis subagents and detailed roles

Spawn at least the following specialized subagents. Give each one the exact mission, scope, checks, deliverable, and restrictions below.

### Locale Architecture and Routing Analyst

- **Mission:** Map locale discovery, persistence, routing, fallback, middleware, user preference, and database-translated content.
- **Scope:** Configuration, middleware, routes, sessions/cookies, user settings, language files, JSON translations, database fields, and tests.
- **Must inspect:**
  - Supported locales, fallback behavior, locale negotiation, route prefixes, canonical URLs, and persistence across login/navigation.
  - Duplicate localization systems or locale codes.
  - Tenant-specific or content-specific locale behavior.
- **Required checks:**
  - Trace locale selection through HTTP, Livewire, mail, notifications, API, and background/web-batch work.
  - Check invalid locale handling.
  - Identify RTL needs and deployment assumptions.
- **Required deliverable:**
  - A locale architecture map.
  - Canonical locale list and fallback policy.
  - Routing/persistence defects and tests.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Hardcoded String and Translation-Key Analyst

- **Mission:** Find user-visible text outside the canonical translation system and normalize key ownership.
- **Scope:** PHP, Blade, Livewire, JavaScript, validation, exceptions, notifications, mail, API resources/errors, SEO, fixtures, and tests.
- **Must inspect:**
  - Literal labels, buttons, headings, placeholders, messages, statuses, accessibility names, and metadata.
  - Dynamic concatenation that breaks grammar.
  - Duplicate or unstable keys and dead translations.
- **Required checks:**
  - Classify developer-only/log strings separately from user-facing text.
  - Map each string to a stable key or established JSON convention.
  - Avoid false positives for protocol constants or code identifiers.
- **Required deliverable:**
  - A hardcoded-string inventory.
  - Key migration map.
  - Intentional nonlocalized exceptions with reasons.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Translation Consistency and Placeholder Analyst

- **Mission:** Verify key parity, placeholders, pluralization, nesting, and semantic consistency across locales.
- **Scope:** All locale files, JSON translations, validation files, notification/mail templates, and tests.
- **Must inspect:**
  - Missing keys, extra stale keys, placeholder mismatch, plural categories, HTML in translations, escaping assumptions, and inconsistent terminology.
  - Keys returning raw identifiers.
  - Duplicate meanings under different keys.
- **Required checks:**
  - Compare locale trees and placeholder sets.
  - Validate interpolation types.
  - Check pluralization for representative counts.
- **Required deliverable:**
  - A locale parity report.
  - Missing/mismatched translations.
  - Terminology and key-consolidation recommendations.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Validation, Notification, Mail, and API Localization Analyst

- **Mission:** Audit non-page user communication for locale ownership and safe rendering.
- **Scope:** Form Requests, Livewire validation, exception messages, notifications, mailables, queued/web-triggered side effects, API errors, and user locale resolution.
- **Must inspect:**
  - Hardcoded text, wrong recipient locale, locale lost in deferred work, HTML/text mismatch, inaccessible email markup, and inconsistent API messages.
  - Fallback leaking raw keys.
  - Provider messages exposed directly.
- **Required checks:**
  - Trace recipient locale at send time.
  - Check retries and serialization of locale context.
  - Verify placeholders and escaping.
- **Required deliverable:**
  - A communication localization matrix.
  - Required code/template fixes.
  - Tests for recipient locale and failure behavior.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Locale-Aware Formatting Analyst

- **Mission:** Standardize dates, times, relative time, numbers, percentages, currency, lists, and measurements.
- **Scope:** Formatters, Blade, Livewire, API resources, notifications, mail, exports, reports, and JavaScript.
- **Must inspect:**
  - Hardcoded date formats, decimal separators, currency placement, timezone assumptions, string concatenation, and inconsistent plural output.
  - Formatting performed directly in Blade.
  - Different server/client locale behavior.
- **Required checks:**
  - Define canonical formatter ownership.
  - Check timezone and currency source.
  - Test representative locales and edge values.
- **Required deliverable:**
  - A formatting policy.
  - Formatter migration map.
  - Locale/timezone/currency tests.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Localized Content and SEO Analyst

- **Mission:** Verify localized public content, slugs, titles, descriptions, structured data, and alternate/canonical metadata where applicable.
- **Scope:** Public pages, SEO builders, route locale strategy, database translations, JSON-LD, Open Graph, and tests.
- **Must inspect:**
  - Missing locale variants, unsafe fallback, duplicate canonical URLs, mixed-language metadata, and untranslated accessibility content.
  - Queries or formatting inside Blade.
  - Unclear source locale for user-generated content.
- **Required checks:**
  - Classify translatable system text versus authored content.
  - Review fallback and indexing policy.
  - Check escaping and structured data.
- **Required deliverable:**
  - A localized public-content matrix.
  - SEO/content defects.
  - Required tests and not-applicable decisions.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Localization Test and Automation Analyst

- **Mission:** Design reliable automated checks for key coverage, placeholders, critical rendering, formatting, and locale persistence.
- **Scope:** Test suite, locale files, browser tests, static scanners, factories, seeders, and CI scripts.
- **Must inspect:**
  - Missing locale coverage, fragile snapshots, false-positive hardcoded scans, and nondeterministic timezone formatting.
  - Critical pages not rendered in every locale.
  - Tests that ignore long-string expansion.
- **Required checks:**
  - Create parity/placeholder checks.
  - Use deterministic locale/time/timezone fixtures.
  - Define critical-page and communication matrices.
- **Required deliverable:**
  - A localization test plan.
  - Automation/check scripts.
  - Required long-text and RTL fixtures.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

## Mandatory plan update before implementation

Update the canonical plan with a dedicated section for this task. At minimum include:

- The canonical supported-locale list, fallback policy, locale routing/persistence, and RTL applicability.
- Every localization source and any duplicate or parallel systems to consolidate.
- Every hardcoded user-facing string and its target stable key or established JSON translation convention.
- Locale key parity, placeholder, pluralization, terminology, HTML/escaping, and dead-key issues.
- Locale-aware date/time/number/currency/list/measurement formatting ownership.
- Recipient-locale handling for validation, notifications, mail, API errors, exports, and deferred operations.
- Localized public content/SEO behavior where applicable.
- Automated parity, hardcoded-string, formatting, critical-render, long-text, and persistence tests.

Give every plan item a stable identifier, dependency, owner, affected files or modules, acceptance criteria, required tests, verification command, status, and rollback note when relevant.

Save the plan, then immediately begin implementation. Do not return a plan to the user and wait for approval.

## Domain-specific guardrails

- Do not remove a supported locale because its translations are incomplete.
- Do not create a second localization mechanism or mix arbitrary key strategies.
- Do not concatenate translated sentence fragments in ways that assume one grammar.
- Do not insert untrusted HTML into translation strings or render translated HTML raw without an explicit trusted boundary.
- Do not localize developer-only protocol identifiers, database keys, enum storage values, or log diagnostics merely to eliminate literals.
- Do not claim translation quality that was not reviewed; use documented fallback and mark human-review needs precisely.

## Required implementation scope

Complete all applicable work below. Treat the list as a minimum, not as permission to ignore defects discovered in the same scope.

- Document the canonical locale architecture, route strategy, persistence, fallback, user/tenant preference, and invalid-locale behavior.
- Scan PHP, Blade, Livewire, JavaScript, validation, exceptions, notifications, mail, API, accessibility, SEO, exports, reports, and seed/demo UI for hardcoded user-facing strings.
- Move every applicable string to the existing canonical translation system with stable keys and update all supported locales.
- Consolidate duplicate keys, preserve backward compatibility where keys are public contracts, and remove dead keys only after reference checks.
- Verify key-tree parity, placeholder parity, pluralization, escaping, terminology, and fallback behavior across all locales.
- Replace translated sentence-fragment concatenation with complete translatable messages and named placeholders.
- Centralize locale-aware date, time, relative-time, number, percentage, currency, list, and measurement formatting outside Blade.
- Ensure validation, Livewire errors, notifications, mail, API user messages, accessibility labels, and public metadata use the correct recipient/current locale.
- Preserve locale context across deferred/queued or web-batch operations according to runtime constraints.
- Create deterministic factories/seed data for localized, Unicode, long-text, missing-optional, and RTL scenarios when applicable.
- Add automated translation parity/placeholder scans, critical-page rendering per locale, locale switching/persistence, formatting, notification/mail, validation, SEO, long-text, and fallback tests.

## Required verification

Run the repository's real commands rather than inventing command names. Perform all safe applicable checks below and capture exact results:

- Supported-locale and fallback behavior tests.
- Key and placeholder parity checks across every locale.
- Repository scan for hardcoded user-facing strings with documented intentional exclusions.
- Critical page, component, form, error, email, notification, and API message rendering in each supported locale.
- Pluralization tests for representative counts and locales.
- Locale-aware date/time/timezone, number, currency, percentage, list, and relative-time tests.
- Locale switching and persistence across login, Livewire navigation, sessions, and user settings.
- Long translation, Unicode, RTL when applicable, and responsive overflow browser checks.
- Full PHP/Livewire/browser suites, frontend build, static analysis, and final raw-key scan.

## Independent review protocol

After implementation and before committing:

1. Freeze broad feature work and capture the exact review scope with `git diff`, `git diff --staged`, and the relevant commit range when applicable.
2. Spawn every reviewer subagent listed below. Reviewer agents must be independent from implementation agents.
3. Run Codex `/review` against uncommitted changes when available.
4. Require reviewers to inspect behavior, not only style.
5. Require each finding to include severity, exact location, failure scenario, evidence, and a proposed correction or test.
6. Have the principal agent reproduce or validate each material finding.
7. Fix every valid in-scope finding.
8. Add a regression test for every behavior defect when practical.
9. Re-run targeted checks after each fix and the full applicable verification suite before completion.
10. Record reviewer findings and dispositions in the canonical code-review document or plan.

## Required independent reviewer subagents

### Translation Coverage Reviewer

- **Review mission:** Independently find user-facing literals, missing keys, placeholder mismatches, and untested communication paths.
- **Scope:** All changed source, locale files, notifications, mail, API errors, accessibility labels, SEO, tests, and scanners.
- **Must inspect:**
  - False negatives in hardcoded scans, untranslated states, locale files drifting, and raw keys.
  - Messages composed from fragments.
  - HTML/escaping mistakes.
- **Required checks:**
  - Sample every application domain and communication channel.
  - Compare placeholder sets.
  - Render critical states.
- **Required deliverable:**
  - Coverage findings.
  - Missing keys/paths.
  - Required scanner or test changes.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### Locale Behavior and Formatting Reviewer

- **Review mission:** Verify locale selection, persistence, fallback, timezone, number, currency, plural, and recipient-locale behavior.
- **Scope:** Middleware, routes, sessions, user settings, formatters, notifications, deferred work, and tests.
- **Must inspect:**
  - Locale lost across navigation or jobs/batches, invalid locale loops, mixed formatting, wrong timezone/currency, and fallback leaks.
  - Server/client inconsistencies.
  - Canonical URL errors.
- **Required checks:**
  - Trace representative users/locales.
  - Test edge values and invalid codes.
  - Inspect recipient locale at send time.
- **Required deliverable:**
  - Behavior findings.
  - Formatting corrections.
  - A locale architecture readiness verdict.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### Localization Regression and UX Reviewer

- **Review mission:** Assess terminology, long-text resilience, accessibility labels, and critical user journeys in every supported locale.
- **Scope:** Critical pages, components, forms, errors, responsive layouts, browser tests, and seeded data.
- **Must inspect:**
  - Clipping, mixed language, ambiguous terminology, untranslated icon labels, broken grammar, and different functional behavior by locale.
  - Tests that render keys but do not inspect output.
  - RTL defects where applicable.
- **Required checks:**
  - Use long and Unicode content.
  - Navigate critical flows in multiple locales.
  - Review error and empty states.
- **Required deliverable:**
  - UX/localization regressions.
  - Affected pages/locales.
  - Required fixes and tests.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

## Required documentation synchronization

Update the canonical plan plus all affected first-party documentation. At minimum review and update, when present or applicable:

- The canonical plan, requirements, compliance matrix, and changelog.
- AGENTS.md localization rules.
- Localization architecture, key naming, formatting, translation workflow, testing, accessibility, SEO, and deployment documentation.
- Supported-locale, placeholder, formatting, and critical-page matrices.
- Human-review backlog containing only translations that genuinely require language review.

Remove stale claims and commands. Keep useful historical changelog information. Ensure documentation describes the implemented and verified state, not the intended future state.

## Definition of done

This prompt is complete only when all applicable conditions are true:

- All supported locales remain functional through one canonical localization system.
- No applicable hardcoded user-facing text remains in first-party PHP, Blade, Livewire, JavaScript, validation, notifications, mail, API errors, accessibility labels, or public metadata.
- Keys, placeholders, plurals, escaping, and terminology are consistent and tested.
- Dates, times, numbers, currency, lists, and other formatted values are locale-aware and prepared outside Blade.
- Recipient locale persists correctly across notifications and deferred work.
- Critical pages and communications render in every locale, including long/Unicode and RTL scenarios where applicable.
- Independent coverage, behavior/formatting, and UX reviewers are resolved, and changes are documented, committed, and safely pushed on `main`.

## Required final report

Return one factual report with these sections:

1. Branch and initial working-tree state.
2. Applicable instructions and canonical plan file.
3. Analysis subagents created, their scopes, and the important findings accepted or rejected.
4. Plan changes made before implementation.
5. Implementation summary by module and file group.
6. Database, security, performance, accessibility, localization, factory, seeder, and test impact where applicable.
7. Exact verification commands and observed results.
8. Independent reviewers created, findings, fixes, and remaining rejected findings with reasons.
9. Documentation synchronized.
10. Final Git status, commit hashes, and factual push result.
11. Remaining limitations, containing only genuine external or environmental blockers with exact affected requirement or plan identifiers.

Do not describe planned work as completed. Do not hide unfinished in-scope work in a limitations section.

## Start now

Read the applicable instruction chain and first-party documentation, inspect the repository, create the required analysis subagents, update the canonical plan, and then immediately implement the complete in-scope plan. Do not stop after analysis or planning.
