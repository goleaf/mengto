# 13. Tailwind CSS 4, CSS-First Configuration, and Repository-Wide Design System

## Principal-agent role

You are the principal Codex implementation agent responsible for upgrading all styling to current Tailwind CSS 4 practices, establishing coherent design tokens, eliminating unsafe class generation, and improving responsive and accessible behavior without arbitrary visual redesign.

## Mission

Migrate the frontend to a clean Tailwind CSS 4 CSS-first architecture, verify source detection and Vite integration, replace obsolete configuration and plugins, centralize repeated visual decisions as design tokens, and refactor every important screen for robust mobile-first responsive behavior and accessibility.

This is not an audit-only, documentation-only, planning-only, or review-only task. Discovery and planning are mandatory intermediate phases. After updating the canonical plan, immediately perform the required implementation, tests, review fixes, documentation synchronization, commit, and safe push directly on `main`.

## Repository-wide technology baseline

Preserve and enforce this target unless a stricter, current repository requirement applies:

- PHP 8.5.
- Laravel 13, using the latest stable mutually compatible 13.x release available at execution time.
- Livewire 4, using the latest stable mutually compatible 4.x release available at execution time.
- Class-based Livewire components with separate PHP classes and Blade views. Do not use Volt.
- Tailwind CSS 4, using the latest stable mutually compatible 4.x release available at execution time.
- The latest stable mutually compatible Vite, `laravel-vite-plugin`, and `@tailwindcss/vite` releases.
- Flux or Flux Pro only when already installed, licensed, intentionally used, and compatible with the selected Livewire version.
- The repository's established PHP test framework, upgraded to the latest stable release compatible with PHP 8.5 and Laravel 13.
- The repository's established JavaScript package manager and lock file. Never introduce a second lock file.

Verify exact versions from official documentation and authoritative package metadata before changing constraints. Never install alpha, beta, release-candidate, development-branch, or unmaintained fork dependencies unless a current repository requirement explicitly requires one and the reason is documented.

## Non-negotiable operating protocol

1. **Work only on the existing `main` branch.**
   - Begin with `git status --short --branch`, `git branch --show-current`, `git remote -v`, and a concise inspection of staged, unstaged, and untracked changes.
   - If the current branch is not `main`, safely switch to the existing `main` branch without losing, overwriting, stashing away, or silently absorbing unrelated user work.
   - Never create a feature branch, temporary branch, worktree branch, release branch, or review branch for this task.

2. **Protect the working tree.**
   - Preserve all unrelated existing changes.
   - Do not use `git reset --hard`, `git clean -fd`, `git checkout -- .`, force push, history rewriting, or any command that can discard work.
   - Before editing a file that already has user changes, inspect and merge carefully rather than replacing the file wholesale.
   - Commit only task-owned changes. Do not claim unrelated user changes as your work.

3. **Load governing instructions before work.**
   - Read the applicable `AGENTS.md` and `AGENTS.override.md` instruction chain from repository root to the current directory.
   - Read the canonical plan, requirements, architecture, testing, security, deployment, localization, and coding-convention documents.
   - Read all relevant first-party Markdown files before making architectural assumptions.
   - Treat nested instructions as scoped overrides rather than optional suggestions.

4. **Use specialized subagents for discovery.**
   - Spawn every required analysis subagent listed in this prompt.
   - Give each subagent a bounded scope, explicit files or domains, expected checks, and a structured deliverable.
   - Run independent scopes in parallel when safe.
   - Do not allow multiple subagents to edit the same files concurrently.
   - The principal agent remains the sole integrator and decision owner.

5. **Do not ask the user questions.**
   - Resolve ambiguity through repository evidence: code, tests, database schema, configuration, logs available in the repository, local Git history, first-party documentation, and current runtime behavior.
   - Choose the safest non-destructive, testable, backward-compatible interpretation.
   - Record material assumptions in the plan or an ADR and continue.
   - If an external credential, service, permission, or production-only resource is unavailable, complete all unblocked work, create deterministic fakes or isolated tests where appropriate, and document the exact external blocker with evidence.

6. **Quality is more important than speed.**
   - Work in small, reviewable, dependency-aware passes.
   - Inspect the diff after each meaningful pass.
   - Run targeted checks immediately after each change set.
   - Fix root causes rather than suppressing warnings, weakening tests, increasing timeouts blindly, or documenting an avoidable defect as future work.

7. **The plan is a mandatory gate, not the final deliverable.**
   - Locate the canonical execution plan. Prefer the existing canonical file; if none exists, create `PLANS.md` at repository root.
   - Do not create competing plan files.
   - Update the plan after discovery and before modifying production code.
   - Immediately begin implementation after saving the updated plan. Do not stop after analysis, recommendations, a checklist, or documentation.
   - Keep the plan current throughout implementation and mark items complete only after evidence-based verification.

8. **Implement, test, review, fix, and re-verify.**
   - Make all safe in-scope code, database, configuration, test, seed, and documentation changes required by the plan.
   - After implementation, spawn the independent reviewer subagents specified in this prompt. Reviewers must not be the same agents that implemented the change.
   - Use Codex `/review` for uncommitted changes when available, in addition to specialist reviews.
   - The principal agent must triage every finding, fix every valid in-scope finding, and record any rejected finding with a concise evidence-based reason.
   - Re-run all affected checks after fixes.

9. **Never fabricate evidence.**
   - Do not claim a command ran, a test passed, a build succeeded, a review completed, a commit was created, or a push succeeded unless you observed the result.
   - Record exact commands, exit codes when useful, counts, failures, skips, warnings, and material output.
   - Distinguish pre-existing failures from regressions introduced by this task.

10. **Finish documentation and Git work.**
    - Synchronize the plan, compliance matrix, changelog, and all affected first-party documentation with the final implementation.
    - Review the complete diff and staged diff for secrets, generated artifacts, unrelated changes, accidental deletions, and stale comments.
    - Create one or more coherent commits directly on `main` after applicable checks pass.
    - Push `main` only when a remote is configured, credentials are available, repository rules permit it, and the push can be performed safely.
    - Report the actual commit hashes and push result.

## Subagent coordination contract

The principal agent must create a work ledger before delegation. The ledger must identify each subagent, its exclusive scope, expected output, dependencies, and completion status.

Every analysis subagent must return a concise structured report containing:

1. Scope inspected.
2. Files, classes, routes, tables, components, or workflows examined.
3. Confirmed findings, each with severity and evidence.
4. Suspected findings that require principal-agent validation.
5. Missing coverage or unavailable evidence.
6. Recommended implementation order.
7. Suggested tests and verification commands.
8. Risks of changing the affected area.

Subagents must not dump raw logs without interpretation. The principal agent must reconcile conflicting reports, verify high-impact claims directly, and convert accepted findings into plan items with owners and acceptance criteria.

## Required analysis subagents and detailed roles

Spawn at least the following specialized subagents. Give each one the exact mission, scope, checks, deliverable, and restrictions below.

### Tailwind Upgrade and Configuration Analyst

- **Mission:** Map the current Tailwind/PostCSS/Vite setup and design a safe Tailwind 4 migration.
- **Scope:** package metadata, lock file, Vite config, Tailwind config, PostCSS config, CSS entry points, plugins, presets, and build scripts.
- **Must inspect:**
  - Version blockers, v3-only directives/plugins, JavaScript config values, obsolete PostCSS dependencies, and duplicated CSS entry points.
  - Configuration still needed by third-party packages.
  - Build environment and Node compatibility.
- **Required checks:**
  - Map each old config value to CSS-first equivalent or documented retention.
  - Verify plugin compatibility.
  - Plan staged build checks.
- **Required deliverable:**
  - A configuration migration map.
  - Dependency changes.
  - Visual-risk and rollback notes.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Tailwind Source Detection and Dynamic Class Analyst

- **Mission:** Ensure every legitimate utility is discoverable and remove unsafe runtime class construction.
- **Scope:** Blade, PHP class maps, Livewire, JavaScript, vendor templates intentionally scanned, CSS `@source`, and safelists.
- **Must inspect:**
  - String interpolation such as `bg-${status}`, hidden paths, generated templates, overbroad safelists, and stale content globs.
  - Classes created in database or user content.
  - Build output missing critical states.
- **Required checks:**
  - Scan all source languages.
  - Map dynamic values to explicit complete-class dictionaries or custom utilities.
  - Test production build rather than dev-only JIT behavior.
- **Required deliverable:**
  - A source-detection registry.
  - Unsafe dynamic-class findings.
  - Required explicit maps and build tests.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Design Token and Theme Architect

- **Mission:** Create a coherent CSS-first token system without erasing intentional product branding.
- **Scope:** Colors, typography, spacing, breakpoints, containers, radius, shadows, z-index, motion, component variants, dark mode, and existing brand docs.
- **Must inspect:**
  - Repeated arbitrary values, inconsistent semantic colors, unreadable contrast, and duplicated component constants.
  - Tokens that should remain component-local.
  - Current brand values and theme behavior.
- **Required checks:**
  - Separate primitive, semantic, and component tokens.
  - Use modern color spaces only after contrast verification.
  - Define stable naming and deprecation rules.
- **Required deliverable:**
  - A token inventory and target `@theme` model.
  - Migration map from repeated values.
  - Contrast and theme test requirements.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Responsive Layout Analyst

- **Mission:** Audit every important layout for mobile-first behavior, container context, overflow, and translated-content resilience.
- **Scope:** Navigation, sidebars, cards, grids, tables, forms, filters, modals, drawers, toolbars, media, charts, pagination, and public/auth layouts.
- **Must inspect:**
  - Desktop-first overrides, horizontal overflow, fixed widths, hover-only interaction, broken tables, and clipping under long text.
  - Components that should use container queries.
  - Viewport unit and mobile browser issues.
- **Required checks:**
  - Test representative widths and zoom.
  - Use content-driven breakpoints and logical properties.
  - Check nested component behavior.
- **Required deliverable:**
  - A screen/component responsive matrix.
  - Prioritized defects.
  - Container-query and layout recommendations.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Tailwind Accessibility Styling Analyst

- **Mission:** Verify focus, contrast, reduced motion, forced colors, touch targets, state visibility, and non-color communication.
- **Scope:** Forms, buttons, links, badges, alerts, dialogs, menus, tables, loading states, transitions, themes, and disabled/error states.
- **Must inspect:**
  - Removed outlines, low contrast, color-only status, insufficient touch targets, motion without preference handling, and invisible forced-color states.
  - Hover styles without keyboard equivalents.
  - Dark mode regressions.
- **Required checks:**
  - Check state variants and semantic markup interaction.
  - Review focus-visible and high-contrast behavior.
  - Test reduced-motion variants.
- **Required deliverable:**
  - An accessibility-style findings map.
  - Required utility/token changes.
  - Verification scenarios.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Modern Tailwind Feature Applicability Analyst

- **Mission:** Evaluate current Tailwind 4 features against real UI needs.
- **Scope:** All reusable components and layouts plus exact installed Tailwind capabilities.
- **Must inspect:**
  - Container queries, logical properties, data/ARIA/has/not/group/peer variants, forced-colors, reduced-motion, pointer/hover capability, dynamic viewport units, scrollbar utilities, masks, text shadows, functional utilities, custom variants, theme-variable defaults, and view transitions.
  - Browser support and fallback needs.
  - Features likely to increase complexity without benefit.
- **Required checks:**
  - Approve only maintainable uses.
  - Document not-applicable reasons.
  - Require visual/accessibility tests for impactful changes.
- **Required deliverable:**
  - A Tailwind feature-applicability matrix.
  - Approved candidates and implementation locations.
  - Rejected candidates with reasons.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### CSS Duplication and Component Abstraction Analyst

- **Mission:** Reduce repeated classes and custom CSS through correct tokens, utilities, and Blade/Flux component boundaries.
- **Scope:** CSS files, Blade class lists, component variants, `@apply`, arbitrary values, custom selectors, and legacy preprocessor assets.
- **Must inspect:**
  - Repeated long class groups, global selectors fighting utilities, overuse of `@apply`, specificity problems, and dead CSS.
  - Patterns best expressed as components versus custom utilities.
  - Legacy Sass/Less dependencies.
- **Required checks:**
  - Measure repetition and ownership.
  - Avoid creating opaque semantic CSS for one-off markup.
  - Check purge/source behavior.
- **Required deliverable:**
  - A duplication/refactor plan.
  - Dead CSS candidates.
  - Component/token/utility decisions.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Frontend Build and Visual Verification Analyst

- **Mission:** Establish reliable build and visual checks for the Tailwind migration.
- **Scope:** Build scripts, output assets, source maps, browser tests, screenshot/visual tools if present, and critical pages.
- **Must inspect:**
  - Missing classes, build warnings, CSS size regressions, ordering conflicts, and theme inconsistencies.
  - Dev-only behavior that fails production builds.
  - Console errors.
- **Required checks:**
  - Build clean production assets.
  - Inspect critical screens at required widths/states/locales.
  - Compare output size and missing utilities.
- **Required deliverable:**
  - A build baseline/final report.
  - Critical visual checklist.
  - Regression tests and screenshots where tools exist.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

## Mandatory plan update before implementation

Update the canonical plan with a dedicated section for this task. At minimum include:

- Current Tailwind, Vite, PostCSS, CSS entry, plugin, and Node setup with target versions.
- Every old configuration value and its CSS-first migration or justified retention.
- Source-detection paths, dynamic class risks, explicit maps, and third-party sources.
- Primitive, semantic, and component token architecture plus migration of repeated arbitrary values.
- Responsive defects by screen/component and representative test widths.
- Accessibility styling defects and required focus/contrast/motion/forced-color controls.
- Approved/rejected modern Tailwind feature applications and browser considerations.
- Build, visual, browser, accessibility, and rollback verification for each migration pass.

Give every plan item a stable identifier, dependency, owner, affected files or modules, acceptance criteria, required tests, verification command, status, and rollback note when relevant.

Save the plan, then immediately begin implementation. Do not return a plan to the user and wait for approval.

## Domain-specific guardrails

- Do not delete `tailwind.config.*` or PostCSS configuration until every needed value/plugin/path is migrated or proven obsolete.
- Do not change deliberate product branding without a current requirement.
- Do not build utility class names through runtime string concatenation that Tailwind cannot discover.
- Do not use arbitrary values repeatedly; promote repeated decisions to tokens or maintainable utilities.
- Do not make `@apply` the default component abstraction and do not add Sass/Less solely for Tailwind.
- Do not use modern features merely to maximize feature count.

## Required implementation scope

Complete all applicable work below. Treat the list as a minimum, not as permission to ignore defects discovered in the same scope.

- Upgrade Tailwind CSS, `@tailwindcss/vite`, Vite integration, and compatible plugins through the established package manager.
- Migrate main stylesheets toward CSS-first configuration with `@import "tailwindcss"`, `@theme`, `@source`, `@custom-variant`, `@utility`, and `@variant` where supported and appropriate.
- Migrate every required color, font, spacing, breakpoint, container, radius, shadow, z-index, animation, variant, source, and plugin behavior before removing old config.
- Remove obsolete PostCSS import/autoprefixer/Tailwind wiring only when no other tool requires it.
- Build a documented design-token system for brand, neutral, success, warning, danger, info, surfaces, borders, text, focus, typography, spacing, containers, breakpoints, radius, shadow, z-index, duration, transition, and animation.
- Replace unsafe dynamic utility construction with explicit complete-class maps, custom utilities, variants, or source declarations.
- Refactor critical screens mobile-first and test navigation, sidebars, tables, grids, forms, filters, dialogs, media, charts, and pagination across representative widths.
- Use container queries, logical properties, ARIA/data/has/not/group/peer variants, reduced-motion, forced-colors, pointer/hover variants, and modern units/utilities only where approved.
- Fix focus visibility, contrast, color-only states, touch targets, theme states, and motion preferences.
- Remove dead CSS and repeated arbitrary values after proving behavior and source detection.
- Create the Tailwind feature matrix, design-token documentation, and build/visual/browser regression coverage.

## Required verification

Run the repository's real commands rather than inventing command names. Perform all safe applicable checks below and capture exact results:

- Frontend dependency audit and exact resolved Tailwind/Vite versions.
- Clean production build using the repository package manager.
- Source-detection test proving critical conditional/state classes exist in production output.
- Repository scan for unsafe dynamic class construction and repeated arbitrary values.
- Responsive checks at small mobile, large mobile, tablet portrait/landscape, small laptop, desktop, and wide desktop widths.
- Long translation, zoom, overflow, touch, keyboard, focus, reduced-motion, forced-colors, dark-mode, and high-contrast checks where applicable.
- CSS/JS asset size comparison and duplicate-entry inspection.
- Critical browser tests, visual snapshots/screenshots where tools exist, console inspection, full PHP/Livewire tests, formatter, and static analysis.

## Independent review protocol

After implementation and before committing:

1. Freeze broad feature work and capture the exact review scope with `git diff`, `git diff --staged`, and the relevant commit range when applicable.
2. Spawn every reviewer subagent listed below. Reviewer agents must be independent from implementation agents.
3. Run Codex `/review` against uncommitted changes when available.
4. Require reviewers to inspect behavior, not only style.
5. Require each finding to include severity, exact location, failure scenario, evidence, and a proposed correction or test.
6. Have the principal agent reproduce or validate each material finding.
7. Fix every valid in-scope finding.
8. Add a regression test for every behavior defect when practical.
9. Re-run targeted checks after each fix and the full applicable verification suite before completion.
10. Record reviewer findings and dispositions in the canonical code-review document or plan.

## Required independent reviewer subagents

### Tailwind Architecture Reviewer

- **Review mission:** Verify CSS-first configuration, token ownership, source detection, and removal of obsolete dependencies.
- **Scope:** Package/config/CSS files, class maps, old config, build output, and documentation.
- **Must inspect:**
  - Lost config values, undiscovered classes, overbroad safelists, token inconsistency, dead dependencies, and specificity conflicts.
  - Modern features used incorrectly.
  - Build behavior differing from development.
- **Required checks:**
  - Rebuild from clean dependencies.
  - Trace old-to-new configuration.
  - Inspect generated CSS for critical utilities.
- **Required deliverable:**
  - Architecture findings.
  - Required configuration/token corrections.
  - A production-build readiness verdict.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### Responsive UI Reviewer

- **Review mission:** Independently test layout behavior across widths, content lengths, zoom, and interaction modes.
- **Scope:** Critical pages/components, translations, media, tables, forms, navigation, dialogs, and browser tests.
- **Must inspect:**
  - Horizontal overflow, clipping, unusable tables/forms, hover-only controls, fixed heights, and breakpoint gaps.
  - Container query misuse.
  - Mobile browser viewport issues.
- **Required checks:**
  - Use representative data and long content.
  - Test touch and keyboard.
  - Check nested containers and orientation changes.
- **Required deliverable:**
  - Responsive defects.
  - Affected screens and widths.
  - Required layout fixes and tests.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### Accessibility Styling Reviewer

- **Review mission:** Verify focus, contrast, non-color status, motion, forced colors, touch targets, and theme behavior.
- **Scope:** Tokens, component states, forms, buttons, links, alerts, dialogs, transitions, and tests.
- **Must inspect:**
  - Invisible focus, insufficient contrast, disabled ambiguity, color-only information, excessive motion, and high-contrast failures.
  - Keyboard/hover mismatch.
  - Dark-theme regressions.
- **Required checks:**
  - Exercise state variants.
  - Inspect semantic HTML interaction.
  - Test reduced-motion and forced-colors where possible.
- **Required deliverable:**
  - Accessibility findings.
  - Required token/utility corrections.
  - Verified state checklist.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### Build Output Reviewer

- **Review mission:** Confirm production assets contain all required utilities and no unexpected duplication or severe size regression.
- **Scope:** Lock file, build config, output manifests/assets, source maps, and command logs.
- **Must inspect:**
  - Missing conditional classes, duplicated CSS/JS, stale assets, warnings, and cache-busting problems.
  - Package-manager inconsistency.
  - Unreported build errors.
- **Required checks:**
  - Run clean production build.
  - Inspect manifest and asset sizes.
  - Sample critical generated selectors.
- **Required deliverable:**
  - Build findings.
  - Before/after size summary.
  - Final release recommendation.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

## Required documentation synchronization

Update the canonical plan plus all affected first-party documentation. At minimum review and update, when present or applicable:

- The canonical plan, compliance matrix, and changelog.
- Tailwind, design-system, frontend, accessibility, localization, and testing documentation.
- A Tailwind feature-applicability matrix and source-detection registry.
- Token naming, component variants, responsive breakpoints, and browser-support decisions.
- Updated setup/build/deployment instructions.

Remove stale claims and commands. Keep useful historical changelog information. Ensure documentation describes the implemented and verified state, not the intended future state.

## Definition of done

This prompt is complete only when all applicable conditions are true:

- The repository uses the selected stable Tailwind CSS 4/Vite integration and produces a clean production build.
- Required configuration is migrated to CSS-first ownership or intentionally retained with documentation.
- Critical classes are discoverable; unsafe dynamic class construction is removed.
- A coherent token system replaces duplicated design decisions without arbitrary visual redesign.
- Critical layouts are mobile-first, overflow-safe, translation-resilient, keyboard/touch usable, and accessible across required widths/states.
- Modern Tailwind features are used only where justified and captured in the applicability matrix.
- Independent architecture, responsive, accessibility, and build reviewers are resolved, and changes are documented, committed, and safely pushed on `main`.

## Required final report

Return one factual report with these sections:

1. Branch and initial working-tree state.
2. Applicable instructions and canonical plan file.
3. Analysis subagents created, their scopes, and the important findings accepted or rejected.
4. Plan changes made before implementation.
5. Implementation summary by module and file group.
6. Database, security, performance, accessibility, localization, factory, seeder, and test impact where applicable.
7. Exact verification commands and observed results.
8. Independent reviewers created, findings, fixes, and remaining rejected findings with reasons.
9. Documentation synchronized.
10. Final Git status, commit hashes, and factual push result.
11. Remaining limitations, containing only genuine external or environmental blockers with exact affected requirement or plan identifiers.

Do not describe planned work as completed. Do not hide unfinished in-scope work in a limitations section.

## Start now

Read the applicable instruction chain and first-party documentation, inspect the repository, create the required analysis subagents, update the canonical plan, and then immediately implement the complete in-scope plan. Do not stop after analysis or planning.
