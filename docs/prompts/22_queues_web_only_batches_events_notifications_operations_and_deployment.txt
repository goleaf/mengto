# 22. Queues, Web-Only Batches, Events, Notifications, Operations, and Deployment

## Principal-agent role

You are the principal Codex implementation agent responsible for aligning long-running work, side effects, failure recovery, runtime services, and deployment with the repository’s real hosting constraints while preserving observable progress and idempotency.

## Mission

Determine which runtime services are actually permitted, then modernize jobs, queues, scheduler work, web-triggered batches, events, notifications, progress tracking, retries, recovery, health checks, and deployment. Required user functionality must not depend on unavailable workers, cron, supervisor, SSH, or runtime Artisan.

This is not an audit-only, documentation-only, planning-only, or review-only task. Discovery and planning are mandatory intermediate phases. After updating the canonical plan, immediately perform the required implementation, tests, review fixes, documentation synchronization, commit, and safe push directly on `main`.

## Repository-wide technology baseline

Preserve and enforce this target unless a stricter, current repository requirement applies:

- PHP 8.5.
- Laravel 13, using the latest stable mutually compatible 13.x release available at execution time.
- Livewire 4, using the latest stable mutually compatible 4.x release available at execution time.
- Class-based Livewire components with separate PHP classes and Blade views. Do not use Volt.
- Tailwind CSS 4, using the latest stable mutually compatible 4.x release available at execution time.
- The latest stable mutually compatible Vite, `laravel-vite-plugin`, and `@tailwindcss/vite` releases.
- Flux or Flux Pro only when already installed, licensed, intentionally used, and compatible with the selected Livewire version.
- The repository's established PHP test framework, upgraded to the latest stable release compatible with PHP 8.5 and Laravel 13.
- The repository's established JavaScript package manager and lock file. Never introduce a second lock file.

Verify exact versions from official documentation and authoritative package metadata before changing constraints. Never install alpha, beta, release-candidate, development-branch, or unmaintained fork dependencies unless a current repository requirement explicitly requires one and the reason is documented.

## Non-negotiable operating protocol

1. **Work only on the existing `main` branch.**
   - Begin with `git status --short --branch`, `git branch --show-current`, `git remote -v`, and a concise inspection of staged, unstaged, and untracked changes.
   - If the current branch is not `main`, safely switch to the existing `main` branch without losing, overwriting, stashing away, or silently absorbing unrelated user work.
   - Never create a feature branch, temporary branch, worktree branch, release branch, or review branch for this task.

2. **Protect the working tree.**
   - Preserve all unrelated existing changes.
   - Do not use `git reset --hard`, `git clean -fd`, `git checkout -- .`, force push, history rewriting, or any command that can discard work.
   - Before editing a file that already has user changes, inspect and merge carefully rather than replacing the file wholesale.
   - Commit only task-owned changes. Do not claim unrelated user changes as your work.

3. **Load governing instructions before work.**
   - Read the applicable `AGENTS.md` and `AGENTS.override.md` instruction chain from repository root to the current directory.
   - Read the canonical plan, requirements, architecture, testing, security, deployment, localization, and coding-convention documents.
   - Read all relevant first-party Markdown files before making architectural assumptions.
   - Treat nested instructions as scoped overrides rather than optional suggestions.

4. **Use specialized subagents for discovery.**
   - Spawn every required analysis subagent listed in this prompt.
   - Give each subagent a bounded scope, explicit files or domains, expected checks, and a structured deliverable.
   - Run independent scopes in parallel when safe.
   - Do not allow multiple subagents to edit the same files concurrently.
   - The principal agent remains the sole integrator and decision owner.

5. **Do not ask the user questions.**
   - Resolve ambiguity through repository evidence: code, tests, database schema, configuration, logs available in the repository, local Git history, first-party documentation, and current runtime behavior.
   - Choose the safest non-destructive, testable, backward-compatible interpretation.
   - Record material assumptions in the plan or an ADR and continue.
   - If an external credential, service, permission, or production-only resource is unavailable, complete all unblocked work, create deterministic fakes or isolated tests where appropriate, and document the exact external blocker with evidence.

6. **Quality is more important than speed.**
   - Work in small, reviewable, dependency-aware passes.
   - Inspect the diff after each meaningful pass.
   - Run targeted checks immediately after each change set.
   - Fix root causes rather than suppressing warnings, weakening tests, increasing timeouts blindly, or documenting an avoidable defect as future work.

7. **The plan is a mandatory gate, not the final deliverable.**
   - Locate the canonical execution plan. Prefer the existing canonical file; if none exists, create `PLANS.md` at repository root.
   - Do not create competing plan files.
   - Update the plan after discovery and before modifying production code.
   - Immediately begin implementation after saving the updated plan. Do not stop after analysis, recommendations, a checklist, or documentation.
   - Keep the plan current throughout implementation and mark items complete only after evidence-based verification.

8. **Implement, test, review, fix, and re-verify.**
   - Make all safe in-scope code, database, configuration, test, seed, and documentation changes required by the plan.
   - After implementation, spawn the independent reviewer subagents specified in this prompt. Reviewers must not be the same agents that implemented the change.
   - Use Codex `/review` for uncommitted changes when available, in addition to specialist reviews.
   - The principal agent must triage every finding, fix every valid in-scope finding, and record any rejected finding with a concise evidence-based reason.
   - Re-run all affected checks after fixes.

9. **Never fabricate evidence.**
   - Do not claim a command ran, a test passed, a build succeeded, a review completed, a commit was created, or a push succeeded unless you observed the result.
   - Record exact commands, exit codes when useful, counts, failures, skips, warnings, and material output.
   - Distinguish pre-existing failures from regressions introduced by this task.

10. **Finish documentation and Git work.**
    - Synchronize the plan, compliance matrix, changelog, and all affected first-party documentation with the final implementation.
    - Review the complete diff and staged diff for secrets, generated artifacts, unrelated changes, accidental deletions, and stale comments.
    - Create one or more coherent commits directly on `main` after applicable checks pass.
    - Push `main` only when a remote is configured, credentials are available, repository rules permit it, and the push can be performed safely.
    - Report the actual commit hashes and push result.

## Subagent coordination contract

The principal agent must create a work ledger before delegation. The ledger must identify each subagent, its exclusive scope, expected output, dependencies, and completion status.

Every analysis subagent must return a concise structured report containing:

1. Scope inspected.
2. Files, classes, routes, tables, components, or workflows examined.
3. Confirmed findings, each with severity and evidence.
4. Suspected findings that require principal-agent validation.
5. Missing coverage or unavailable evidence.
6. Recommended implementation order.
7. Suggested tests and verification commands.
8. Risks of changing the affected area.

Subagents must not dump raw logs without interpretation. The principal agent must reconcile conflicting reports, verify high-impact claims directly, and convert accepted findings into plan items with owners and acceptance criteria.

## Required analysis subagents and detailed roles

Spawn at least the following specialized subagents. Give each one the exact mission, scope, checks, deliverable, and restrictions below.

### Runtime Constraint and Hosting Analyst

- **Mission:** Establish the factual operational environment and hard constraints.
- **Scope:** AGENTS/requirements, deployment docs/scripts, hosting config, queues, scheduler, cron, supervisor, Redis, SQLite/database, storage, SSH, and health checks.
- **Must inspect:**
  - Services declared versus actually available, shared-hosting restrictions, runtime Artisan assumptions, and environment divergence.
  - Infrastructure dependencies hidden in user flows.
  - Production permission and timeout limits.
- **Required checks:**
  - Map local/test/staging/production capabilities.
  - Identify hard versus historical constraints.
  - Verify commands and service configuration.
- **Required deliverable:**
  - A runtime capability matrix.
  - Unavailable-service blockers.
  - Architecture implications and tests.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Queue and Job Architecture Analyst

- **Mission:** Audit jobs, batches, uniqueness, serialization, retries, backoff, timeouts, failures, and after-commit dispatch.
- **Scope:** Jobs, queue config, workers, Horizon if present, dispatch sites, payloads, failed jobs, events, and tests.
- **Must inspect:**
  - Full models/large payloads, pre-commit dispatch, non-idempotent retry, missing timeout/backoff, silent failure, duplicate jobs, and stale tenant/locale context.
  - Operational tools installed but unavailable.
  - Queue-specific database transactions.
- **Required checks:**
  - Classify each job as permitted, required, replaceable, or dead.
  - Trace retry/failure behavior.
  - Check serialization and deployment compatibility.
- **Required deliverable:**
  - A job inventory and disposition.
  - Required job fixes or web-batch replacements.
  - Queue tests and operations requirements.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Web-Only Batch and Progress Analyst

- **Mission:** Design resumable short HTTP/Livewire batches when background workers are forbidden.
- **Scope:** Imports, exports, cleanup, reports, notifications, media, migration backfills, progress models, locks, and Livewire UI.
- **Must inspect:**
  - Long blocking requests, no cursor/checkpoint, duplicate chunks, lost progress on refresh, unsafe retries, missing cancellation, and concurrent runs.
  - Progress stored only in browser.
  - Chunk size not bounded by server limits.
- **Required checks:**
  - Define persistent state machine, cursor, processed/total, checkpoints, errors, retry, resume, cancel, and lock semantics.
  - Ensure every request is idempotent and bounded.
  - Design UI and tests.
- **Required deliverable:**
  - A web-batch architecture.
  - Operation state schemas.
  - Failure/interruption/concurrency test matrix.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Event, Listener, and Side-Effect Analyst

- **Mission:** Make events factual, listeners focused, and side effects correctly ordered around commits.
- **Scope:** Events, listeners, observers, notifications, mail, webhooks, cache invalidation, jobs/web batches, and transactions.
- **Must inspect:**
  - Events used as hidden commands, recursive chains, multiple unrelated listener duties, pre-commit sends, duplicate notifications, and side-effect failures rolling back valid state incorrectly.
  - Locale/tenant context loss.
  - Unobservable failures.
- **Required checks:**
  - Trace event flow and transaction timing.
  - Classify synchronous versus deferred work.
  - Check idempotency and deduplication.
- **Required deliverable:**
  - A side-effect topology.
  - Refactor and ordering plan.
  - Required event/notification tests.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Notification and Communication Analyst

- **Mission:** Audit delivery rules, channels, recipient/locale selection, deduplication, retry, and failure behavior.
- **Scope:** Notifications, mailables, templates, database notifications, SMS/push providers if present, preferences, queues/web batches, and tests.
- **Must inspect:**
  - Wrong recipient or locale, duplicate sends, ignored preferences, secret leakage, retry storms, and delivery before commit.
  - Missing audit/status data.
  - User-visible functionality depending on unavailable worker.
- **Required checks:**
  - Map event-to-recipient/channel.
  - Check opt-outs/permissions.
  - Design idempotency and delivery status.
- **Required deliverable:**
  - A notification matrix.
  - Required code/config/template changes.
  - Delivery and failure tests.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Failure Recovery and Operational State Analyst

- **Mission:** Ensure every long or deferred operation can fail, retry, resume, cancel, and report status safely.
- **Scope:** Jobs, web batches, progress records, locks, retries, failed-job storage, integration errors, admin recovery UI, and logs.
- **Must inspect:**
  - Stuck “running” states, no retry limit, no checkpoint, duplicate work, lock orphaning, silent failure, and operator-only SSH recovery.
  - Cancellation leaving partial invalid state.
  - Error details exposing secrets.
- **Required checks:**
  - Inject interruption at each stage.
  - Define terminal and retryable states.
  - Provide web-visible recovery where required.
- **Required deliverable:**
  - A recovery state machine.
  - Required admin/user controls.
  - Failure/retry/resume tests.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Deployment and Rollback Analyst

- **Mission:** Make installation, build, migration, cache, service, storage, health, and rollback steps reproducible and safe.
- **Scope:** Deployment scripts, CI/CD, README/docs, Composer/npm scripts, env example, caches, migrations, seeders, queues/web batches, storage permissions, backups, and health endpoints.
- **Must inspect:**
  - `composer update` in production deploy, destructive migrations, automatic demo seeding, missing backup/rollback, unavailable commands, stale workers, and secret handling.
  - Config/route/view cache assumptions.
  - Health endpoint data leakage.
- **Required checks:**
  - Build canonical deploy sequence.
  - Define pre/post deploy smoke checks.
  - Account for mixed-version schema/code and worker restarts.
  - Verify rollback limitations.
- **Required deliverable:**
  - A deployment runbook.
  - Rollback and health-check plan.
  - Required script/document changes.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Operations Test and Smoke Architect

- **Mission:** Create automated and scripted verification for runtime model, long operations, notifications, deployment, and health.
- **Scope:** Tests, scripts, CI, progress fixtures, queue fakes/real test driver, browser flows, and deployment environment.
- **Must inspect:**
  - No retry/resume/cancel tests, fake-only queue tests hiding serialization, undocumented smoke commands, and health endpoints untested.
  - Deployment commands that cannot run from clean state.
  - Production-only assumptions.
- **Required checks:**
  - Design isolated job and web-batch tests.
  - Test repeated/interrupted execution.
  - Verify deployment caches and smoke paths.
- **Required deliverable:**
  - An operations verification matrix.
  - Exact safe commands.
  - Environment-specific limitations.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

## Mandatory plan update before implementation

Update the canonical plan with a dedicated section for this task. At minimum include:

- The local/test/staging/production runtime capability matrix for workers, queues, cron, scheduler, supervisor, Redis, SSH, runtime Artisan, database, storage, and timeouts.
- Every job, scheduled task, command, long HTTP request, event/listener, notification, and maintenance operation with disposition.
- Queue architecture where permitted, including payload, after-commit, retry, backoff, timeout, uniqueness, idempotency, tenant/locale context, and failed-job handling.
- Web-only batch architecture where workers are prohibited, including persistent state, cursor, checkpoints, progress, error, retry, resume, cancel, and locks.
- Side-effect ordering, notification deduplication/preferences/locale, and failure isolation.
- Deployment, migration, cache, seed, storage, health, backup, smoke, and rollback sequences.
- Schema/config/UI changes required for operation progress and recovery.
- Unit/feature/Livewire/browser/operations tests for every execution model.

Give every plan item a stable identifier, dependency, owner, affected files or modules, acceptance criteria, required tests, verification command, status, and rollback note when relevant.

Save the plan, then immediately begin implementation. Do not return a plan to the user and wait for approval.

## Domain-specific guardrails

- Do not make required user functionality depend on queue workers, cron, supervisor, SSH, or runtime Artisan when repository constraints prohibit them.
- Do not simulate a queue with one unbounded HTTP request.
- Do not dispatch side effects before the relevant database transaction commits when that can create inconsistent behavior.
- Do not put full model graphs, services, closures, or secrets in job/batch payloads.
- Do not use deployment commands that update dependencies nondeterministically or seed demo data automatically in production.
- Do not expose sensitive internal dependency details through public health endpoints.

## Required implementation scope

Complete all applicable work below. Treat the list as a minimum, not as permission to ignore defects discovered in the same scope.

- Establish and document the exact runtime capability matrix and reconcile contradictory old instructions.
- Inventory every job, queue dispatch, scheduled task, command, event, listener, observer, notification, long request, and operational recovery path.
- Where queues are allowed, make jobs small, serializable, idempotent, after-commit when required, bounded by timeout, retry/backoff-aware, tenant/locale-aware, observable, and covered by tests.
- Where queues/workers/cron are prohibited, replace required asynchronous behavior with short idempotent web-triggered batches that persist status, cursor, checkpoints, processed/total counts, errors, retries, resume, and safe cancellation.
- Use atomic locks and unique/idempotency controls to prevent duplicate concurrent runs and ensure refresh/retry does not duplicate work.
- Make events past-tense facts, listeners single-purpose, and notification/mail/webhook/cache side effects correctly ordered and deduplicated.
- Respect notification recipient permissions/preferences and preserve locale context through queue or web-batch execution.
- Provide user/admin-visible progress and recovery controls for long operations so SSH is not required for normal recovery.
- Create reproducible deployment scripts/runbooks for dependency install from locks, frontend build, environment validation, backups, safe migrations, cache builds, storage permissions, worker restart when applicable, health checks, and post-deploy smoke tests.
- Create rollback/forward-repair plans for schema and serialized job/session/cache compatibility.
- Add tests for job serialization/retry/backoff/timeout/failure/idempotency, web-batch interruption/resume/cancel/lock, event ordering, notification deduplication/locale, health authorization, and deployment command smoke paths.

## Required verification

Run the repository's real commands rather than inventing command names. Perform all safe applicable checks below and capture exact results:

- Runtime capability matrix validation against configuration and available services.
- Job tests for serialization, after-commit, retry/backoff, timeout, failure, uniqueness/idempotency, and tenant/locale context where queues are used.
- Web-batch tests for bounded chunks, persistent progress, refresh, interruption, resume, retry, cancellation, duplicate/concurrent requests, and lock expiry where workers are unavailable.
- Event/listener tests for fact naming, ordering, no recursion, after-commit side effects, and failure isolation.
- Notification/mail tests for recipient, preference, locale, deduplication, content, and failure behavior.
- Deployment dry-run or clean-environment install/build/cache/migration smoke steps where safe.
- Health endpoint access and non-disclosure tests.
- Full PHP suite, static analysis, formatter, migration/seed checks, frontend build, and critical progress UI browser tests.

## Independent review protocol

After implementation and before committing:

1. Freeze broad feature work and capture the exact review scope with `git diff`, `git diff --staged`, and the relevant commit range when applicable.
2. Spawn every reviewer subagent listed below. Reviewer agents must be independent from implementation agents.
3. Run Codex `/review` against uncommitted changes when available.
4. Require reviewers to inspect behavior, not only style.
5. Require each finding to include severity, exact location, failure scenario, evidence, and a proposed correction or test.
6. Have the principal agent reproduce or validate each material finding.
7. Fix every valid in-scope finding.
8. Add a regression test for every behavior defect when practical.
9. Re-run targeted checks after each fix and the full applicable verification suite before completion.
10. Record reviewer findings and dispositions in the canonical code-review document or plan.

## Required independent reviewer subagents

### Runtime Architecture Reviewer

- **Review mission:** Verify the selected queue or web-batch model matches actual hosting capabilities and does not hide unavailable infrastructure.
- **Scope:** Runtime matrix, jobs, web batches, config, deployment, and requirements.
- **Must inspect:**
  - Required feature depending on unavailable worker/cron/SSH, fake progress, unbounded HTTP work, and dead operational packages.
  - Environment drift and undocumented services.
  - Mixed execution models with duplicate side effects.
- **Required checks:**
  - Trace every long operation in each environment.
  - Inspect deployment/service setup.
  - Review fallback and failure behavior.
- **Required deliverable:**
  - Runtime findings.
  - Required architecture corrections.
  - Environment readiness verdict.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### Failure Recovery and Idempotency Reviewer

- **Review mission:** Stress interruption, retries, duplicate requests, lock expiry, cancellation, and side-effect ordering.
- **Scope:** Jobs/batches, progress state, locks, transactions, events, notifications, and tests.
- **Must inspect:**
  - Stuck states, duplicate processing/messages, skipped chunks, incorrect resume cursor, partial invalid data, and unreleased locks.
  - Cancellation reported successful while data is inconsistent.
  - Retry storms.
- **Required checks:**
  - Inject failures before/after checkpoints and commits.
  - Repeat operations concurrently.
  - Review terminal-state transitions.
- **Required deliverable:**
  - Recovery findings.
  - Required state/lock/idempotency fixes.
  - A resumability verdict.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### Deployment Safety Reviewer

- **Review mission:** Independently walk the deployment and rollback runbook from a clean or representative environment.
- **Scope:** Scripts, docs, CI/CD, env, locks, builds, migrations, seeders, caches, storage, workers, health, and smoke tests.
- **Must inspect:**
  - Nondeterministic dependency updates, destructive migration order, demo seeding, missing backup, stale workers, secret leakage, and unverified rollback.
  - Commands that do not exist or assume wrong paths.
  - Health checks giving false confidence.
- **Required checks:**
  - Validate commands.
  - Simulate pre/post deploy checks.
  - Review mixed-version and failure scenarios.
- **Required deliverable:**
  - Deployment findings.
  - Required script/runbook corrections.
  - Go/no-go checklist.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

## Required documentation synchronization

Update the canonical plan plus all affected first-party documentation. At minimum review and update, when present or applicable:

- The canonical plan, requirements, compliance matrix, and changelog.
- Operations, deployment, queue, web-batch, event, notification, testing, security, database, and recovery documentation.
- Runtime capability matrix and long-operation state diagrams.
- Canonical deploy, rollback, health, and smoke-test runbooks.
- AGENTS.md constraints for no-worker/no-cron/shared-hosting environments where applicable.

Remove stale claims and commands. Keep useful historical changelog information. Ensure documentation describes the implemented and verified state, not the intended future state.

## Definition of done

This prompt is complete only when all applicable conditions are true:

- Every long-running or deferred operation uses an execution model supported by the actual environment.
- Queue jobs, where allowed, are small, idempotent, bounded, after-commit where needed, observable, and tested.
- Web-only batches, where required, are bounded, persistent, resumable, retryable, cancel-safe, concurrency-safe, and usable without SSH.
- Events and listeners have clear factual semantics and side effects are ordered, deduplicated, localized, and failure-isolated.
- Deployment is reproducible from lock files, migrations and caches are safe, health/smoke checks are meaningful, and rollback/forward repair is documented.
- Independent runtime, recovery, and deployment reviews are resolved, and changes are documented, committed, and safely pushed on `main`.

## Required final report

Return one factual report with these sections:

1. Branch and initial working-tree state.
2. Applicable instructions and canonical plan file.
3. Analysis subagents created, their scopes, and the important findings accepted or rejected.
4. Plan changes made before implementation.
5. Implementation summary by module and file group.
6. Database, security, performance, accessibility, localization, factory, seeder, and test impact where applicable.
7. Exact verification commands and observed results.
8. Independent reviewers created, findings, fixes, and remaining rejected findings with reasons.
9. Documentation synchronized.
10. Final Git status, commit hashes, and factual push result.
11. Remaining limitations, containing only genuine external or environmental blockers with exact affected requirement or plan identifiers.

Do not describe planned work as completed. Do not hide unfinished in-scope work in a limitations section.

## Start now

Read the applicable instruction chain and first-party documentation, inspect the repository, create the required analysis subagents, update the canonical plan, and then immediately implement the complete in-scope plan. Do not stop after analysis or planning.
