# 18. Comprehensive PHP Test Suite for the Entire Application

## Principal-agent role

You are the principal Codex implementation agent responsible for creating meaningful automated PHP verification for every critical requirement, route, policy, Livewire action, domain transition, database invariant, integration boundary, file lifecycle, cache rule, and security control.

## Mission

Audit and rebuild the test strategy around observable behavior. Preserve the established primary PHP test framework, add comprehensive unit/feature/Livewire/architecture coverage, use browser tests only where needed, repair application defects exposed by tests, stabilize flaky tests, and produce honest coverage and quality evidence.

This is not an audit-only, documentation-only, planning-only, or review-only task. Discovery and planning are mandatory intermediate phases. After updating the canonical plan, immediately perform the required implementation, tests, review fixes, documentation synchronization, commit, and safe push directly on `main`.

## Repository-wide technology baseline

Preserve and enforce this target unless a stricter, current repository requirement applies:

- PHP 8.5.
- Laravel 13, using the latest stable mutually compatible 13.x release available at execution time.
- Livewire 4, using the latest stable mutually compatible 4.x release available at execution time.
- Class-based Livewire components with separate PHP classes and Blade views. Do not use Volt.
- Tailwind CSS 4, using the latest stable mutually compatible 4.x release available at execution time.
- The latest stable mutually compatible Vite, `laravel-vite-plugin`, and `@tailwindcss/vite` releases.
- Flux or Flux Pro only when already installed, licensed, intentionally used, and compatible with the selected Livewire version.
- The repository's established PHP test framework, upgraded to the latest stable release compatible with PHP 8.5 and Laravel 13.
- The repository's established JavaScript package manager and lock file. Never introduce a second lock file.

Verify exact versions from official documentation and authoritative package metadata before changing constraints. Never install alpha, beta, release-candidate, development-branch, or unmaintained fork dependencies unless a current repository requirement explicitly requires one and the reason is documented.

## Non-negotiable operating protocol

1. **Work only on the existing `main` branch.**
   - Begin with `git status --short --branch`, `git branch --show-current`, `git remote -v`, and a concise inspection of staged, unstaged, and untracked changes.
   - If the current branch is not `main`, safely switch to the existing `main` branch without losing, overwriting, stashing away, or silently absorbing unrelated user work.
   - Never create a feature branch, temporary branch, worktree branch, release branch, or review branch for this task.

2. **Protect the working tree.**
   - Preserve all unrelated existing changes.
   - Do not use `git reset --hard`, `git clean -fd`, `git checkout -- .`, force push, history rewriting, or any command that can discard work.
   - Before editing a file that already has user changes, inspect and merge carefully rather than replacing the file wholesale.
   - Commit only task-owned changes. Do not claim unrelated user changes as your work.

3. **Load governing instructions before work.**
   - Read the applicable `AGENTS.md` and `AGENTS.override.md` instruction chain from repository root to the current directory.
   - Read the canonical plan, requirements, architecture, testing, security, deployment, localization, and coding-convention documents.
   - Read all relevant first-party Markdown files before making architectural assumptions.
   - Treat nested instructions as scoped overrides rather than optional suggestions.

4. **Use specialized subagents for discovery.**
   - Spawn every required analysis subagent listed in this prompt.
   - Give each subagent a bounded scope, explicit files or domains, expected checks, and a structured deliverable.
   - Run independent scopes in parallel when safe.
   - Do not allow multiple subagents to edit the same files concurrently.
   - The principal agent remains the sole integrator and decision owner.

5. **Do not ask the user questions.**
   - Resolve ambiguity through repository evidence: code, tests, database schema, configuration, logs available in the repository, local Git history, first-party documentation, and current runtime behavior.
   - Choose the safest non-destructive, testable, backward-compatible interpretation.
   - Record material assumptions in the plan or an ADR and continue.
   - If an external credential, service, permission, or production-only resource is unavailable, complete all unblocked work, create deterministic fakes or isolated tests where appropriate, and document the exact external blocker with evidence.

6. **Quality is more important than speed.**
   - Work in small, reviewable, dependency-aware passes.
   - Inspect the diff after each meaningful pass.
   - Run targeted checks immediately after each change set.
   - Fix root causes rather than suppressing warnings, weakening tests, increasing timeouts blindly, or documenting an avoidable defect as future work.

7. **The plan is a mandatory gate, not the final deliverable.**
   - Locate the canonical execution plan. Prefer the existing canonical file; if none exists, create `PLANS.md` at repository root.
   - Do not create competing plan files.
   - Update the plan after discovery and before modifying production code.
   - Immediately begin implementation after saving the updated plan. Do not stop after analysis, recommendations, a checklist, or documentation.
   - Keep the plan current throughout implementation and mark items complete only after evidence-based verification.

8. **Implement, test, review, fix, and re-verify.**
   - Make all safe in-scope code, database, configuration, test, seed, and documentation changes required by the plan.
   - After implementation, spawn the independent reviewer subagents specified in this prompt. Reviewers must not be the same agents that implemented the change.
   - Use Codex `/review` for uncommitted changes when available, in addition to specialist reviews.
   - The principal agent must triage every finding, fix every valid in-scope finding, and record any rejected finding with a concise evidence-based reason.
   - Re-run all affected checks after fixes.

9. **Never fabricate evidence.**
   - Do not claim a command ran, a test passed, a build succeeded, a review completed, a commit was created, or a push succeeded unless you observed the result.
   - Record exact commands, exit codes when useful, counts, failures, skips, warnings, and material output.
   - Distinguish pre-existing failures from regressions introduced by this task.

10. **Finish documentation and Git work.**
    - Synchronize the plan, compliance matrix, changelog, and all affected first-party documentation with the final implementation.
    - Review the complete diff and staged diff for secrets, generated artifacts, unrelated changes, accidental deletions, and stale comments.
    - Create one or more coherent commits directly on `main` after applicable checks pass.
    - Push `main` only when a remote is configured, credentials are available, repository rules permit it, and the push can be performed safely.
    - Report the actual commit hashes and push result.

## Subagent coordination contract

The principal agent must create a work ledger before delegation. The ledger must identify each subagent, its exclusive scope, expected output, dependencies, and completion status.

Every analysis subagent must return a concise structured report containing:

1. Scope inspected.
2. Files, classes, routes, tables, components, or workflows examined.
3. Confirmed findings, each with severity and evidence.
4. Suspected findings that require principal-agent validation.
5. Missing coverage or unavailable evidence.
6. Recommended implementation order.
7. Suggested tests and verification commands.
8. Risks of changing the affected area.

Subagents must not dump raw logs without interpretation. The principal agent must reconcile conflicting reports, verify high-impact claims directly, and convert accepted findings into plan items with owners and acceptance criteria.

## Required analysis subagents and detailed roles

Spawn at least the following specialized subagents. Give each one the exact mission, scope, checks, deliverable, and restrictions below.

### Authentication Test Specialist

- **Mission:** Design exhaustive tests for account, session, token, invitation, reset, verification, MFA, and abuse-control behavior.
- **Scope:** Authentication requirements/code, sessions, cookies, tokens, notifications, rate limits, factories, seeders, and existing tests.
- **Must inspect:**
  - Happy paths, invalid credentials, blocked/inactive states, session rotation, logout invalidation, expiry, replay, concurrency, wrong recipient, and production demo safeguards.
  - Tests that bypass the real guard/middleware path.
  - Time-dependent flakiness.
- **Required checks:**
  - Build a state/action matrix.
  - Use time travel and notification fakes.
  - Test direct HTTP/Livewire invocation.
- **Required deliverable:**
  - A complete auth test plan.
  - Missing fixtures/states.
  - High-risk untested behavior.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Authorization and Tenant Test Specialist

- **Mission:** Create matrices for policies, gates, roles, owners, memberships, privacy, state, and cross-tenant isolation.
- **Scope:** Policies, routes, Livewire, APIs, files, caches, commands, factories, seeders, and existing tests.
- **Must inspect:**
  - Allowed and denied paths, direct calls, list leakage, wrong tenant, private/deleted/archived states, and super-admin behavior.
  - Tests that only assert hidden UI.
  - Missing absence-of-data assertions.
- **Required checks:**
  - Use data providers by role/action/state.
  - Test both response and no leaked persistence/data.
  - Swap identifiers deliberately.
- **Required deliverable:**
  - An authorization test matrix.
  - Required factory graphs.
  - Coverage gaps by severity.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Domain, Database, and State-Machine Test Specialist

- **Mission:** Test pure domain rules, state transitions, transactions, constraints, migrations, casts, scopes, and concurrency.
- **Scope:** Actions, services, models, enums, migrations, schema, transactions, locks, factories, and current tests.
- **Must inspect:**
  - Missing invalid-transition cases, rollback, unique/check/foreign constraints, money/date rules, soft deletes, and race conditions.
  - Tests using mocked persistence instead of real invariants.
  - Engine-specific behavior.
- **Required checks:**
  - Separate unit and database feature tests.
  - Design concurrency/idempotency simulations.
  - Verify fresh and upgrade migrations.
- **Required deliverable:**
  - A domain/database test catalog.
  - Required fixtures and migration states.
  - Critical branch coverage targets.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Livewire Test Specialist

- **Mission:** Cover every important component rendering, state, form, action, event, pagination, URL/session, upload, sort, and security behavior.
- **Scope:** Livewire components/forms/views, policies, queries, JavaScript integrations, and tests.
- **Must inspect:**
  - Mount-only authorization, direct action forgery, public-state trust, missing validation, pagination/filter bugs, events, uploads, query counts, and empty/error states.
  - Markup-only assertions without behavioral checks.
  - Features requiring browser-level testing.
- **Required checks:**
  - Inventory public methods/properties.
  - Use component test APIs and direct calls.
  - Add query-budget checks.
- **Required deliverable:**
  - A component/action test matrix.
  - Required PHP tests.
  - Browser-only gaps.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### API and Integration Test Specialist

- **Mission:** Test API contracts, external clients, webhooks, payments, retries, timeouts, and idempotency without real network calls.
- **Scope:** API routes/resources/errors, HTTP clients, provider DTOs, webhooks, payments, fakes, fixtures, and tests.
- **Must inspect:**
  - Success/error structures, field exposure, auth, malformed JSON, timeout, 4xx/5xx, rate limits, invalid response, retry safety, signatures, replay, duplicates, amount/currency/owner mismatch, and secret redaction.
  - Stray external calls.
  - Tests coupled to vendor implementation.
- **Required checks:**
  - Define provider fakes for every outcome.
  - Assert idempotent persistence and side effects.
  - Prevent network access globally in tests where practical.
- **Required deliverable:**
  - An API/integration test matrix.
  - Fixture registry.
  - Missing contract and failure coverage.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Files, Cache, and Performance Test Specialist

- **Mission:** Cover upload/download lifecycle, image processing, private access, cache scope/TTL/invalidation, locks, query budgets, and bounded processing.
- **Scope:** Filesystem code, validation, media services, caches, Redis/Memcached abstractions, imports/exports, query-heavy pages, and tests.
- **Must inspect:**
  - Spoofed files, path traversal, unauthorized download, cleanup, thumbnails, cache leaks, stale keys, lock timeout, stampede, N+1, memory, and pagination.
  - Tests using real storage or services.
  - Performance assertions too fragile or too lax.
- **Required checks:**
  - Use fake disks and deterministic fixtures.
  - Design cache clock control and tenant/user/locale separation tests.
  - Set stable query budgets.
- **Required deliverable:**
  - A file/cache/performance test plan.
  - Required fixtures and instrumentation.
  - Regression thresholds.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Localization and Accessibility Test Specialist

- **Mission:** Cover locale parity, formatting, critical rendering, accessibility semantics, keyboard/focus behavior, and responsive browser paths.
- **Scope:** Locale files, formatters, pages, forms, notifications, browser tests, accessibility tooling, and seed data.
- **Must inspect:**
  - Missing locale rendering, raw keys, placeholder mismatch, wrong recipient locale, labels/errors/focus, and long-text overflow.
  - Tests relying solely on snapshots or automated scores.
  - RTL and reduced-motion gaps.
- **Required checks:**
  - Build deterministic locale/timezone fixtures.
  - Identify browser-only keyboard/focus cases.
  - Use long/Unicode data.
- **Required deliverable:**
  - A localization/accessibility coverage matrix.
  - Required PHP/browser tests.
  - Manual verification notes.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Architecture Test Specialist

- **Mission:** Turn durable repository rules into automated, low-noise checks.
- **Scope:** AGENTS.md, source tree, Blade, config, package metadata, namespaces, tests, and static-analysis tools.
- **Must inspect:**
  - No Volt, no @php/@endphp, no env outside config, no direct DB/model/service calls in Blade, no debug calls, no unsafe dynamic Tailwind classes, no duplicate Alpine, no production demo accounts, and no real network in tests.
  - Dependency-direction and naming rules where enforceable.
  - False-positive/false-negative risk.
- **Required checks:**
  - Prefer AST/reflection/tool APIs over brittle grep where practical.
  - Document exceptions narrowly.
  - Add tests that demonstrate each rule fails correctly.
- **Required deliverable:**
  - An architecture-test suite design.
  - Implemented rules and exclusions.
  - Maintenance guidance.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Coverage, Flakiness, and Test-Quality Analyst

- **Mission:** Measure meaningful coverage, identify false confidence, stabilize tests, and prevent test-suite erosion.
- **Scope:** Coverage reports/config, skipped/incomplete/risky tests, random/time/file/network dependencies, parallel tests, CI, and assertions.
- **Must inspect:**
  - Assertion-free tests, implementation-detail tests, excessive mocking, broad exclusions, changed expectations hiding regressions, nondeterminism, and order dependence.
  - Critical branches lacking coverage.
  - Coverage percentage without meaningful risk coverage.
- **Required checks:**
  - Compare baseline/final counts.
  - Run repeated/parallel tests where safe.
  - Inspect skipped and removed tests.
  - Prioritize critical branches over cosmetic percentage.
- **Required deliverable:**
  - A quality and flakiness report.
  - Coverage gap list by risk.
  - Required stabilization and threshold policy.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

## Mandatory plan update before implementation

Update the canonical plan with a dedicated section for this task. At minimum include:

- The current test framework, configuration, commands, counts, failures, skips, coverage, and CI behavior.
- A requirement-to-test traceability map across authentication, authorization, domain, Livewire, database, API, integrations, files, cache, localization, accessibility, and security.
- Critical untested branches, workflows, negative paths, race conditions, and migration/seed behavior.
- Factory, state, fixture, fake, and seeder work required to support deterministic tests.
- Architecture rules to automate and intentional narrow exceptions.
- Flaky, slow, implementation-coupled, redundant, or misleading tests to repair or replace.
- The test implementation sequence and targeted checks after each application fix.
- Meaningful coverage goals and explicit high-risk branch requirements.

Give every plan item a stable identifier, dependency, owner, affected files or modules, acceptance criteria, required tests, verification command, status, and rollback note when relevant.

Save the plan, then immediately begin implementation. Do not return a plan to the user and wait for approval.

## Domain-specific guardrails

- Do not install a second primary PHP test framework without a documented migration benefit.
- Do not increase coverage through assertion-free, duplicate, trivial getter, or implementation-detail tests.
- Do not modify expectations merely to accept a regression; fix the application or update a genuinely changed requirement with evidence.
- Do not make real external network calls or use non-test databases/filesystems in automated tests.
- Do not broadly exclude difficult first-party code from coverage or silence risky/skipped tests.
- Do not require browser tests for behavior that can be tested faster and more reliably in PHP.

## Required implementation scope

Complete all applicable work below. Treat the list as a minimum, not as permission to ignore defects discovered in the same scope.

- Capture the exact baseline test counts, failures, skips, risky/incomplete tests, coverage, duration, and parallel behavior.
- Preserve and upgrade the established PHPUnit or Pest framework and update deprecated configuration/APIs without weakening behavior.
- Create requirement-linked unit tests for pure domain logic and feature tests for Laravel-integrated behavior.
- Create comprehensive Livewire tests for rendering, mount, public state, locked properties, forms, validation, authorization, actions, events, pagination, URL/session state, uploads, sorting, redirects, query counts, and forged calls.
- Test all routes, policies, gates, roles, ownership, privacy, tenant isolation, private files, commands/web maintenance endpoints, and administrative operations.
- Test migrations, upgrade paths, constraints, models, casts, enums, scopes, soft deletes, transactions, concurrency, idempotency, factories, states, and seeders.
- Test API contracts, errors, pagination, resources, external failures, retries, signatures, webhooks, payments, and secret redaction with deterministic fakes and no network.
- Test file validation/lifecycle, cache miss/hit/scope/TTL/invalidation/locks, query budgets, pagination, and bounded batch processing.
- Test every supported locale, formatting, validation, mail/notifications, public metadata, long/Unicode content, and critical accessibility/browser journeys.
- Implement architecture tests for durable repository prohibitions and dependency boundaries where reliable.
- Repair application defects exposed by tests and add a regression test for each defect before marking it resolved.
- Stabilize time, random data, filesystem, event, notification, queue/web-batch, and parallel-test behavior.
- Run full suite, parallel suite where safe, coverage, static analysis, formatter, migration/seed, frontend build, and browser tests before completion.

## Required verification

Run the repository's real commands rather than inventing command names. Perform all safe applicable checks below and capture exact results:

- Exact baseline and final test, assertion, pass, fail, skip, risky/incomplete, duration, and coverage counts.
- Targeted suites after every implementation group.
- Complete PHP suite and parallel run where safe.
- Coverage report with critical-branch review, not only total percentage.
- Architecture tests and proof that representative forbidden fixtures fail.
- Fresh migration, representative upgrade migration, DatabaseSeeder, fixed-seeder idempotency, and factory/state inventories.
- No-real-network enforcement and fake filesystem/cache/notification/mail/event behavior.
- Repeated runs for flaky tests and order-dependence detection where practical.
- Frontend build and browser tests for browser-only Livewire, accessibility, navigation, and responsive behavior.
- Final CI command parity check.

## Independent review protocol

After implementation and before committing:

1. Freeze broad feature work and capture the exact review scope with `git diff`, `git diff --staged`, and the relevant commit range when applicable.
2. Spawn every reviewer subagent listed below. Reviewer agents must be independent from implementation agents.
3. Run Codex `/review` against uncommitted changes when available.
4. Require reviewers to inspect behavior, not only style.
5. Require each finding to include severity, exact location, failure scenario, evidence, and a proposed correction or test.
6. Have the principal agent reproduce or validate each material finding.
7. Fix every valid in-scope finding.
8. Add a regression test for every behavior defect when practical.
9. Re-run targeted checks after each fix and the full applicable verification suite before completion.
10. Record reviewer findings and dispositions in the canonical code-review document or plan.

## Required independent reviewer subagents

### Test Quality Reviewer

- **Review mission:** Inspect whether tests prove user/domain behavior with meaningful assertions and realistic boundaries.
- **Scope:** New/changed tests, fixtures, mocks, application fixes, coverage, and requirements.
- **Must inspect:**
  - Trivial assertions, overmocking, internal implementation coupling, duplicate tests, and missing side-effect assertions.
  - Tests that pass when behavior is broken.
  - Application changes made only to satisfy tests.
- **Required checks:**
  - Review representative tests in every domain.
  - Mutation-think through assertions.
  - Compare to requirements and public contracts.
- **Required deliverable:**
  - Quality findings.
  - Tests to strengthen/remove/replace.
  - A behavioral confidence verdict.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### Missing Scenario Reviewer

- **Review mission:** Find important positive, negative, boundary, state, concurrency, and failure scenarios absent from the suite.
- **Scope:** Requirements, routes, policies, Livewire actions, state machines, integrations, files, cache, and tests.
- **Must inspect:**
  - Wrong role/tenant/owner, expiry/replay, invalid transitions, rollback, malformed provider data, file attacks, and degraded infrastructure.
  - Critical pages or commands not exercised.
  - Uncovered locale/accessibility states.
- **Required checks:**
  - Trace each critical workflow.
  - Compare branch logic to tests.
  - Use compliance matrix gaps.
- **Required deliverable:**
  - A missing-scenario backlog by severity.
  - Required tests before completion.
  - Affected requirement IDs.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### Flakiness and False-Positive Reviewer

- **Review mission:** Stress test determinism, isolation, parallelism, time, randomness, file paths, and asynchronous behavior.
- **Scope:** Test configuration, factories, seeders, fakes, clocks, queues/batches, browser waits, and parallel setup.
- **Must inspect:**
  - Order dependence, shared state, sleeps, random collisions, real services, leftover files, and overly broad exception catches.
  - Skipped failures or retries hiding flakes.
  - Environment-specific assumptions.
- **Required checks:**
  - Repeat and reorder tests.
  - Run parallel where safe.
  - Inspect cleanup and time control.
- **Required deliverable:**
  - Flakiness findings.
  - Required stabilization.
  - Repeat-run evidence.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### Coverage Integrity Reviewer

- **Review mission:** Verify coverage numbers reflect meaningful application logic and no difficult code was hidden.
- **Scope:** Coverage config/report, exclusions, critical modules, branch paths, removed tests, and compliance matrix.
- **Must inspect:**
  - Broad exclusions, unexecuted critical branches, getter-heavy inflation, and percentage drops masked by config changes.
  - Critical security/data logic lacking branch coverage.
  - Reported numbers not reproducible.
- **Required checks:**
  - Inspect coverage by file/branch.
  - Compare baseline config.
  - Sample uncovered high-risk lines.
- **Required deliverable:**
  - Coverage integrity findings.
  - Required tests or exclusion reversals.
  - Final coverage statement.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

## Required documentation synchronization

Update the canonical plan plus all affected first-party documentation. At minimum review and update, when present or applicable:

- The canonical plan, requirements, compliance matrix, and changelog.
- Testing strategy, test commands, fixtures, factories, seeders, architecture tests, browser tests, coverage, and CI documentation.
- A requirement-to-test traceability matrix.
- Documented narrow exclusions and environment limitations.
- AGENTS.md definition-of-done and regression-test requirements.

Remove stale claims and commands. Keep useful historical changelog information. Ensure documentation describes the implemented and verified state, not the intended future state.

## Definition of done

This prompt is complete only when all applicable conditions are true:

- Every critical requirement and public mutation has meaningful automated PHP coverage at the appropriate layer.
- Authentication, authorization, tenancy, tokens, state transitions, data integrity, API, integrations, files, cache, localization, and security have positive and adversarial tests.
- Livewire components have direct action/state/query coverage and browser tests only for browser-dependent behavior.
- Factories, states, seeders, migrations, and architecture rules are automatically verified.
- The suite makes no real network calls, does not touch non-test data, and is deterministic and parallel-safe where supported.
- Coverage is honest, critical branches are covered, and no broad exclusion or weak assertion inflates confidence.
- Independent quality, missing-scenario, flakiness, and coverage reviewers are resolved, and changes are documented, committed, and safely pushed on `main`.

## Required final report

Return one factual report with these sections:

1. Branch and initial working-tree state.
2. Applicable instructions and canonical plan file.
3. Analysis subagents created, their scopes, and the important findings accepted or rejected.
4. Plan changes made before implementation.
5. Implementation summary by module and file group.
6. Database, security, performance, accessibility, localization, factory, seeder, and test impact where applicable.
7. Exact verification commands and observed results.
8. Independent reviewers created, findings, fixes, and remaining rejected findings with reasons.
9. Documentation synchronized.
10. Final Git status, commit hashes, and factual push result.
11. Remaining limitations, containing only genuine external or environmental blockers with exact affected requirement or plan identifiers.

Do not describe planned work as completed. Do not hide unfinished in-scope work in a limitations section.

## Start now

Read the applicable instruction chain and first-party documentation, inspect the repository, create the required analysis subagents, update the canonical plan, and then immediately implement the complete in-scope plan. Do not stop after analysis or planning.
