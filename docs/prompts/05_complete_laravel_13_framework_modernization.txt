# 05. Complete Laravel 13 Framework Modernization

## Principal-agent role

You are the principal Codex implementation agent responsible for aligning the application with Laravel 13 architecture, defaults, security controls, bootstrap conventions, and first-party package behavior without erasing project-specific configuration.

## Mission

Compare the existing application with a clean, current Laravel 13 structure, migrate outdated integration points, retain all intentional custom behavior, apply useful Laravel 13 capabilities, and prove framework boot, routing, middleware, authentication, sessions, cache, queues, filesystem, mail, logging, tests, and deployment behavior.

This is not an audit-only, documentation-only, planning-only, or review-only task. Discovery and planning are mandatory intermediate phases. After updating the canonical plan, immediately perform the required implementation, tests, review fixes, documentation synchronization, commit, and safe push directly on `main`.

## Repository-wide technology baseline

Preserve and enforce this target unless a stricter, current repository requirement applies:

- PHP 8.5.
- Laravel 13, using the latest stable mutually compatible 13.x release available at execution time.
- Livewire 4, using the latest stable mutually compatible 4.x release available at execution time.
- Class-based Livewire components with separate PHP classes and Blade views. Do not use Volt.
- Tailwind CSS 4, using the latest stable mutually compatible 4.x release available at execution time.
- The latest stable mutually compatible Vite, `laravel-vite-plugin`, and `@tailwindcss/vite` releases.
- Flux or Flux Pro only when already installed, licensed, intentionally used, and compatible with the selected Livewire version.
- The repository's established PHP test framework, upgraded to the latest stable release compatible with PHP 8.5 and Laravel 13.
- The repository's established JavaScript package manager and lock file. Never introduce a second lock file.

Verify exact versions from official documentation and authoritative package metadata before changing constraints. Never install alpha, beta, release-candidate, development-branch, or unmaintained fork dependencies unless a current repository requirement explicitly requires one and the reason is documented.

## Non-negotiable operating protocol

1. **Work only on the existing `main` branch.**
   - Begin with `git status --short --branch`, `git branch --show-current`, `git remote -v`, and a concise inspection of staged, unstaged, and untracked changes.
   - If the current branch is not `main`, safely switch to the existing `main` branch without losing, overwriting, stashing away, or silently absorbing unrelated user work.
   - Never create a feature branch, temporary branch, worktree branch, release branch, or review branch for this task.

2. **Protect the working tree.**
   - Preserve all unrelated existing changes.
   - Do not use `git reset --hard`, `git clean -fd`, `git checkout -- .`, force push, history rewriting, or any command that can discard work.
   - Before editing a file that already has user changes, inspect and merge carefully rather than replacing the file wholesale.
   - Commit only task-owned changes. Do not claim unrelated user changes as your work.

3. **Load governing instructions before work.**
   - Read the applicable `AGENTS.md` and `AGENTS.override.md` instruction chain from repository root to the current directory.
   - Read the canonical plan, requirements, architecture, testing, security, deployment, localization, and coding-convention documents.
   - Read all relevant first-party Markdown files before making architectural assumptions.
   - Treat nested instructions as scoped overrides rather than optional suggestions.

4. **Use specialized subagents for discovery.**
   - Spawn every required analysis subagent listed in this prompt.
   - Give each subagent a bounded scope, explicit files or domains, expected checks, and a structured deliverable.
   - Run independent scopes in parallel when safe.
   - Do not allow multiple subagents to edit the same files concurrently.
   - The principal agent remains the sole integrator and decision owner.

5. **Do not ask the user questions.**
   - Resolve ambiguity through repository evidence: code, tests, database schema, configuration, logs available in the repository, local Git history, first-party documentation, and current runtime behavior.
   - Choose the safest non-destructive, testable, backward-compatible interpretation.
   - Record material assumptions in the plan or an ADR and continue.
   - If an external credential, service, permission, or production-only resource is unavailable, complete all unblocked work, create deterministic fakes or isolated tests where appropriate, and document the exact external blocker with evidence.

6. **Quality is more important than speed.**
   - Work in small, reviewable, dependency-aware passes.
   - Inspect the diff after each meaningful pass.
   - Run targeted checks immediately after each change set.
   - Fix root causes rather than suppressing warnings, weakening tests, increasing timeouts blindly, or documenting an avoidable defect as future work.

7. **The plan is a mandatory gate, not the final deliverable.**
   - Locate the canonical execution plan. Prefer the existing canonical file; if none exists, create `PLANS.md` at repository root.
   - Do not create competing plan files.
   - Update the plan after discovery and before modifying production code.
   - Immediately begin implementation after saving the updated plan. Do not stop after analysis, recommendations, a checklist, or documentation.
   - Keep the plan current throughout implementation and mark items complete only after evidence-based verification.

8. **Implement, test, review, fix, and re-verify.**
   - Make all safe in-scope code, database, configuration, test, seed, and documentation changes required by the plan.
   - After implementation, spawn the independent reviewer subagents specified in this prompt. Reviewers must not be the same agents that implemented the change.
   - Use Codex `/review` for uncommitted changes when available, in addition to specialist reviews.
   - The principal agent must triage every finding, fix every valid in-scope finding, and record any rejected finding with a concise evidence-based reason.
   - Re-run all affected checks after fixes.

9. **Never fabricate evidence.**
   - Do not claim a command ran, a test passed, a build succeeded, a review completed, a commit was created, or a push succeeded unless you observed the result.
   - Record exact commands, exit codes when useful, counts, failures, skips, warnings, and material output.
   - Distinguish pre-existing failures from regressions introduced by this task.

10. **Finish documentation and Git work.**
    - Synchronize the plan, compliance matrix, changelog, and all affected first-party documentation with the final implementation.
    - Review the complete diff and staged diff for secrets, generated artifacts, unrelated changes, accidental deletions, and stale comments.
    - Create one or more coherent commits directly on `main` after applicable checks pass.
    - Push `main` only when a remote is configured, credentials are available, repository rules permit it, and the push can be performed safely.
    - Report the actual commit hashes and push result.

## Subagent coordination contract

The principal agent must create a work ledger before delegation. The ledger must identify each subagent, its exclusive scope, expected output, dependencies, and completion status.

Every analysis subagent must return a concise structured report containing:

1. Scope inspected.
2. Files, classes, routes, tables, components, or workflows examined.
3. Confirmed findings, each with severity and evidence.
4. Suspected findings that require principal-agent validation.
5. Missing coverage or unavailable evidence.
6. Recommended implementation order.
7. Suggested tests and verification commands.
8. Risks of changing the affected area.

Subagents must not dump raw logs without interpretation. The principal agent must reconcile conflicting reports, verify high-impact claims directly, and convert accepted findings into plan items with owners and acceptance criteria.

## Required analysis subagents and detailed roles

Spawn at least the following specialized subagents. Give each one the exact mission, scope, checks, deliverable, and restrictions below.

### Laravel Bootstrap and Configuration Analyst

- **Mission:** Compare current bootstrap and configuration against Laravel 13 and identify safe migrations.
- **Scope:** bootstrap/app.php, bootstrap/providers.php, config files, public entry point, console entry point, environment example, and package discovery.
- **Must inspect:**
  - Obsolete bootstrap APIs, missing providers, stale config keys, copied defaults that diverge unexpectedly, and env usage outside config.
  - Project-specific behavior that must be preserved.
  - Config files no longer used or missing for installed packages.
- **Required checks:**
  - Diff against a clean Laravel 13 skeleton conceptually, not by blind replacement.
  - Trace each custom bootstrap callback.
  - Check configuration caching compatibility.
- **Required deliverable:**
  - A bootstrap/config migration map.
  - Preserve/replace/remove decisions for each custom block.
  - Required boot and cache tests.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Routing and Middleware Analyst

- **Mission:** Audit route registration, middleware aliases/groups, bindings, rate limits, trusted hosts/proxies, CSRF, origin, and health routes.
- **Scope:** web/API/console routes, bootstrap middleware config, middleware classes, route service logic, rate limiters, and bindings.
- **Must inspect:**
  - Business logic in routes, obsolete middleware registration, route cache blockers, missing authorization, and unsafe exclusions.
  - CSRF/origin protections weakened by broad exceptions.
  - Binding that can cross tenant boundaries.
- **Required checks:**
  - Trace representative routes through middleware.
  - Check route naming and caching.
  - Verify health routes reveal no sensitive data.
- **Required deliverable:**
  - A route/middleware findings map.
  - Required Laravel 13 registration changes.
  - Security and regression tests.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Service Container and Provider Analyst

- **Mission:** Modernize providers, bindings, package discovery, boot logic, events, observers, and macros.
- **Scope:** App service providers, domain providers, container bindings, facade extensions, event registration, observers, and package providers.
- **Must inspect:**
  - Service locator use, eager side effects during boot, invalid singleton lifetimes, stale provider references, and duplicate registration.
  - Bindings that should be contextual or interface-based.
  - Boot logic that breaks config cache or tests.
- **Required checks:**
  - Verify resolution paths.
  - Identify circular dependencies.
  - Check provider side effects under CLI and tests.
- **Required deliverable:**
  - A provider/binding map.
  - Required lifecycle fixes.
  - Container-resolution tests.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Exception, Logging, and Response Analyst

- **Mission:** Align exception handling and logging with Laravel 13 while preserving safe domain and API responses.
- **Scope:** Exception configuration, handlers or bootstrap callbacks, render/report logic, log channels, context, API errors, and user-facing error pages.
- **Must inspect:**
  - Internal message leakage, swallowed exceptions, duplicate reporting, stale handler classes, and unsafe log context.
  - Different error shapes across entry points.
  - Config-cache compatibility.
- **Required checks:**
  - Trace validation, authorization, domain, external, and unexpected errors.
  - Verify production debug behavior.
  - Check sensitive-data redaction.
- **Required deliverable:**
  - An error taxonomy and migration plan.
  - Required response and logging tests.
  - Exact safe user-facing behavior.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Authentication, Session, and Security Configuration Analyst

- **Mission:** Verify Laravel 13 authentication/session configuration and framework security defaults.
- **Scope:** Auth config, guards, providers, session config, cookies, CSRF/origin settings, password reset, verification, rate limiting, and middleware.
- **Must inspect:**
  - Session fixation, insecure cookie flags, stale auth provider setup, broad CSRF exceptions, and account enumeration.
  - Environment-dependent security defaults.
  - Mismatch between tests and production config.
- **Required checks:**
  - Trace login/logout/reset/verification flows.
  - Check session and cookie behavior.
  - Verify rate limiter keys and stores.
- **Required deliverable:**
  - A security-configuration findings report.
  - Required framework config changes.
  - Authentication/session regression tests.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Laravel 13 Feature Applicability Analyst

- **Mission:** Evaluate new or modern framework features against actual repository requirements.
- **Scope:** Middleware/controller attributes, authorization attributes, cache TTL extension, image APIs, API resources, queue attributes, origin protection, health checks, AI/vector/semantic features, and first-party integrations.
- **Must inspect:**
  - Real use cases, compatibility, operational cost, and testability.
  - Features that duplicate existing working architecture.
  - Features inappropriate under shared-hosting or no-queue constraints.
- **Required checks:**
  - Approve only features with measurable correctness or maintenance benefit.
  - Document not-applicable reasons.
  - Identify required migration and tests for approved features.
- **Required deliverable:**
  - A Laravel 13 applicability matrix.
  - Approved implementation candidates.
  - Rejected features with exact reasons.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

### Framework Regression Test Analyst

- **Mission:** Design comprehensive regression coverage for framework integration changes.
- **Scope:** Boot, routes, middleware, providers, auth, sessions, cache, filesystem, mail, queues, exceptions, and config/route/view caches.
- **Must inspect:**
  - Current gaps and tests coupled to old framework internals.
  - Environment-specific behavior.
  - Route cache and config cache compatibility.
- **Required checks:**
  - Create behavior-focused test scenarios.
  - Separate unit, feature, integration, and smoke checks.
  - Identify tests needed before each risky change.
- **Required deliverable:**
  - A framework regression test plan.
  - Critical smoke path list.
  - Expected command results and failure classifications.
- **Restrictions:**
  - Default to read-only analysis during the discovery phase.
  - Do not modify files owned by another subagent.
  - Do not make broad architectural decisions independently; surface evidence and options to the principal agent.
  - Do not report a guess as a fact. Mark uncertainty and explain what evidence is missing.

## Mandatory plan update before implementation

Update the canonical plan with a dedicated section for this task. At minimum include:

- Every structural difference between the repository and the target Laravel 13 integration points.
- Bootstrap, provider, middleware, route, exception, cache, session, queue, filesystem, mail, logging, auth, and testing changes.
- Project-specific configuration that must be preserved and how it will be proven.
- Laravel 13 feature applicability decisions and tests.
- Security-sensitive defaults that must not be weakened.
- The sequence of framework changes with boot/cache/test checkpoints.
- Deployment and rollback considerations for config, session, queue, and serialized state.
- Regression tests for every changed framework boundary.

Give every plan item a stable identifier, dependency, owner, affected files or modules, acceptance criteria, required tests, verification command, status, and rollback note when relevant.

Save the plan, then immediately begin implementation. Do not return a plan to the user and wait for approval.

## Domain-specific guardrails

- Do not overwrite bootstrap or config files with skeleton defaults. Merge behavior deliberately.
- Do not weaken CSRF, origin, authentication, session, rate limiting, or exception protections to make tests pass.
- Do not introduce AI, vector search, semantic search, Reverb, queues, or image processing solely because Laravel supports them.
- Do not remove custom provider or middleware behavior until every caller and test is understood.

## Required implementation scope

Complete all applicable work below. Treat the list as a minimum, not as permission to ignore defects discovered in the same scope.

- Modernize application bootstrap, provider registration, middleware configuration, route loading, command registration, event/observer registration, and exception handling for Laravel 13.
- Audit and update every configuration file while preserving intentional project values and moving all env access into config.
- Modernize routing, bindings, rate limiters, trusted host/proxy handling, CSRF/origin protection, maintenance mode, and health checks.
- Modernize container bindings, provider lifecycles, contextual dependencies, and boot-time side effects.
- Normalize safe error rendering, reporting, structured log context, and production debug behavior.
- Verify and modernize authentication, session cookies, reset/verification flows, cache/session stores, queues when allowed, filesystem, mail, notifications, and scheduled behavior.
- Evaluate and implement only justified Laravel 13 features, recording every decision in an applicability matrix.
- Repair all affected application code, tests, migrations, seeders, commands, and deployment scripts.
- Add behavior-focused regression tests for each changed framework boundary.
- Verify config, route, and view caching and the application bootstrap in HTTP and CLI contexts.

## Required verification

Run the repository's real commands rather than inventing command names. Perform all safe applicable checks below and capture exact results:

- Application boot through CLI and representative HTTP smoke paths.
- `php artisan about`, `route:list`, configuration inspection, and package discovery.
- Config cache build and clear, route cache build and clear when supported, and view cache build.
- Authentication/session, middleware, provider, exception, logging, cache, filesystem, mail, queue, and health-route tests where applicable.
- Formatter, static analysis, targeted framework tests, full PHP suite, migration/seed checks, and frontend build.
- Production configuration review for debug, cookies, trusted proxies/hosts, CSRF/origin rules, log redaction, and secrets.
- A final comparison against the documented Laravel 13 applicability matrix and canonical requirements.

## Independent review protocol

After implementation and before committing:

1. Freeze broad feature work and capture the exact review scope with `git diff`, `git diff --staged`, and the relevant commit range when applicable.
2. Spawn every reviewer subagent listed below. Reviewer agents must be independent from implementation agents.
3. Run Codex `/review` against uncommitted changes when available.
4. Require reviewers to inspect behavior, not only style.
5. Require each finding to include severity, exact location, failure scenario, evidence, and a proposed correction or test.
6. Have the principal agent reproduce or validate each material finding.
7. Fix every valid in-scope finding.
8. Add a regression test for every behavior defect when practical.
9. Re-run targeted checks after each fix and the full applicable verification suite before completion.
10. Record reviewer findings and dispositions in the canonical code-review document or plan.

## Required independent reviewer subagents

### Laravel Architecture Reviewer

- **Review mission:** Verify that the framework modernization uses Laravel 13 correctly and preserves project-specific behavior.
- **Scope:** Bootstrap, config, providers, middleware, routes, exceptions, and changed integration code.
- **Must inspect:**
  - Blind skeleton replacement, stale APIs, duplicate registration, incorrect lifetimes, and lost custom behavior.
  - Framework features used against repository constraints.
  - Config/route cache failures.
- **Required checks:**
  - Trace custom behavior before and after.
  - Inspect clean-install assumptions.
  - Run critical boot and cache commands.
- **Required deliverable:**
  - Framework correctness findings.
  - Lost-behavior risks.
  - Required corrections and tests.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### Laravel Security Configuration Reviewer

- **Review mission:** Independently assess security-sensitive framework defaults and changes.
- **Scope:** Auth, sessions, cookies, CSRF, origin protection, rate limits, exceptions, logs, trusted proxies/hosts, and health routes.
- **Must inspect:**
  - Protection bypasses, unsafe production defaults, account enumeration, sensitive disclosure, and broad exclusions.
  - Environment values that become insecure when missing.
  - Insufficient negative tests.
- **Required checks:**
  - Model realistic attacks.
  - Inspect configuration cache output behavior.
  - Review production-specific branches.
- **Required deliverable:**
  - Severity-ranked security findings.
  - Required configuration and test fixes.
  - A verified-control list.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

### Framework Regression Reviewer

- **Review mission:** Determine whether framework-level behavior remains stable across CLI, HTTP, tests, and deployment commands.
- **Scope:** Regression tests, smoke tests, cache builds, route output, deployment docs, and command logs.
- **Must inspect:**
  - Removed behavior, untested middleware order, provider side effects, route-name changes, and error-contract drift.
  - Commands reported but not reproducible.
  - Tests tied to implementation details.
- **Required checks:**
  - Re-run key flows.
  - Compare route and config behavior.
  - Inspect failures and skips.
- **Required deliverable:**
  - Regression gaps.
  - Required test or implementation fixes.
  - Final readiness verdict.
- **Restrictions:**
  - Work read-only unless the principal agent explicitly delegates a narrowly scoped fix after the review is complete.
  - Do not assume the implementation is correct because tests are green.
  - Prioritize defects by severity, reproducibility, blast radius, and likelihood.
  - Cite exact files, symbols, line ranges when possible, and provide a concrete failure scenario for every finding.

## Required documentation synchronization

Update the canonical plan plus all affected first-party documentation. At minimum review and update, when present or applicable:

- The canonical plan, compliance matrix, and changelog.
- `AGENTS.md`, architecture, security, deployment, operations, testing, and configuration documentation.
- A Laravel 13 applicability matrix.
- ADRs for material framework choices or removed legacy behavior.
- Updated environment-variable and cache/deployment command references.

Remove stale claims and commands. Keep useful historical changelog information. Ensure documentation describes the implemented and verified state, not the intended future state.

## Definition of done

This prompt is complete only when all applicable conditions are true:

- The application uses Laravel 13 integration points correctly and boots in CLI and HTTP contexts.
- All intentional custom bootstrap, provider, middleware, route, auth, error, and config behavior is preserved or deliberately migrated.
- Security controls are not weakened and have negative tests.
- Config, route when supported, and view caches build successfully.
- Approved Laravel 13 features are implemented and tested; rejected features have clear not-applicable reasons.
- Full applicable verification passes and independent framework, security, and regression reviews are resolved.
- Documentation, commits, and safe push on `main` are complete and factual.

## Required final report

Return one factual report with these sections:

1. Branch and initial working-tree state.
2. Applicable instructions and canonical plan file.
3. Analysis subagents created, their scopes, and the important findings accepted or rejected.
4. Plan changes made before implementation.
5. Implementation summary by module and file group.
6. Database, security, performance, accessibility, localization, factory, seeder, and test impact where applicable.
7. Exact verification commands and observed results.
8. Independent reviewers created, findings, fixes, and remaining rejected findings with reasons.
9. Documentation synchronized.
10. Final Git status, commit hashes, and factual push result.
11. Remaining limitations, containing only genuine external or environmental blockers with exact affected requirement or plan identifiers.

Do not describe planned work as completed. Do not hide unfinished in-scope work in a limitations section.

## Start now

Read the applicable instruction chain and first-party documentation, inspect the repository, create the required analysis subagents, update the canonical plan, and then immediately implement the complete in-scope plan. Do not stop after analysis or planning.
