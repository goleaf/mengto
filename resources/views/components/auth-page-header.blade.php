@props(['eyebrow', 'title', 'description'])

<header class="auth-page-header">
    <p class="auth-page-header__eyebrow">{{ $eyebrow }}</p>
    <h1 class="auth-page-header__title">{{ $title }}</h1>
    <p class="auth-page-header__description">{{ $description }}</p>
</header>
